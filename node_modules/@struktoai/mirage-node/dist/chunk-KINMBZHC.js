// src/resource/slack/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactSlackConfig(config) {
  const out = { token: "<REDACTED>" };
  if (config.searchToken !== void 0) out.searchToken = "<REDACTED>";
  return out;
}
function normalizeSlackConfig(input) {
  return normalizeFields(input, {
    rename: { search_token: "searchToken" }
  });
}

export {
  redactSlackConfig,
  normalizeSlackConfig
};
//# sourceMappingURL=chunk-KINMBZHC.js.map