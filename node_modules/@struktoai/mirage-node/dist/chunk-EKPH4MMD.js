// src/resource/box/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactBoxConfig(config) {
  const out = {};
  if (config.clientId !== void 0) out.clientId = config.clientId;
  if (config.clientSecret !== void 0) out.clientSecret = "<REDACTED>";
  if (config.refreshToken !== void 0) out.refreshToken = "<REDACTED>";
  if (config.accessToken !== void 0) out.accessToken = "<REDACTED>";
  return out;
}
function normalizeBoxConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken",
      access_token: "accessToken",
      developer_token: "accessToken"
    }
  });
}

export {
  redactBoxConfig,
  normalizeBoxConfig
};
//# sourceMappingURL=chunk-EKPH4MMD.js.map