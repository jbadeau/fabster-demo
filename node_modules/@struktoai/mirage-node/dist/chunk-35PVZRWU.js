// src/resource/gdrive/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGDriveConfig(config) {
  return {
    clientId: config.clientId,
    clientSecret: "<REDACTED>",
    refreshToken: "<REDACTED>"
  };
}
function normalizeGDriveConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken"
    }
  });
}

export {
  redactGDriveConfig,
  normalizeGDriveConfig
};
//# sourceMappingURL=chunk-35PVZRWU.js.map