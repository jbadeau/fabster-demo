// src/resource/ssh/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactSshConfig(config) {
  const out = { host: config.host };
  if (config.hostname !== void 0) out.hostname = config.hostname;
  if (config.port !== void 0) out.port = config.port;
  if (config.username !== void 0) out.username = config.username;
  if (config.password !== void 0) out.password = "<REDACTED>";
  if (config.identityFile !== void 0) out.identityFile = config.identityFile;
  if (config.passphrase !== void 0) out.passphrase = "<REDACTED>";
  if (config.root !== void 0) out.root = config.root;
  if (config.timeout !== void 0) out.timeout = config.timeout;
  if (config.knownHosts !== void 0) out.knownHosts = config.knownHosts;
  return out;
}
function normalizeSshConfig(input) {
  return normalizeFields(input, {
    rename: {
      identity_file: "identityFile",
      known_hosts: "knownHosts"
    }
  });
}

export {
  redactSshConfig,
  normalizeSshConfig
};
//# sourceMappingURL=chunk-FZ2GJ4BJ.js.map