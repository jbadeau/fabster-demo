// src/resource/gdocs/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGDocsConfig(config) {
  return {
    clientId: config.clientId,
    clientSecret: "<REDACTED>",
    refreshToken: "<REDACTED>"
  };
}
function normalizeGDocsConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken"
    }
  });
}

export {
  redactGDocsConfig,
  normalizeGDocsConfig
};
//# sourceMappingURL=chunk-3Y625AUP.js.map