// src/resource/linear/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactLinearConfig(config) {
  const out = { apiKey: "<REDACTED>" };
  if (config.workspace !== void 0) out.workspace = config.workspace;
  if (config.teamIds !== void 0) out.teamIds = config.teamIds;
  if (config.baseUrl !== void 0) out.baseUrl = config.baseUrl;
  return out;
}
function normalizeLinearConfig(input) {
  return normalizeFields(input, {
    rename: {
      api_key: "apiKey",
      team_ids: "teamIds",
      base_url: "baseUrl"
    }
  });
}

export {
  redactLinearConfig,
  normalizeLinearConfig
};
//# sourceMappingURL=chunk-6Y6GWMFF.js.map