// src/resource/gsheets/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGSheetsConfig(config) {
  return {
    clientId: config.clientId,
    clientSecret: "<REDACTED>",
    refreshToken: "<REDACTED>"
  };
}
function normalizeGSheetsConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken"
    }
  });
}

export {
  redactGSheetsConfig,
  normalizeGSheetsConfig
};
//# sourceMappingURL=chunk-I4RRGZGB.js.map