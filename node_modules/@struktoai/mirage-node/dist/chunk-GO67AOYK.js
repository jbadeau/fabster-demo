// src/resource/gmail/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGmailConfig(config) {
  return {
    clientId: config.clientId,
    clientSecret: "<REDACTED>",
    refreshToken: "<REDACTED>"
  };
}
function normalizeGmailConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken"
    }
  });
}

export {
  redactGmailConfig,
  normalizeGmailConfig
};
//# sourceMappingURL=chunk-GO67AOYK.js.map