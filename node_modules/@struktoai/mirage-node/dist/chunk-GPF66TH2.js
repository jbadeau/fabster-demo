// src/resource/github_ci/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGitHubCIConfig(config) {
  const out = {
    token: "<REDACTED>",
    owner: config.owner,
    repo: config.repo
  };
  if (config.days !== void 0) out.days = config.days;
  if (config.maxRuns !== void 0) out.maxRuns = config.maxRuns;
  if (config.baseUrl !== void 0) out.baseUrl = config.baseUrl;
  return out;
}
function normalizeGitHubCIConfig(input) {
  return normalizeFields(input, {
    rename: { base_url: "baseUrl" }
  });
}

export {
  redactGitHubCIConfig,
  normalizeGitHubCIConfig
};
//# sourceMappingURL=chunk-GPF66TH2.js.map