// src/resource/dropbox/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactDropboxConfig(config) {
  return {
    clientId: config.clientId,
    clientSecret: "<REDACTED>",
    refreshToken: "<REDACTED>"
  };
}
function normalizeDropboxConfig(input) {
  return normalizeFields(input, {
    rename: {
      client_id: "clientId",
      client_secret: "clientSecret",
      refresh_token: "refreshToken"
    }
  });
}

export {
  redactDropboxConfig,
  normalizeDropboxConfig
};
//# sourceMappingURL=chunk-RQYYYBQG.js.map