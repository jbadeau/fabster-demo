// src/resource/trello/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactTrelloConfig(config) {
  const out = { apiKey: "<REDACTED>", apiToken: "<REDACTED>" };
  if (config.workspaceId !== void 0) out.workspaceId = config.workspaceId;
  if (config.boardIds !== void 0) out.boardIds = config.boardIds;
  if (config.baseUrl !== void 0) out.baseUrl = config.baseUrl;
  return out;
}
function normalizeTrelloConfig(input) {
  return normalizeFields(input, {
    rename: {
      api_key: "apiKey",
      api_token: "apiToken",
      workspace_id: "workspaceId",
      board_ids: "boardIds",
      base_url: "baseUrl"
    }
  });
}

export {
  redactTrelloConfig,
  normalizeTrelloConfig
};
//# sourceMappingURL=chunk-QWWOBAYU.js.map