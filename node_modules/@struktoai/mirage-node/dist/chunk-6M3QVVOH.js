// src/resource/langfuse/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactLangfuseConfig(config) {
  const out = {
    publicKey: config.publicKey,
    secretKey: "<REDACTED>"
  };
  if (config.host !== void 0) out.host = config.host;
  if (config.defaultTraceLimit !== void 0) out.defaultTraceLimit = config.defaultTraceLimit;
  if (config.defaultSearchLimit !== void 0) out.defaultSearchLimit = config.defaultSearchLimit;
  if (config.defaultFromTimestamp !== void 0) {
    out.defaultFromTimestamp = config.defaultFromTimestamp;
  }
  return out;
}
function normalizeLangfuseConfig(input) {
  return normalizeFields(input, {
    rename: {
      public_key: "publicKey",
      secret_key: "secretKey",
      default_trace_limit: "defaultTraceLimit",
      default_search_limit: "defaultSearchLimit",
      default_from_timestamp: "defaultFromTimestamp"
    }
  });
}

export {
  redactLangfuseConfig,
  normalizeLangfuseConfig
};
//# sourceMappingURL=chunk-6M3QVVOH.js.map