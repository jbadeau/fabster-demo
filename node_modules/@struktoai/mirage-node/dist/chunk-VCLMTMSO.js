// src/resource/github/config.ts
import { normalizeFields } from "@struktoai/mirage-core";
function redactGitHubConfig(config) {
  const out = {
    token: "<REDACTED>",
    owner: config.owner,
    repo: config.repo
  };
  if (config.ref !== void 0) out.ref = config.ref;
  if (config.baseUrl !== void 0) out.baseUrl = config.baseUrl;
  return out;
}
function normalizeGitHubConfig(input) {
  return normalizeFields(input, {
    rename: { base_url: "baseUrl" }
  });
}

export {
  redactGitHubConfig,
  normalizeGitHubConfig
};
//# sourceMappingURL=chunk-VCLMTMSO.js.map