import { Node } from 'web-tree-sitter';
export { Node as ShellNode } from 'web-tree-sitter';
import { OAuthClientProvider } from '@modelcontextprotocol/sdk/client/auth.js';
import { Client } from '@modelcontextprotocol/sdk/client/index.js';
import { OAuthClientMetadata, OAuthClientInformationMixed, OAuthTokens } from '@modelcontextprotocol/sdk/shared/auth.js';

declare const MountMode: Readonly<{
    readonly READ: "read";
    readonly WRITE: "write";
    readonly EXEC: "exec";
}>;
type MountMode = (typeof MountMode)[keyof typeof MountMode];
declare const ConsistencyPolicy: Readonly<{
    readonly LAZY: "lazy";
    readonly ALWAYS: "always";
}>;
type ConsistencyPolicy = (typeof ConsistencyPolicy)[keyof typeof ConsistencyPolicy];
declare const ResourceName: Readonly<{
    readonly DISK: "disk";
    readonly S3: "s3";
    readonly RAM: "ram";
    readonly GITHUB: "github";
    readonly LINEAR: "linear";
    readonly GDOCS: "gdocs";
    readonly GSHEETS: "gsheets";
    readonly GSLIDES: "gslides";
    readonly GDRIVE: "gdrive";
    readonly DROPBOX: "dropbox";
    readonly BOX: "box";
    readonly SLACK: "slack";
    readonly DISCORD: "discord";
    readonly GMAIL: "gmail";
    readonly TRELLO: "trello";
    readonly TELEGRAM: "telegram";
    readonly MONGODB: "mongodb";
    readonly NOTION: "notion";
    readonly LANGFUSE: "langfuse";
    readonly SSH: "ssh";
    readonly REDIS: "redis";
    readonly GITHUB_CI: "github_ci";
    readonly GCS: "gcs";
    readonly OCI: "oci";
    readonly R2: "r2";
    readonly EMAIL: "email";
    readonly PAPERCLIP: "paperclip";
    readonly OPFS: "opfs";
    readonly SUPABASE: "supabase";
    readonly POSTGRES: "postgres";
    readonly SSCHOLAR_PAPER: "sscholar-paper";
    readonly SSCHOLAR_AUTHOR: "sscholar-author";
    readonly VERCEL: "vercel";
    readonly POSTHOG: "posthog";
}>;
type ResourceName = (typeof ResourceName)[keyof typeof ResourceName];
declare const DEFAULT_SESSION_ID = "default";
declare const DEFAULT_AGENT_ID = "default";
declare const FileType: Readonly<{
    readonly DIRECTORY: "directory";
    readonly TEXT: "text";
    readonly BINARY: "binary";
    readonly JSON: "json";
    readonly CSV: "csv";
    readonly IMAGE_PNG: "image/png";
    readonly IMAGE_JPEG: "image/jpeg";
    readonly IMAGE_GIF: "image/gif";
    readonly ZIP: "application/zip";
    readonly GZIP: "application/gzip";
    readonly PDF: "application/pdf";
    readonly PARQUET: "parquet";
    readonly ORC: "orc";
    readonly FEATHER: "feather";
    readonly HDF5: "hdf5";
}>;
type FileType = (typeof FileType)[keyof typeof FileType];
interface FileStatInit {
    name: string;
    size?: number | null;
    modified?: string | null;
    fingerprint?: string | null;
    type?: FileType | null;
    extra?: Record<string, unknown>;
}
declare class FileStat {
    readonly name: string;
    readonly size: number | null;
    readonly modified: string | null;
    readonly fingerprint: string | null;
    readonly type: FileType | null;
    readonly extra: Record<string, unknown>;
    constructor(init: FileStatInit);
}
interface PathSpecInit {
    original: string;
    directory: string;
    pattern?: string | null;
    resolved?: boolean;
    prefix?: string;
}
declare class PathSpec {
    readonly original: string;
    readonly directory: string;
    readonly pattern: string | null;
    readonly resolved: boolean;
    readonly prefix: string;
    constructor(init: PathSpecInit);
    get stripPrefix(): string;
    get key(): string;
    get dir(): PathSpec;
    child(name: string): string;
    static fromStrPath(path: string, prefix?: string): PathSpec;
}

declare class Accessor {
    [key: string]: unknown;
}
declare class NOOPAccessor extends Accessor {
}

declare const ResourceType: Readonly<{
    readonly FILE: "file";
    readonly FOLDER: "folder";
}>;
type ResourceType = (typeof ResourceType)[keyof typeof ResourceType];
declare const LookupStatus: Readonly<{
    readonly EXPIRED: "expired";
    readonly NOT_FOUND: "not_found";
}>;
type LookupStatus = (typeof LookupStatus)[keyof typeof LookupStatus];
declare const IndexType: Readonly<{
    readonly RAM: "ram";
    readonly REDIS: "redis";
}>;
type IndexType = (typeof IndexType)[keyof typeof IndexType];
interface IndexEntryInit {
    id: string;
    name: string;
    resourceType: string;
    remoteTime?: string;
    indexTime?: string;
    vfsName?: string;
    size?: number | null;
}
declare class IndexEntry {
    id: string;
    name: string;
    resourceType: string;
    remoteTime: string;
    indexTime: string;
    vfsName: string;
    size: number | null;
    constructor(init: IndexEntryInit);
    copyWith(updates: Partial<IndexEntryInit>): IndexEntry;
}
interface LookupResult {
    entry?: IndexEntry | null;
    status?: LookupStatus | null;
}
interface ListResult {
    entries?: string[] | null;
    status?: LookupStatus | null;
}
interface IndexConfig {
    type?: IndexType;
    ttl?: number;
}

declare abstract class IndexCacheStore {
    abstract get(resourcePath: string): Promise<LookupResult>;
    abstract put(resourcePath: string, entry: IndexEntry): Promise<void>;
    abstract listDir(resourcePath: string): Promise<ListResult>;
    abstract setDir(resourcePath: string, entries: readonly [string, IndexEntry][], expiredAt?: Date | null): Promise<void>;
    abstract invalidateDir(resourcePath: string): Promise<void>;
    abstract clear(): Promise<void>;
}

declare class RAMIndexCacheStore extends IndexCacheStore {
    private readonly ttl;
    private readonly entries;
    private readonly children;
    private readonly expiry;
    private readonly lock;
    constructor(options?: {
        ttl?: number;
    });
    static fromConfig(config: IndexConfig): RAMIndexCacheStore;
    get(resourcePath: string): Promise<LookupResult>;
    put(resourcePath: string, entry: IndexEntry): Promise<void>;
    listDir(resourcePath: string): Promise<ListResult>;
    setDir(resourcePath: string, entries: readonly [string, IndexEntry][], expiredAt?: Date | null): Promise<void>;
    invalidateDir(resourcePath: string): Promise<void>;
    clear(): Promise<void>;
}

type ByteSource = Uint8Array | AsyncIterable<Uint8Array>;
declare function materialize(source: ByteSource | null | undefined): Promise<Uint8Array>;
interface IOResultInit {
    stdout?: ByteSource | null;
    stderr?: ByteSource | null;
    exitCode?: number;
    reads?: Record<string, ByteSource>;
    writes?: Record<string, ByteSource>;
    cache?: string[];
}
declare class IOResult {
    stdout: ByteSource | null;
    stderr: ByteSource | null;
    exitCode: number;
    reads: Record<string, ByteSource>;
    writes: Record<string, ByteSource>;
    cache: string[];
    streamSource: IOResult | null;
    constructor(init?: IOResultInit);
    materializeStdout(): Promise<Uint8Array>;
    stdoutStr(errors?: 'replace' | 'strict'): Promise<string>;
    materializeStderr(): Promise<Uint8Array>;
    stderrStr(errors?: 'replace' | 'strict'): Promise<string>;
    syncExitCode(): void;
    merge(other: IOResult): Promise<IOResult>;
    mergeAggregate(other: IOResult): Promise<IOResult>;
}

type AggregateResult = [path: string, data: Uint8Array];
declare function concatAggregate(results: AggregateResult[]): Uint8Array;
declare function headerAggregate(results: AggregateResult[]): Uint8Array;
declare function prefixAggregate(results: AggregateResult[]): Uint8Array;
declare function wcAggregate(results: AggregateResult[]): Uint8Array;

declare const OperandKind: Readonly<{
    readonly NONE: "none";
    readonly PATH: "path";
    readonly TEXT: "text";
}>;
type OperandKind = (typeof OperandKind)[keyof typeof OperandKind];
interface OptionInit {
    short?: string | null;
    long?: string | null;
    valueKind?: OperandKind;
    numericShorthand?: boolean;
    description?: string;
}
declare class Option {
    readonly short: string | null;
    readonly long: string | null;
    readonly valueKind: OperandKind;
    readonly numericShorthand: boolean;
    readonly description: string | null;
    constructor(init?: OptionInit);
}
interface OperandInit {
    kind?: OperandKind;
}
declare class Operand {
    readonly kind: OperandKind;
    constructor(init?: OperandInit);
}
interface CommandSpecInit {
    options?: readonly Option[];
    positional?: readonly Operand[];
    rest?: Operand | null;
    ignoreTokens?: readonly string[];
    description?: string;
}
declare class CommandSpec {
    readonly options: readonly Option[];
    readonly positional: readonly Operand[];
    readonly rest: Operand | null;
    readonly ignoreTokens: ReadonlySet<string>;
    readonly description: string | null;
    constructor(init?: CommandSpecInit);
}
interface ParsedArgsInit {
    flags: Record<string, string | boolean>;
    args: [string, OperandKind][];
    cachePaths?: string[];
    pathFlagValues?: string[];
}
declare class ParsedArgs {
    readonly flags: Record<string, string | boolean>;
    readonly args: [string, OperandKind][];
    readonly cachePaths: string[];
    readonly pathFlagValues: string[];
    constructor(init: ParsedArgsInit);
    paths(): string[];
    routingPaths(): string[];
    texts(): string[];
    flag(name: string, fallback?: string | boolean | null): string | boolean | null;
}

/**
 * Options bag passed to command functions. Mirrors Python's keyword arguments
 * (`command="", stdin=None, index=None, prefix="", **_extra`) collected into
 * a single typed object.
 *
 * Parity note: Python's `handle_command_provision` passes `command`, `prefix`,
 * and `index` as keyword arguments directly. TS surfaces them here so
 * provision functions can read them via `opts.command`, `opts.mountPrefix`,
 * and `opts.index` (matching Python's `command=`, `prefix=`, `index=`).
 */
type CommandDispatch = (op: string, path: PathSpec, args?: readonly unknown[], kwargs?: Record<string, unknown>) => Promise<[unknown, IOResult]>;
interface CommandHistory {
    entries(): readonly {
        readonly command: string;
        readonly sessionId: string;
    }[];
    clear(): void;
}
interface CommandOpts {
    stdin: ByteSource | null;
    flags: Record<string, string | boolean>;
    filetypeFns: Record<string, CommandFn> | null;
    mountPrefix?: string;
    cwd: string;
    resource: Resource;
    command?: string;
    index?: IndexCacheStore | null;
    dispatch?: CommandDispatch;
    history?: CommandHistory;
    sessionId?: string;
}
type CommandFnResult = [ByteSource | null, IOResult] | null;
/**
 * Command function signature mirroring Python's
 * `async def cat(accessor, paths, *texts, stdin=None, n=False, **_extra)`.
 * TS gets four positional params: accessor, paths, texts (Python `*texts`),
 * and an opts bag (Python `**kwargs`). Generic on the accessor type so
 * resource-specific commands can declare e.g. `accessor: RAMAccessor`.
 */
type CommandFn<A extends Accessor = Accessor> = (accessor: A, paths: PathSpec[], texts: string[], opts: CommandOpts) => Promise<CommandFnResult> | CommandFnResult;
type ProvisionFn<A extends Accessor = Accessor> = (accessor: A, paths: PathSpec[], texts: string[], opts: CommandOpts) => unknown;
type AggregateFn = (results: AggregateResult[]) => Uint8Array;
interface RegisteredCommandInit {
    name: string;
    spec: CommandSpec;
    resource: string | null;
    filetype?: string | null;
    fn: CommandFn;
    provisionFn?: ProvisionFn | null;
    aggregate?: AggregateFn | null;
    src?: string | null;
    dst?: string | null;
    write?: boolean;
}
declare class RegisteredCommand {
    readonly name: string;
    readonly spec: CommandSpec;
    readonly resource: string | null;
    readonly filetype: string | null;
    readonly fn: CommandFn;
    readonly provisionFn: ProvisionFn | null;
    readonly aggregate: AggregateFn | null;
    readonly src: string | null;
    readonly dst: string | null;
    readonly write: boolean;
    constructor(init: RegisteredCommandInit);
}
interface CommandOptions<A extends Accessor = Accessor> {
    name: string;
    resource: string | string[] | null;
    spec: CommandSpec;
    fn: CommandFn<A>;
    filetype?: string | null;
    provision?: ProvisionFn<A> | null;
    aggregate?: AggregateFn | null;
    write?: boolean;
}
declare function command<A extends Accessor = Accessor>(options: CommandOptions<A>): RegisteredCommand[];
interface CrossCommandOptions {
    name: string;
    src: string;
    dst: string;
    spec: CommandSpec;
    fn: CommandFn;
}
declare function crossCommand(options: CrossCommandOptions): RegisteredCommand;

interface OpKwargs {
    index?: IndexCacheStore;
    filetype?: string | null;
    [k: string]: unknown;
}
type OpFn = (accessor: Accessor, path: PathSpec, args: readonly unknown[], kwargs: OpKwargs) => unknown;
interface RegisteredOp {
    name: string;
    resource: string | null;
    filetype: string | null;
    fn(this: void, accessor: Accessor, path: PathSpec, args: readonly unknown[], kwargs: OpKwargs): unknown;
    write: boolean;
}
interface OpOptions {
    resource: string | string[];
    filetype?: string | null;
    write?: boolean;
}
declare function op(name: string, options: OpOptions): (target: OpFn, _context: ClassMethodDecoratorContext) => void;
declare class OpsRegistry {
    private readonly registered;
    register(ro: RegisteredOp): void;
    unregisterResource(resourceKind: string): void;
    registerResource(resource: Resource): void;
    find(name: string, resource: string | null, filetype?: string | null): RegisteredOp | null;
    resolve(name: string, resource: string, filetype?: string | null): OpFn;
    call(name: string, resourceKind: string, accessor: Accessor, path: PathSpec, args?: readonly unknown[], kwargs?: OpKwargs): Promise<unknown>;
}
declare function registerOp(registry: OpsRegistry, name: string, fn: OpFn, options: OpOptions): void;

interface FindOptions {
    name?: string | null;
    type?: 'f' | 'd' | null;
    minSize?: number | null;
    maxSize?: number | null;
    maxDepth?: number | null;
    minDepth?: number | null;
    nameExclude?: string | null;
    orNames?: string[] | null;
    iname?: string | null;
    pathPattern?: string | null;
    mtimeMin?: number | null;
    mtimeMax?: number | null;
}
interface Resource {
    readonly kind: string;
    readonly prompt?: string;
    readonly writePrompt?: string;
    readonly indexTtl?: number;
    readonly isRemote?: boolean;
    readonly index?: IndexCacheStore;
    readonly accessor?: Accessor;
    readonly opsMap?: Record<string, unknown>;
    open(): Promise<void>;
    close(): Promise<void>;
    ops?(): readonly RegisteredOp[];
    commands?(): readonly RegisteredCommand[];
    streamPath?(path: PathSpec): AsyncIterable<Uint8Array>;
    readFile?(path: PathSpec): Promise<Uint8Array>;
    writeFile?(path: PathSpec, data: Uint8Array): Promise<void>;
    appendFile?(path: PathSpec, data: Uint8Array): Promise<void>;
    readdir?(path: PathSpec): Promise<string[]>;
    stat?(path: PathSpec): Promise<FileStat>;
    exists?(path: PathSpec): Promise<boolean>;
    mkdir?(path: PathSpec, options?: {
        recursive?: boolean;
    }): Promise<void>;
    rmdir?(path: PathSpec): Promise<void>;
    unlink?(path: PathSpec): Promise<void>;
    rename?(src: PathSpec, dst: PathSpec): Promise<void>;
    truncate?(path: PathSpec, length: number): Promise<void>;
    copy?(src: PathSpec, dst: PathSpec): Promise<void>;
    rmR?(path: PathSpec): Promise<void>;
    du?(path: PathSpec): Promise<number>;
    find?(path: PathSpec, options?: FindOptions): Promise<string[]>;
    glob?(paths: readonly PathSpec[], prefix?: string): Promise<PathSpec[]>;
    fingerprint?(path: PathSpec): Promise<string | null>;
}
declare function throwUnsupported(op: string): never;

declare class RAMStore {
    readonly files: Map<string, Uint8Array<ArrayBufferLike>>;
    readonly dirs: Set<string>;
    readonly modified: Map<string, string>;
}

declare class RAMAccessor extends Accessor {
    readonly store: RAMStore;
    constructor(store: RAMStore);
}

interface RAMResourceState {
    type: string;
    needsOverride: boolean;
    redactedFields: string[];
    files: Record<string, Uint8Array>;
    dirs: string[];
    modified: Record<string, string>;
}
declare class RAMResource implements Resource {
    readonly kind: "ram";
    readonly isRemote: boolean;
    readonly indexTtl: number;
    readonly store: RAMStore;
    readonly accessor: RAMAccessor;
    readonly index: IndexCacheStore;
    readonly prompt = "{prefix}\n  In-memory filesystem. Files and directories are created on demand.\n  Standard commands: ls, cat, head, tail, grep, wc, find, tree, mkdir, touch, cp, mv, rm, tee, echo > file.";
    readonly opsMap: Record<string, unknown>;
    open(): Promise<void>;
    close(): Promise<void>;
    ops(): readonly RegisteredOp[];
    commands(): readonly RegisteredCommand[];
    streamPath(path: PathSpec): AsyncIterable<Uint8Array>;
    readFile(path: PathSpec): Promise<Uint8Array>;
    writeFile(path: PathSpec, data: Uint8Array): Promise<void>;
    appendFile(path: PathSpec, data: Uint8Array): Promise<void>;
    readdir(path: PathSpec): Promise<string[]>;
    stat(path: PathSpec): Promise<FileStat>;
    exists(path: PathSpec): Promise<boolean>;
    mkdir(path: PathSpec, options?: {
        recursive?: boolean;
    }): Promise<void>;
    rmdir(path: PathSpec): Promise<void>;
    unlink(path: PathSpec): Promise<void>;
    rename(src: PathSpec, dst: PathSpec): Promise<void>;
    truncate(path: PathSpec, length: number): Promise<void>;
    copy(src: PathSpec, dst: PathSpec): Promise<void>;
    rmR(path: PathSpec): Promise<void>;
    du(path: PathSpec): Promise<number>;
    find(path: PathSpec, options?: FindOptions): Promise<string[]>;
    glob(paths: readonly PathSpec[], prefix?: string): Promise<PathSpec[]>;
    getState(): RAMResourceState;
    loadState(state: RAMResourceState): void;
}

declare class DevResource extends RAMResource {
    readonly store: RAMStore;
    readonly accessor: RAMAccessor;
}

declare class DevFiles extends Map<string, Uint8Array> {
    has(key: string): boolean;
    get(key: string): Uint8Array | undefined;
    set(_key: string, _value: Uint8Array): this;
    delete(_key: string): boolean;
    clear(): void;
    get size(): number;
    keys(): MapIterator<string>;
    values(): MapIterator<Uint8Array>;
    entries(): MapIterator<[string, Uint8Array]>;
    [Symbol.iterator](): MapIterator<[string, Uint8Array]>;
    forEach(callback: (value: Uint8Array, key: string, map: Map<string, Uint8Array>) => void, thisArg?: unknown): void;
}
declare class DevStore {
    readonly files: Map<string, Uint8Array>;
    readonly dirs: Set<string>;
    readonly modified: Map<string, string>;
}

interface FileCache {
    get(key: string): Promise<Uint8Array | null>;
    set(key: string, data: Uint8Array, options?: {
        fingerprint?: string | null;
        ttl?: number | null;
    }): Promise<void>;
    add(key: string, data: Uint8Array, options?: {
        fingerprint?: string | null;
        ttl?: number | null;
    }): Promise<boolean>;
    remove(key: string): Promise<void>;
    exists(key: string | PathSpec): Promise<boolean>;
    isFresh(key: string, remoteFingerprint: string): Promise<boolean>;
    clear(): Promise<void>;
    allCached(keys: readonly string[]): Promise<boolean>;
    multiGet(keys: readonly string[]): Promise<(Uint8Array | null)[]>;
    readonly cacheSize: number;
    readonly cacheLimit: number;
    maxDrainBytes: number | null;
}

interface OpRecordInit {
    op: string;
    path: string;
    source: string;
    bytes: number;
    timestamp: number;
    durationMs: number;
}
declare class OpRecord {
    readonly op: string;
    readonly path: string;
    readonly source: string;
    bytes: number;
    readonly timestamp: number;
    durationMs: number;
    constructor(init: OpRecordInit);
    get isCache(): boolean;
    toJSON(): Record<string, unknown>;
}

interface ExecutionNodeInit {
    command?: string | null;
    op?: string | null;
    stderr?: Uint8Array;
    exitCode?: number;
    children?: ExecutionNode[];
    records?: OpRecord[];
}
declare class ExecutionNode {
    command: string | null;
    op: string | null;
    stderr: Uint8Array;
    exitCode: number;
    children: ExecutionNode[];
    records: OpRecord[];
    constructor(init?: ExecutionNodeInit);
    toJSON(): Record<string, unknown>;
}
interface ExecutionRecordInit {
    agent: string;
    command: string;
    stdout: Uint8Array;
    stdin?: Uint8Array | null;
    exitCode: number;
    tree: ExecutionNode;
    timestamp: number;
    sessionId?: string;
}
declare class ExecutionRecord {
    readonly agent: string;
    readonly command: string;
    readonly stdout: Uint8Array;
    readonly stdin: Uint8Array | null;
    readonly exitCode: number;
    readonly tree: ExecutionNode;
    readonly timestamp: number;
    readonly sessionId: string;
    constructor(init: ExecutionRecordInit);
    toJSON(): Record<string, unknown>;
}

declare class Observer {
    readonly resource: Resource;
    readonly prefix: string;
    private readonly sessionIds;
    constructor(resource: Resource, prefix?: string);
    get sessions(): ReadonlySet<string>;
    isObserverPath(path: string): boolean;
    logOp(rec: OpRecord, agent: string, session: string, cwd?: string): Promise<void>;
    logCommand(rec: ExecutionRecord, cwd?: string): Promise<void>;
    private append;
}

declare const JobStatus: Readonly<{
    readonly RUNNING: "running";
    readonly COMPLETED: "completed";
    readonly KILLED: "killed";
}>;
type JobStatus = (typeof JobStatus)[keyof typeof JobStatus];
type JobTaskResult = [ByteSource | null, IOResult, ExecutionNode];
declare class Job {
    readonly id: number;
    readonly command: string;
    readonly task: Promise<JobTaskResult>;
    readonly abort: AbortController;
    readonly cwd: string;
    readonly agent: string;
    readonly sessionId: string;
    readonly createdAt: number;
    status: JobStatus;
    stdout: Uint8Array;
    stderr: Uint8Array;
    exitCode: number;
    executionNode: ExecutionNode | null;
    ioResult: IOResult | null;
    constructor(init: {
        id: number;
        command: string;
        task: Promise<JobTaskResult>;
        abort: AbortController;
        cwd: string;
        agent?: string;
        sessionId?: string;
    });
}
declare class JobTable {
    private readonly jobs;
    private nextId;
    submit(init: {
        command: string;
        task: Promise<JobTaskResult>;
        abort: AbortController;
        cwd: string;
        agent?: string;
        sessionId?: string;
    }): Job;
    get(jobId: number): Job | null;
    listJobs(): Job[];
    runningJobs(): Job[];
    kill(jobId: number): boolean;
    wait(jobId: number): Promise<Job>;
    waitAll(): Promise<Job[]>;
    popCompleted(): Job[];
}

interface ShellParserConfig {
    engineWasm: Uint8Array | ArrayBuffer;
    grammarWasm: Uint8Array | ArrayBuffer;
}
interface ShellParser {
    parse(command: string): Node;
}

declare function createShellParser(config: ShellParserConfig): Promise<ShellParser>;

interface ResourceStateBase {
    type: string;
    needsOverride?: boolean;
}
type ResourceState = RAMResourceState | (ResourceStateBase & Record<string, unknown>);
interface MountSnapshot {
    index: number;
    prefix: string;
    mode: string;
    resourceClass: string;
    resourceState: ResourceState;
}
interface CacheEntrySnapshot {
    key: string;
    data: Uint8Array;
    fingerprint: string | null;
    ttl: number | null;
    cachedAt: number;
    size: number;
}
interface CacheSnapshot {
    limit: number;
    entries: CacheEntrySnapshot[];
}
interface ExecutionNodeSnapshot {
    command: string | null;
    op: string | null;
    stderr: Uint8Array;
    exitCode: number;
    children: ExecutionNodeSnapshot[];
}
interface ExecutionRecordSnapshot {
    agent: string;
    command: string;
    stdout: Uint8Array;
    stdin: Uint8Array | null;
    exitCode: number;
    tree: ExecutionNodeSnapshot;
    timestamp: number;
    sessionId: string;
}
interface WorkspaceStateDict {
    version: number;
    mounts: MountSnapshot[];
    cache: CacheSnapshot;
    history: ExecutionRecordSnapshot[];
}

declare const Precision: Readonly<{
    readonly EXACT: "exact";
    readonly RANGE: "range";
    readonly UNKNOWN: "unknown";
    readonly UPPER_BOUND: "upper_bound";
}>;
type Precision = (typeof Precision)[keyof typeof Precision];
interface ProvisionResultInit {
    op?: string | null;
    command?: string | null;
    children?: ProvisionResult[];
    networkReadLow?: number;
    networkReadHigh?: number;
    cacheReadLow?: number;
    cacheReadHigh?: number;
    networkWriteLow?: number;
    networkWriteHigh?: number;
    cacheWriteLow?: number;
    cacheWriteHigh?: number;
    readOps?: number;
    cacheHits?: number;
    precision?: Precision;
    estimatedCostUsd?: number | null;
}
declare class ProvisionResult {
    op: string | null;
    command: string | null;
    children: ProvisionResult[];
    networkReadLow: number;
    networkReadHigh: number;
    cacheReadLow: number;
    cacheReadHigh: number;
    networkWriteLow: number;
    networkWriteHigh: number;
    cacheWriteLow: number;
    cacheWriteHigh: number;
    readOps: number;
    cacheHits: number;
    precision: Precision;
    estimatedCostUsd: number | null;
    constructor(init?: ProvisionResultInit);
    private fmtRange;
    get networkRead(): string;
    get cacheRead(): string;
    get networkWrite(): string;
    get cacheWrite(): string;
}

type Resolver = (path: string) => Promise<[Resource, PathSpec, MountMode]>;
declare class WorkspaceFS {
    private readonly resolver;
    private readonly ops;
    constructor(resolver: Resolver, ops: OpsRegistry);
    readFile(path: string, options?: {
        raw?: boolean;
    }): Promise<Uint8Array>;
    readFileText(path: string, encoding?: string): Promise<string>;
    writeFile(path: string, data: Uint8Array | string): Promise<void>;
    readdir(path: string): Promise<string[]>;
    stat(path: string): Promise<FileStat>;
    exists(path: string): Promise<boolean>;
    isDir(path: string): Promise<boolean>;
    isFile(path: string): Promise<boolean>;
    mkdir(path: string): Promise<void>;
    unlink(path: string): Promise<void>;
    rmdir(path: string): Promise<void>;
    rename(src: string, dst: string): Promise<void>;
    cat(path: string): Promise<string>;
    listFiles(path: string): Promise<string[]>;
}

interface MountInit {
    prefix: string;
    resource: Resource;
    mode?: MountMode;
    consistency?: ConsistencyPolicy;
}
declare class Mount {
    readonly prefix: string;
    readonly resource: Resource;
    readonly mode: MountMode;
    readonly consistency: ConsistencyPolicy;
    private readonly cmds;
    private readonly generalCmds;
    private readonly cmdSpecs;
    private readonly ops;
    private readonly generalOps;
    private readonly crossCmds;
    constructor(init: MountInit);
    register(cmd: RegisteredCommand): void;
    registerGeneral(cmd: RegisteredCommand): void;
    resolveCommand(cmdName: string, extension?: string | null): RegisteredCommand | null;
    isGeneralCommand(cmdName: string): boolean;
    allCommands(): readonly RegisteredCommand[];
    specFor(cmdName: string): CommandSpec | null;
    filetypeHandlers(cmdName: string): Record<string, CommandFn>;
    unregister(names: string[]): void;
    commands(): Record<string, (string | null)[]>;
    registeredOps(): Record<string, (string | null)[]>;
    registerCross(cmd: RegisteredCommand, targetResourceType: string): void;
    resolveCross(cmdName: string, targetResourceType: string): RegisteredCommand | null;
    registerOp(op: RegisteredOp): void;
    registerGeneralOp(op: RegisteredOp): void;
    /**
     * Batch-register commands and ops. Mirrors Python's
     * `Mount.register_fns(...)`. Each entry is a `RegisteredCommand` or
     * `RegisteredOp`; commands with `resource: null` go to the general
     * table, ops with `resource: null` likewise. Resource-specific
     * entries must match this mount's resource kind.
     */
    registerFns(items: readonly (RegisteredCommand | RegisteredOp)[]): void;
    private resolveCascade;
    executeCmd(cmdName: string, paths: PathSpec[], texts: string[], flags: Record<string, string | boolean>, opts?: {
        stdin?: ByteSource | null;
        cwd?: string;
        dispatch?: CommandDispatch;
        history?: CommandHistory;
        sessionId?: string;
    }): Promise<[ByteSource | null, IOResult]>;
    executeOp(opName: string, path: string, args?: readonly unknown[], kwargs?: OpKwargs): Promise<unknown>;
}

interface OpsMountInfo {
    prefix: string;
    resourceType: string;
    mode: MountMode;
}
declare class MountRegistry {
    private readonly mountList;
    private defaultMountRef;
    private consistency;
    private readonly defaultMode;
    constructor(resources: Record<string, Resource>, defaultMode: MountMode, modeOverrides?: Record<string, MountMode>);
    setConsistency(consistency: ConsistencyPolicy): void;
    getConsistency(): ConsistencyPolicy;
    /**
     * Add a mount dynamically. Mirrors Python's `registry.mount(...)`.
     * Registers the resource's commands and ops on the new mount and
     * re-sorts mounts by prefix length (longest first).
     */
    mount(prefix: string, resource: Resource, mode?: MountMode, consistency?: ConsistencyPolicy): Mount;
    /**
     * Remove a mount by exact prefix. Mirrors Python's `registry.unmount(...)`.
     * Per-mount commands and ops live on the Mount instance and die with it.
     * The /dev/ mount is reserved and cannot be removed.
     */
    unmount(prefix: string): Mount;
    mountForPrefix(prefix: string): Mount | null;
    isMountRoot(path: string): boolean;
    descendantMounts(path: string): Mount[];
    childMountNames(parentPath: string, includeHidden?: boolean): string[];
    opsMounts(): OpsMountInfo[];
    findResourceByName(resourceName: string | null): Resource | null;
    getResourceType(path: string | null): string | null;
    groupByMount(paths: readonly string[]): [Mount, string[]][];
    setDefaultMount(resource: Resource): Mount;
    get defaultMount(): Mount | null;
    resolve(path: string): [Resource, PathSpec, MountMode];
    mountFor(path: string): Mount | null;
    allMounts(): readonly Mount[];
    isExecAllowed(): boolean;
    mountForCommand(cmdName: string): Mount | null;
    resolveMount(cmdName: string, pathScopes: readonly PathSpec[], cwd: string): Promise<Mount | null>;
    private evictStale;
}

interface PythonRunArgs {
    code: string;
    args: string[];
    env: Record<string, string>;
    stdin: Uint8Array | null;
}
interface PythonRunResult {
    stdout: Uint8Array;
    stderr: Uint8Array;
    exitCode: number;
}
interface PythonReplRunArgs {
    code: string;
    sessionId: string;
}
type ReplStatus = 'complete' | 'incomplete' | 'exit';
interface PythonReplRunResult {
    stdout: Uint8Array;
    stderr: Uint8Array;
    exitCode: number;
    status: ReplStatus;
}

declare class AsyncLineIterator implements AsyncIterableIterator<Uint8Array> {
    private readonly source;
    private buf;
    private exhausted;
    constructor(source: AsyncIterable<Uint8Array> | AsyncIterator<Uint8Array>);
    [Symbol.asyncIterator](): AsyncIterableIterator<Uint8Array>;
    next(): Promise<IteratorResult<Uint8Array>>;
    readline(): Promise<Uint8Array | null>;
    remainingBytes(): Promise<Uint8Array>;
}

interface SessionInit {
    sessionId: string;
    cwd?: string;
    env?: Record<string, string>;
    createdAt?: number;
    functions?: Record<string, unknown>;
    lastExitCode?: number;
    positionalArgs?: string[];
}
declare class Session {
    readonly sessionId: string;
    cwd: string;
    env: Record<string, string>;
    readonly createdAt: number;
    functions: Record<string, unknown>;
    lastExitCode: number;
    positionalArgs: string[];
    stdinBuffer: AsyncLineIterator | null;
    localVars: Map<string, string | null> | null;
    constructor(init: SessionInit);
    toJSON(): Record<string, unknown>;
    static fromJSON(data: {
        sessionId: string;
        cwd?: string;
        env?: Record<string, string>;
        createdAt?: number;
    }): Session;
}

declare class SessionManager {
    private readonly sessions;
    readonly defaultId: string;
    constructor(defaultSessionId: string);
    get cwd(): string;
    set cwd(value: string);
    get env(): Record<string, string>;
    set env(value: Record<string, string>);
    create(sessionId: string): Session;
    get(sessionId: string): Session;
    list(): Session[];
    close(sessionId: string): Promise<void>;
    closeAll(): Promise<void>;
    private defaultSession;
}

interface ExecutionHistoryOptions {
    maxEntries?: number;
    onPersist?: (record: ExecutionRecord) => void | Promise<void>;
}
declare class ExecutionHistory {
    private readonly buffer;
    private readonly maxEntries;
    private readonly onPersist;
    constructor(options?: ExecutionHistoryOptions);
    append(record: ExecutionRecord): Promise<void>;
    entries(): ExecutionRecord[];
    clear(): void;
}

interface WorkspaceOptions {
    mode?: MountMode;
    modeOverrides?: Record<string, MountMode>;
    ops?: OpsRegistry;
    shellParser?: ShellParser;
    shellParserFactory?: () => Promise<ShellParser>;
    agentId?: string;
    sessionId?: string;
    cacheLimit?: string | number;
    cache?: FileCache & Resource;
    observerResource?: Resource;
    observerPrefix?: string;
    python?: {
        autoLoadFromImports?: boolean;
        bootstrapCode?: string;
        denyPackages?: readonly string[];
    };
}
declare class ExecuteResult {
    readonly stdout: Uint8Array;
    readonly stderr: Uint8Array;
    readonly exitCode: number;
    constructor(stdout: Uint8Array, stderr: Uint8Array, exitCode: number);
    get stdoutText(): string;
    get stderrText(): string;
}
interface ExecuteOptions {
    stdin?: ByteSource | null;
    provision?: boolean;
    sessionId?: string;
    agentId?: string;
    native?: boolean;
    signal?: AbortSignal;
    noHistory?: boolean;
}
declare class Workspace {
    readonly registry: MountRegistry;
    readonly sessionManager: SessionManager;
    private readonly opsRegistry;
    private shellParser;
    private readonly shellParserFactory;
    private shellParserPromise;
    private readonly opened;
    private readonly openOrder;
    readonly jobTable: JobTable;
    private readonly agentId;
    readonly cache: FileCache & Resource;
    readonly history: ExecutionHistory;
    readonly observer: Observer;
    readonly records: OpRecord[];
    readonly fs: WorkspaceFS;
    private closed;
    private readonly workspaceId;
    private readonly closers;
    private readonly pythonRuntime;
    private fuseMountpointValue;
    private fuseOwnedInProcess;
    get fuseMountpoint(): string | null;
    get ownsFuseMount(): boolean;
    setFuseMountpoint(path: string | null, options?: {
        owned?: boolean;
    }): void;
    constructor(resources: Record<string, Resource>, options?: WorkspaceOptions);
    private buildWorkspaceBridge;
    private getShellParser;
    get ops(): OpsRegistry;
    get cwd(): string;
    set cwd(value: string);
    get env(): Record<string, string>;
    set env(value: Record<string, string>);
    createSession(sessionId: string): Session;
    getSession(sessionId: string): Session;
    listSessions(): Session[];
    closeSession(sessionId: string): Promise<void>;
    closeAllSessions(): Promise<void>;
    mounts(): readonly Mount[];
    mount(prefix: string): Mount | null;
    /**
     * Add a mount to a running workspace. Registers the resource's ops globally
     * on this workspace's OpsRegistry so dispatch can find them.
     */
    addMount(prefix: string, resource: Resource, mode?: MountMode): Mount;
    private forwardAddMountToPython;
    /**
     * Remove a mount by prefix. Closes the resource if the workspace had opened
     * it and no other mount still references it. Drops cache entries under the
     * unmounted prefix. Forbidden prefixes: cache root, observer prefix, /dev/.
     * In-flight ops that already resolved their Mount are not interrupted.
     */
    unmount(prefix: string): Promise<void>;
    get cacheMount(): Mount;
    get maxDrainBytes(): number | null;
    set maxDrainBytes(value: number | null);
    get filePrompt(): string;
    stat(path: string): Promise<unknown>;
    readdir(path: string): Promise<string[]>;
    dispatch(opName: string, path: string, args?: readonly unknown[], kwargs?: OpKwargs): Promise<unknown>;
    resolve(path: string): Promise<[Resource, PathSpec, MountMode]>;
    provision(command: string): Promise<ProvisionResult>;
    execute(command: string, options?: ExecuteOptions & {
        provision?: false | undefined;
    }): Promise<ExecuteResult>;
    execute(command: string, options: ExecuteOptions & {
        provision: true;
    }): Promise<ProvisionResult>;
    execute(command: string, options: ExecuteOptions): Promise<ExecuteResult | ProvisionResult>;
    executePythonRepl(code: string, options?: {
        sessionId?: string;
    }): Promise<PythonReplRunResult>;
    toStateDict(): Promise<WorkspaceStateDict>;
    restore(state: WorkspaceStateDict): Promise<void>;
    snapshot(target: string): Promise<number>;
    static load<T extends typeof Workspace>(this: T, source: string | Uint8Array, options?: WorkspaceOptions, overrides?: Record<string, Resource>): Promise<InstanceType<T>>;
    static fromState<T extends typeof Workspace>(this: T, state: WorkspaceStateDict, options?: WorkspaceOptions, overrides?: Record<string, Resource>): Promise<InstanceType<T>>;
    copy(options?: WorkspaceOptions): Promise<this>;
    close(): Promise<void>;
}

/**
 * A {@link Workspace} wrapper that owns its lifecycle and provides a
 * canonical handle for callers that want a single owner per workspace.
 *
 * In Python this class pins the workspace to its own asyncio event
 * loop in a background thread, isolating it from the caller's loop.
 * Node.js has a single event loop per thread, so there's no
 * cross-loop handoff to perform; TS `call()` simply awaits the
 * supplied promise on the current loop. Callers who need true OS-
 * thread isolation should place the workspace inside a Worker and
 * communicate via message-passing.
 *
 * What this class DOES provide in TS:
 * - A clear owner: the runner is responsible for closing the workspace.
 * - Lifecycle guardrails: `call()` rejects after `stop()`.
 * - API parity with Python for code that ports over verbatim.
 *
 * @example
 * ```ts
 * const runner = new WorkspaceRunner(new Workspace({ '/': ram }))
 * try {
 *   const result = await runner.call(runner.ws.execute('ls /'))
 * } finally {
 *   await runner.stop()
 * }
 * ```
 */
declare class WorkspaceRunner {
    readonly ws: Workspace;
    private stopped;
    private stopping;
    constructor(ws: Workspace);
    /**
     * Await a workspace-produced promise.
     *
     * Rejects if the runner has been stopped. Does not block — Node's
     * single event loop interleaves this call with other work as usual.
     *
     * @param p a promise produced from the workspace API (e.g.
     *   `runner.ws.execute('ls /')`).
     */
    call<T>(p: Promise<T>): Promise<T>;
    /**
     * Close the workspace and mark the runner as stopped. Idempotent;
     * concurrent calls are deduplicated.
     */
    stop(): Promise<void>;
}

declare const RAM_OPS: readonly RegisteredOp[];

declare function extractWriteData(args: readonly unknown[]): Uint8Array;

declare const RAM_COMMANDS: readonly RegisteredCommand[];

declare const GENERAL_BC: RegisteredCommand[];

declare const GENERAL_CURL: RegisteredCommand[];

declare const GENERAL_DATE: RegisteredCommand[];

declare const GENERAL_EXPR: RegisteredCommand[];

declare const GENERAL_SEQ: RegisteredCommand[];

declare const GENERAL_WGET: RegisteredCommand[];

declare const GENERAL_COMMANDS: readonly RegisteredCommand[];

declare const RAM_AWK: RegisteredCommand[];

declare const RAM_BASE64: RegisteredCommand[];

declare const RAM_BASENAME: RegisteredCommand[];

declare function catProvision(accessor: RAMAccessor, paths: PathSpec[], _texts: string[], _opts: CommandOpts): Promise<ProvisionResult>;
declare const RAM_CAT: RegisteredCommand[];

declare const RAM_CAT_FEATHER: RegisteredCommand[];

declare const RAM_CAT_HDF5: RegisteredCommand[];

declare const RAM_CAT_PARQUET: RegisteredCommand[];

declare const RAM_CMP: RegisteredCommand[];

declare const RAM_COLUMN: RegisteredCommand[];

declare const RAM_COMM: RegisteredCommand[];

declare const RAM_CP: RegisteredCommand[];

declare const RAM_CSPLIT: RegisteredCommand[];

declare const RAM_CUT: RegisteredCommand[];

declare const RAM_CUT_FEATHER: RegisteredCommand[];

declare const RAM_CUT_HDF5: RegisteredCommand[];

declare const RAM_CUT_PARQUET: RegisteredCommand[];

declare const RAM_DIFF: RegisteredCommand[];

declare const RAM_DIRNAME: RegisteredCommand[];

declare const RAM_DU: RegisteredCommand[];

declare const RAM_EXPAND: RegisteredCommand[];

declare const RAM_FILE: RegisteredCommand[];

declare const RAM_FILE_FEATHER: RegisteredCommand[];

declare const RAM_FILE_HDF5: RegisteredCommand[];

declare const RAM_FILE_PARQUET: RegisteredCommand[];

declare const RAM_FIND: RegisteredCommand[];

declare const RAM_FMT: RegisteredCommand[];

declare const RAM_FOLD: RegisteredCommand[];

declare const RAM_GREP: RegisteredCommand[];

declare const RAM_GREP_FEATHER: RegisteredCommand[];

declare const RAM_GREP_HDF5: RegisteredCommand[];

declare const RAM_GREP_PARQUET: RegisteredCommand[];

declare const RAM_GUNZIP: RegisteredCommand[];

declare const RAM_GZIP: RegisteredCommand[];

declare function headProvision(accessor: RAMAccessor, paths: PathSpec[], texts: string[], opts: CommandOpts): Promise<ProvisionResult>;
declare const RAM_HEAD: RegisteredCommand[];

declare const RAM_HEAD_FEATHER: RegisteredCommand[];

declare const RAM_HEAD_HDF5: RegisteredCommand[];

declare const RAM_HEAD_PARQUET: RegisteredCommand[];

declare const RAM_ICONV: RegisteredCommand[];

declare const RAM_JOIN: RegisteredCommand[];

declare function jqProvision(accessor: RAMAccessor, paths: PathSpec[], texts: string[], _opts: CommandOpts): Promise<ProvisionResult>;
declare const RAM_JQ: RegisteredCommand[];

declare const RAM_LN: RegisteredCommand[];

declare const RAM_LOOK: RegisteredCommand[];

declare const RAM_LS: RegisteredCommand[];

declare const RAM_LS_FEATHER: RegisteredCommand[];

declare const RAM_LS_HDF5: RegisteredCommand[];

declare const RAM_LS_PARQUET: RegisteredCommand[];

declare const RAM_MD5: RegisteredCommand[];

declare const RAM_MKDIR: RegisteredCommand[];

declare const RAM_MKTEMP: RegisteredCommand[];

declare const RAM_MV: RegisteredCommand[];

declare const RAM_NL: RegisteredCommand[];

declare const RAM_PASTE: RegisteredCommand[];

declare const RAM_PATCH: RegisteredCommand[];

declare const RAM_READLINK: RegisteredCommand[];

declare const RAM_REALPATH: RegisteredCommand[];

declare const RAM_REV: RegisteredCommand[];

declare const RAM_RG: RegisteredCommand[];

declare const RAM_RM: RegisteredCommand[];

declare const RAM_SED: RegisteredCommand[];

declare const RAM_SHA256SUM: RegisteredCommand[];

declare const RAM_SHUF: RegisteredCommand[];

declare const RAM_SORT: RegisteredCommand[];

declare const RAM_SPLIT: RegisteredCommand[];

declare function statProvision(accessor: RAMAccessor, paths: PathSpec[], _texts: string[], _opts: CommandOpts): ProvisionResult;
declare const RAM_STAT: RegisteredCommand[];

declare const RAM_STAT_FEATHER: RegisteredCommand[];

declare const RAM_STAT_HDF5: RegisteredCommand[];

declare const RAM_STAT_PARQUET: RegisteredCommand[];

declare const RAM_STRINGS: RegisteredCommand[];

declare const RAM_TAC: RegisteredCommand[];

declare const RAM_TAIL: RegisteredCommand[];

declare const RAM_TAIL_FEATHER: RegisteredCommand[];

declare const RAM_TAIL_HDF5: RegisteredCommand[];

declare const RAM_TAIL_PARQUET: RegisteredCommand[];

declare const RAM_TAR: RegisteredCommand[];

declare const RAM_TEE: RegisteredCommand[];

declare const RAM_TOUCH: RegisteredCommand[];

declare const RAM_TR: RegisteredCommand[];

declare const RAM_TREE: RegisteredCommand[];

declare const RAM_TSORT: RegisteredCommand[];

declare const RAM_UNEXPAND: RegisteredCommand[];

declare const RAM_UNIQ: RegisteredCommand[];

declare const RAM_UNZIP: RegisteredCommand[];

declare const RAM_WC: RegisteredCommand[];

declare const RAM_WC_FEATHER: RegisteredCommand[];

declare const RAM_WC_HDF5: RegisteredCommand[];

declare const RAM_WC_PARQUET: RegisteredCommand[];

declare const RAM_XXD: RegisteredCommand[];

declare const RAM_ZCAT: RegisteredCommand[];

declare const RAM_ZGREP: RegisteredCommand[];

declare const RAM_ZIP: RegisteredCommand[];

declare const S3_COMMANDS: readonly RegisteredCommand[];

declare function specOf(name: string): CommandSpec;
declare const BUILTIN_SPECS: Readonly<Record<string, CommandSpec>>;

declare const AMBIGUOUS_NAMES: Readonly<Record<string, string>>;

declare function resolvePath(cwd: string, path: string): string;
declare function parseCommand(spec: CommandSpec, argv: string[], cwd: string): ParsedArgs;
declare function parseToKwargs(parsed: ParsedArgs): Record<string, string | boolean>;

declare class CachableAsyncIterator implements AsyncIterableIterator<Uint8Array> {
    private readonly source;
    private readonly buffer;
    private exhaustedFlag;
    private drainResolve;
    private readonly drainPromise;
    constructor(source: AsyncIterable<Uint8Array>);
    get exhausted(): boolean;
    [Symbol.asyncIterator](): AsyncIterableIterator<Uint8Array>;
    next(): Promise<IteratorResult<Uint8Array>>;
    drain(): Promise<Uint8Array>;
    drainBounded(maxBytes: number): Promise<[Uint8Array, boolean]>;
    waitForDrain(): Promise<Uint8Array>;
    private markExhausted;
}

declare function mergeStdoutStderr(stdout: ByteSource | null, io: IOResult): AsyncIterable<Uint8Array>;
declare function wrapCachableStreams(stdout: ByteSource | null, io: IOResult): [ByteSource | null, IOResult];
declare function exitOnEmpty(stream: AsyncIterable<Uint8Array>, io: IOResult): AsyncIterable<Uint8Array>;
declare function drain(stream: ByteSource | null): Promise<void>;
declare function closeQuietly(stream: ByteSource | null): Promise<void>;
declare function peekExitCode(stream: ByteSource | null): Promise<ByteSource | null>;
declare function asyncChain(...streams: (ByteSource | null)[]): AsyncIterable<Uint8Array>;
declare function yieldBytes(data: Uint8Array): AsyncIterable<Uint8Array>;
declare function quietMatch(stream: AsyncIterable<Uint8Array>, io: IOResult): AsyncIterable<Uint8Array>;

interface LogEntryInit {
    type: 'op' | 'command';
    agent: string;
    session: string;
    timestamp: number;
    cwd?: string;
    op?: string;
    path?: string;
    source?: string;
    bytes?: number;
    durationMs?: number;
    command?: string;
    exitCode?: number;
    stdout?: string;
}
declare class LogEntry {
    readonly type: 'op' | 'command';
    readonly agent: string;
    readonly session: string;
    readonly timestamp: number;
    readonly cwd: string | undefined;
    readonly op: string | undefined;
    readonly path: string | undefined;
    readonly source: string | undefined;
    readonly bytes: number | undefined;
    readonly durationMs: number | undefined;
    readonly command: string | undefined;
    readonly exitCode: number | undefined;
    readonly stdout: string | undefined;
    constructor(init: LogEntryInit);
    static fromOpRecord(rec: OpRecord, agent: string, session: string, cwd?: string): LogEntry;
    static fromExecutionRecord(rec: ExecutionRecord, cwd?: string): LogEntry;
    toJsonLine(): string;
}

declare function runWithRecording<T>(fn: () => Promise<T>): Promise<[T, OpRecord[]]>;
declare function setVirtualPrefix(prefix: string): void;
declare function record(op: string, path: string, source: string, nbytes: number, startMs: number): void;
declare function recordStream(op: string, path: string, source: string): OpRecord | null;

declare function guessType(path: string): FileType;

declare function describe$2(raw: Uint8Array): string;
declare function cat$2(raw: Uint8Array, maxRows?: number): Uint8Array;
declare function head$2(raw: Uint8Array, n?: number): Uint8Array;
declare function tail$2(raw: Uint8Array, n?: number): Uint8Array;
declare function wc$2(raw: Uint8Array): number;
declare function ls$2(raw: Uint8Array, meta: {
    size: number;
    modified: string | null;
    name: string;
}): Uint8Array;
declare function lsFallback$2(meta: {
    size: number;
    modified: string | null;
    name: string;
}): Uint8Array;
declare function stat$o(raw: Uint8Array): Uint8Array;
declare function grep$2(raw: Uint8Array, pattern: string, ignoreCase?: boolean): Uint8Array;
declare function cut$2(raw: Uint8Array, columns: readonly string[]): Uint8Array;

declare function describe$1(raw: Uint8Array): Promise<string>;
declare function cat$1(raw: Uint8Array, maxRows?: number): Promise<Uint8Array>;
declare function head$1(raw: Uint8Array, n?: number): Promise<Uint8Array>;
declare function tail$1(raw: Uint8Array, n?: number): Promise<Uint8Array>;
declare function ls$1(raw: Uint8Array, meta: {
    size: number;
    modified: string | null;
    name: string;
}): Promise<Uint8Array>;
declare function lsFallback$1(meta: {
    size: number;
    modified: string | null;
    name: string;
}): Uint8Array;
declare function wc$1(raw: Uint8Array): Promise<number>;
declare function stat$n(raw: Uint8Array): Promise<Uint8Array>;
declare function grep$1(raw: Uint8Array, pattern: string, ignoreCase?: boolean): Promise<Uint8Array>;
declare function cut$1(raw: Uint8Array, columns: readonly string[]): Promise<Uint8Array>;

declare function describe(raw: Uint8Array): string;
declare function cat(raw: Uint8Array, maxRows?: number): Promise<Uint8Array>;
declare function head(raw: Uint8Array, n?: number): Promise<Uint8Array>;
declare function tail(raw: Uint8Array, n?: number): Promise<Uint8Array>;
declare function ls(raw: Uint8Array, meta: {
    size: number;
    modified: string | null;
    name: string;
}): Uint8Array;
declare function lsFallback(meta: {
    size: number;
    modified: string | null;
    name: string;
}): Uint8Array;
declare function wc(raw: Uint8Array): number;
declare function stat$m(raw: Uint8Array): Uint8Array;
declare function grep(raw: Uint8Array, pattern: string, ignoreCase?: boolean): Promise<Uint8Array>;
declare function cut(raw: Uint8Array, columns: readonly string[]): Promise<Uint8Array>;

declare function detectFileType(header: Uint8Array, stat: FileStat): FileType;
declare const FILE_MIME_MAP: Readonly<Record<string, string>>;
declare function formatFileResult(pathOriginal: string, result: FileType, brief: boolean, mime: boolean): string;

declare function escapeRegex(s: string): string;
declare function compilePattern(pattern: string, ignoreCase?: boolean, fixedString?: boolean, wholeWord?: boolean): RegExp;
interface GrepLinesOptions {
    invert: boolean;
    lineNumbers: boolean;
    countOnly: boolean;
    filesOnly: boolean;
    onlyMatching: boolean;
    maxCount: number | null;
}
declare function grepLines(path: string, data: readonly string[], compiled: RegExp, opts: GrepLinesOptions): string[];
interface GrepStreamOptions {
    invert: boolean;
    lineNumbers: boolean;
    onlyMatching: boolean;
    maxCount: number | null;
    countOnly: boolean;
    afterContext: number;
    beforeContext: number;
}
declare function grepStream(source: AsyncIterable<Uint8Array>, pat: RegExp, opts: GrepStreamOptions): AsyncIterable<Uint8Array>;
type AsyncReaddir = (path: string) => Promise<string[]>;
type AsyncStat = (path: string) => Promise<FileStat>;
type AsyncReadBytes = (path: string) => Promise<Uint8Array>;
interface GrepFilesOnlyOptions {
    recursive: boolean;
    ignoreCase: boolean;
    invert: boolean;
    lineNumbers: boolean;
    countOnly: boolean;
    fixedString: boolean;
    onlyMatching: boolean;
    maxCount: number | null;
    wholeWord: boolean;
}
declare function grepRecursive(readdirFn: AsyncReaddir, statFn: AsyncStat, readBytesFn: AsyncReadBytes, path: string, compiled: RegExp, opts: GrepFilesOnlyOptions, warnings: string[] | null): Promise<string[]>;
declare function grepFilesOnly(readdirFn: AsyncReaddir, statFn: AsyncStat, readBytesFn: AsyncReadBytes, path: string, pattern: string, opts: GrepFilesOnlyOptions, warnings?: string[] | null): Promise<string[]>;

declare function grepContextLines(lines: readonly string[], pat: RegExp, invert: boolean, lineNumbers: boolean, maxCount: number | null, afterContext: number, beforeContext: number): Uint8Array[];

declare const TYPE_EXTENSIONS: Record<string, string[]>;
type AsyncReaddirFn = (path: string) => Promise<string[]>;
type AsyncStatFn = (path: string) => Promise<FileStat>;
type AsyncReadBytesFn = (path: string) => Promise<Uint8Array>;
declare function rgMatchesFilter(entry: string, fileType: string | null, globPattern: string | null, hidden: boolean): boolean;
interface RgFullOptions {
    ignoreCase: boolean;
    invert: boolean;
    lineNumbers: boolean;
    countOnly: boolean;
    filesOnly: boolean;
    fixedString: boolean;
    onlyMatching: boolean;
    maxCount: number | null;
    wholeWord: boolean;
    contextBefore: number;
    contextAfter: number;
    fileType: string | null;
    globPattern: string | null;
    hidden: boolean;
}
declare function rgFull(readdirFn: AsyncReaddirFn, statFn: AsyncStatFn, readBytesFn: AsyncReadBytesFn, path: string, pattern: string, opts: RgFullOptions, warnings: string[] | null): Promise<string[]>;
type FiletypeFn = (paths: PathSpec[], pattern: string, opts: {
    stdin: null;
    ignoreCase: boolean;
}) => Promise<[AsyncIterable<Uint8Array> | Uint8Array | null, unknown]>;
interface RgFolderFiletypeOptions {
    ignoreCase: boolean;
    invert: boolean;
    lineNumbers: boolean;
    countOnly: boolean;
    filesOnly: boolean;
    onlyMatching: boolean;
    maxCount: number | null;
    fixedString: boolean;
    wholeWord: boolean;
    fileType: string | null;
    globPattern: string | null;
    hidden: boolean;
}
declare function rgFolderFiletype(readdirFn: AsyncReaddirFn, statFn: AsyncStatFn, readBytesFn: AsyncReadBytesFn, path: string, pattern: string, filetypeFns: Record<string, FiletypeFn>, opts: RgFolderFiletypeOptions, warnings: string[] | null): Promise<string[]>;

type SortKey = string | number | (string | number)[];
interface SortKeyOptions {
    keyField: number | null;
    fieldSep: string | null;
    ignoreCase: boolean;
    numeric: boolean;
    humanNumeric: boolean;
    version: boolean;
    month: boolean;
}
declare function sortKey(line: string, opts: SortKeyOptions): SortKey;
declare function compareKeys(a: SortKey, b: SortKey): number;

declare function parseN(n: string | null): [number, boolean];
declare function tailBytes(data: Uint8Array, lines: number, bytesMode?: number | null, plusMode?: boolean): Uint8Array;
declare function countNewlines(data: Uint8Array): number;

declare function readStdinAsync(stdin: ByteSource | null): Promise<Uint8Array | null>;
declare function wrapBytes(data: Uint8Array): AsyncIterable<Uint8Array>;
declare function resolveSource(stdin: ByteSource | null, errorMsg: string): AsyncIterable<Uint8Array>;

declare function humanSize(n: number): string;
interface LsLongOptions {
    human?: boolean;
    owner?: string;
    group?: string;
    sizeWidth?: number;
}
declare function formatLsLong(stats: readonly FileStat[], opts?: LsLongOptions): string[];

declare function interpretEscapes(text: string): string;

declare function gzip(bytes: Uint8Array): Promise<Uint8Array>;
declare function gunzip(bytes: Uint8Array): Promise<Uint8Array>;
declare function deflateRaw(bytes: Uint8Array): Promise<Uint8Array>;
declare function inflateRaw(bytes: Uint8Array): Promise<Uint8Array>;

declare function encodeBase64(bytes: Uint8Array): string;
declare function decodeBase64(str: string): Uint8Array;

declare function sha256(bytes: Uint8Array): Promise<Uint8Array>;
declare function sha256Hex(bytes: Uint8Array): Promise<string>;
declare function md5(bytes: Uint8Array): Uint8Array;
declare function md5Hex(bytes: Uint8Array): string;

declare function jqEval(obj: unknown, expr: string): Promise<unknown>;

declare function parseJsonAuto(raw: Uint8Array): unknown;
declare function parseJsonPath(raw: Uint8Array, path: string): unknown;
declare function isJsonlPath(path: string): boolean;
declare function isStreamableJsonlExpr(expression: string): boolean;
declare function evalJsonlStream(source: AsyncIterable<Uint8Array>, expression: string): AsyncIterable<Uint8Array>;

declare function unifiedDiff(a: readonly string[], b: readonly string[], fromFile?: string, toFile?: string, n?: number): string[];
declare function edScript(aLines: readonly string[], bLines: readonly string[]): string[];

type SedAddr = ['line', string] | ['last', ''] | ['regex', string];
interface SedCommand {
    cmd: string;
    addrStart?: SedAddr | null;
    addrEnd?: SedAddr | null;
    pattern?: string;
    replacement?: string;
    exprFlags?: string;
    text?: string;
    label?: string;
}
declare function parseOneCommand(rest: string): [SedCommand, string];
declare function parseProgram(expr: string): SedCommand[];
declare function translateReplacement(repl: string): string;
declare function executeProgram(text: string, commands: SedCommand[], suppress?: boolean): string;

interface TarEntry {
    name: string;
    data: Uint8Array;
    isFile: boolean;
}
declare function writeTar(entries: readonly TarEntry[]): Uint8Array;
declare function readTar(data: Uint8Array): TarEntry[];

interface CacheEntryInit {
    size: number;
    cachedAt: number;
    fingerprint?: string | null;
    ttl?: number | null;
}
declare class CacheEntry {
    readonly size: number;
    readonly cachedAt: number;
    readonly fingerprint: string | null;
    readonly ttl: number | null;
    constructor(init: CacheEntryInit);
    get expired(): boolean;
}

declare function parseLimit(limit: string | number): number;
declare function defaultFingerprint(data: Uint8Array): string;

declare class RAMFileCacheStore extends RAMResource implements FileCache {
    private readonly entries;
    private readonly lock;
    private readonly limit;
    private size;
    maxDrainBytes: number | null;
    constructor(options?: {
        limit?: string | number;
        maxDrainBytes?: number | null;
    });
    get cacheSize(): number;
    get cacheLimit(): number;
    snapshotEntries(): {
        key: string;
        entry: CacheEntry;
    }[];
    loadEntry(key: string, data: Uint8Array, entry: CacheEntry): void;
    get(key: string): Promise<Uint8Array | null>;
    set(key: string, data: Uint8Array, options?: {
        fingerprint?: string | null;
        ttl?: number | null;
    }): Promise<void>;
    add(key: string, data: Uint8Array, options?: {
        fingerprint?: string | null;
        ttl?: number | null;
    }): Promise<boolean>;
    remove(key: string): Promise<void>;
    exists(key: string | PathSpec): Promise<boolean>;
    isFresh(key: string, remoteFingerprint: string): Promise<boolean>;
    clear(): Promise<void>;
    allCached(keys: readonly string[]): Promise<boolean>;
    multiGet(keys: readonly string[]): Promise<(Uint8Array | null)[]>;
    private evict;
}

interface CallFrameInit {
    positional?: string[];
    locals?: Record<string, string>;
    functionName?: string;
    loopLevel?: number;
}
declare class CallFrame {
    positional: string[];
    locals: Record<string, string>;
    functionName: string;
    loopLevel: number;
    constructor(init?: CallFrameInit);
}
declare class CallStack {
    private readonly frames;
    get current(): CallFrame;
    push(positional?: string[], functionName?: string): void;
    pop(): CallFrame;
    get depth(): number;
    getPositional(index: number): string;
    getAllPositional(): string[];
    getPositionalCount(): number;
    shift(n?: number): void;
    setPositional(values: string[]): void;
    setLocal(name: string, value: string): void;
    getLocal(name: string): string | null;
}

interface TSNodeLike {
    type: string;
    text: string;
    children: TSNodeLike[];
    namedChildren: TSNodeLike[];
    isNamed?: boolean;
}
declare function lookupVar(name: string, session: Session, callStack: CallStack | null): string;
declare function expandBraces(node: TSNodeLike, env: Record<string, string>, callStack: CallStack | null): string;

type ExecuteNodeFn = (node: TSNodeLike, session: Session, stdin: ByteSource | null, callStack: CallStack | null) => Promise<[ByteSource | null, IOResult, ExecutionNode]>;
type JobHandlerResult = [ByteSource | null, IOResult, ExecutionNode];
declare function handleBackground(executeNode: ExecuteNodeFn, left: TSNodeLike, right: TSNodeLike | null, session: Session, jobTable: JobTable | null, agentId: string | null, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<JobHandlerResult>;
declare function handleWait(jobTable: JobTable, parts: string[]): Promise<JobHandlerResult>;
declare function handleKill(jobTable: JobTable, parts: string[]): JobHandlerResult;
declare function handleJobs(jobTable: JobTable, parts: string[]): JobHandlerResult;
declare function handlePs(jobTable: JobTable, parts: string[]): JobHandlerResult;

type DispatchFn = (op: string, path: PathSpec, args?: readonly unknown[], kwargs?: Record<string, unknown>) => Promise<[unknown, IOResult]>;
type Result$6 = [ByteSource | null, IOResult, ExecutionNode];
declare function isCrossMount(cmdName: string, scopes: PathSpec[], registry: MountRegistry): boolean;
declare function handleCrossMount(cmdName: string, scopes: PathSpec[], textArgs: string[], dispatch: DispatchFn, cmdStr: string): Promise<Result$6>;

type Result$5 = [ByteSource | null, IOResult, ExecutionNode];
declare class ReturnSignal extends Error {
    readonly exitCode: number;
    constructor(exitCode: number);
}
declare function handleCommand(executeNode: ExecuteNodeFn, dispatch: DispatchFn, registry: MountRegistry, parts: readonly (string | PathSpec)[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null, jobTable?: JobTable | null, ensureOpen?: (resource: Resource) => Promise<void>, unmount?: (prefix: string) => Promise<void>, history?: CommandHistory): Promise<Result$5>;

declare const BarrierPolicy: Readonly<{
    readonly STREAM: "stream";
    readonly STATUS: "status";
    readonly VALUE: "value";
}>;
type BarrierPolicy = (typeof BarrierPolicy)[keyof typeof BarrierPolicy];
declare function applyBarrier(stdout: ByteSource | null, io: IOResult, policy: BarrierPolicy): Promise<ByteSource | null>;

type Result$4 = [ByteSource | null, IOResult, ExecutionNode];
declare function handlePipe(executeNode: ExecuteNodeFn, commands: readonly TSNodeLike[], stderrFlags: readonly boolean[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$4>;
declare function handleConnection(executeNode: ExecuteNodeFn, left: TSNodeLike, op: string | null, right: TSNodeLike, session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$4>;
declare function handleSubshell(executeNode: ExecuteNodeFn, body: readonly TSNodeLike[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$4>;

declare const NodeType: Readonly<{
    readonly COMMAND: "command";
    readonly PIPELINE: "pipeline";
    readonly LIST: "list";
    readonly REDIRECTED_STATEMENT: "redirected_statement";
    readonly SUBSHELL: "subshell";
    readonly IF_STATEMENT: "if_statement";
    readonly FOR_STATEMENT: "for_statement";
    readonly WHILE_STATEMENT: "while_statement";
    readonly CASE_STATEMENT: "case_statement";
    readonly CASE_ITEM: "case_item";
    readonly FUNCTION_DEFINITION: "function_definition";
    readonly DECLARATION_COMMAND: "declaration_command";
    readonly UNSET_COMMAND: "unset_command";
    readonly TEST_COMMAND: "test_command";
    readonly COMPOUND_STATEMENT: "compound_statement";
    readonly NEGATED_COMMAND: "negated_command";
    readonly VARIABLE_ASSIGNMENT: "variable_assignment";
    readonly FOR: "for";
    readonly SELECT: "select";
    readonly WHILE: "while";
    readonly UNTIL: "until";
    readonly EXPORT: "export";
    readonly LOCAL: "local";
    readonly WORD: "word";
    readonly NUMBER: "number";
    readonly COMMAND_NAME: "command_name";
    readonly VARIABLE_NAME: "variable_name";
    readonly SIMPLE_EXPANSION: "simple_expansion";
    readonly EXPANSION: "expansion";
    readonly COMMAND_SUBSTITUTION: "command_substitution";
    readonly ARITHMETIC_EXPANSION: "arithmetic_expansion";
    readonly CONCATENATION: "concatenation";
    readonly STRING: "string";
    readonly STRING_CONTENT: "string_content";
    readonly RAW_STRING: "raw_string";
    readonly PROCESS_SUBSTITUTION: "process_substitution";
    readonly EXTGLOB_PATTERN: "extglob_pattern";
    readonly DO_GROUP: "do_group";
    readonly ELIF_CLAUSE: "elif_clause";
    readonly ELSE_CLAUSE: "else_clause";
    readonly FILE_REDIRECT: "file_redirect";
    readonly HEREDOC_REDIRECT: "heredoc_redirect";
    readonly HEREDOC_BODY: "heredoc_body";
    readonly HEREDOC_START: "heredoc_start";
    readonly HEREDOC_END: "heredoc_end";
    readonly HERESTRING_REDIRECT: "herestring_redirect";
    readonly FILE_DESCRIPTOR: "file_descriptor";
    readonly ARRAY: "array";
    readonly AND: "&&";
    readonly OR: "||";
    readonly SEMI: ";";
    readonly BACKGROUND: "&";
    readonly PIPE: "|";
    readonly PIPE_STDERR: "|&";
    readonly REDIRECT_OUT: ">";
    readonly REDIRECT_APPEND: ">>";
    readonly REDIRECT_IN: "<";
    readonly REDIRECT_STDERR: ">&";
    readonly REDIRECT_BOTH: "&>";
    readonly REDIRECT_BOTH_APPEND: "&>>";
    readonly HEREDOC_START_TOKEN: "<<";
    readonly HERESTRING_TOKEN: "<<<";
    readonly OPEN_PAREN: "(";
    readonly CLOSE_PAREN: ")";
    readonly OPEN_BRACE: "{";
    readonly CLOSE_BRACE: "}";
    readonly OPEN_BRACKET: "[";
    readonly CLOSE_BRACKET: "]";
    readonly DOUBLE_OPEN_PAREN: "((";
    readonly DOUBLE_CLOSE_PAREN: "))";
    readonly DOUBLE_SEMICOLON: ";;";
    readonly DQUOTE: "\"";
    readonly IF: "if";
    readonly THEN: "then";
    readonly ELIF: "elif";
    readonly ELSE: "else";
    readonly FI: "fi";
    readonly IN: "in";
    readonly DO: "do";
    readonly DONE: "done";
    readonly CASE: "case";
    readonly ESAC: "esac";
    readonly FUNCTION: "function";
    readonly PROGRAM: "program";
    readonly BINARY_EXPRESSION: "binary_expression";
    readonly UNARY_EXPRESSION: "unary_expression";
    readonly NEGATION_EXPRESSION: "negation_expression";
    readonly PARENTHESIZED_EXPRESSION: "parenthesized_expression";
    readonly TERNARY_EXPRESSION: "ternary_expression";
    readonly TEST_OPERATOR: "test_operator";
    readonly SPECIAL_VARIABLE_NAME: "special_variable_name";
    readonly COMMENT: "comment";
    readonly ERROR: "ERROR";
}>;
type NodeType = (typeof NodeType)[keyof typeof NodeType];
declare const RedirectKind: Readonly<{
    readonly STDOUT: "stdout";
    readonly STDERR: "stderr";
    readonly STDIN: "stdin";
    readonly STDERR_TO_STDOUT: "stderr_to_stdout";
    readonly HEREDOC: "heredoc";
    readonly HERESTRING: "herestring";
}>;
type RedirectKind = (typeof RedirectKind)[keyof typeof RedirectKind];
interface RedirectInit {
    fd: number;
    target: unknown;
    targetNode?: unknown;
    kind?: RedirectKind;
    append?: boolean;
    pipeline?: unknown;
    expandVars?: boolean;
}
declare class Redirect {
    readonly fd: number;
    readonly target: unknown;
    readonly targetNode: unknown;
    readonly kind: RedirectKind;
    readonly append: boolean;
    pipeline: unknown;
    readonly expandVars: boolean;
    constructor(init: RedirectInit);
}
declare const ShellBuiltin: Readonly<{
    readonly PWD: "pwd";
    readonly CD: "cd";
    readonly EXPORT: "export";
    readonly UNSET: "unset";
    readonly LOCAL: "local";
    readonly SET: "set";
    readonly PRINTENV: "printenv";
    readonly WHOAMI: "whoami";
    readonly MAN: "man";
    readonly TRUE: "true";
    readonly FALSE: "false";
    readonly SOURCE: "source";
    readonly DOT: ".";
    readonly EVAL: "eval";
    readonly READ: "read";
    readonly SHIFT: "shift";
    readonly TRAP: "trap";
    readonly TEST: "test";
    readonly BRACKET: "[";
    readonly DOUBLE_BRACKET: "[[";
    readonly WAIT: "wait";
    readonly FG: "fg";
    readonly KILL: "kill";
    readonly JOBS: "jobs";
    readonly PS: "ps";
    readonly ECHO: "echo";
    readonly PRINTF: "printf";
    readonly SLEEP: "sleep";
    readonly BASH: "bash";
    readonly SH: "sh";
    readonly PYTHON: "python";
    readonly PYTHON3: "python3";
    readonly XARGS: "xargs";
    readonly TIMEOUT: "timeout";
    readonly BREAK: "break";
    readonly CONTINUE: "continue";
    readonly RETURN: "return";
}>;
type ShellBuiltin = (typeof ShellBuiltin)[keyof typeof ShellBuiltin];

type Result$3 = [ByteSource | null, IOResult, ExecutionNode];
declare function handleRedirect(executeNode: ExecuteNodeFn, dispatch: DispatchFn, command: TSNodeLike, redirects: readonly Redirect[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$3>;

type Result$2 = [ByteSource | null, IOResult, ExecutionNode];
declare class BreakSignal extends Error {
    readonly stdout: ByteSource | null;
    readonly io: IOResult;
    constructor(stdout?: ByteSource | null, io?: IOResult);
}
declare class ContinueSignal extends Error {
    readonly stdout: ByteSource | null;
    readonly io: IOResult;
    constructor(stdout?: ByteSource | null, io?: IOResult);
}
declare function handleIf(executeNode: ExecuteNodeFn, branches: readonly [TSNodeLike, TSNodeLike[]][], elseBody: TSNodeLike[] | null, session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;
declare function handleFor(executeNode: ExecuteNodeFn, variable: string, values: readonly (string | PathSpec)[], body: readonly TSNodeLike[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;
declare function handleWhile(executeNode: ExecuteNodeFn, condition: TSNodeLike, body: readonly TSNodeLike[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;
declare function handleUntil(executeNode: ExecuteNodeFn, condition: TSNodeLike, body: readonly TSNodeLike[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;
declare function handleCase(executeNode: ExecuteNodeFn, word: string, items: readonly [readonly string[], TSNodeLike | null][], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;
declare function handleSelect(executeNode: ExecuteNodeFn, variable: string, values: readonly (string | PathSpec)[], body: readonly TSNodeLike[], session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result$2>;

type Result$1 = [ByteSource | null, IOResult, ExecutionNode];
type ExecuteStringFn = (script: string, opts: {
    sessionId: string;
}) => Promise<IOResult>;
declare function handleCd(dispatch: DispatchFn, isMountRoot: (path: string) => boolean, path: string | PathSpec, session: Session): Promise<Result$1>;
declare function handleExport(assignments: string[], session: Session): Result$1;
declare function handleUnset(names: string[], session: Session): Result$1;
declare function handlePrintenv(name: string | null, session: Session): Result$1;
declare function handleEval(executeFn: ExecuteStringFn, args: string[], session: Session): Promise<Result$1>;
declare function handleTest(dispatch: DispatchFn, argv: (string | PathSpec)[], _session: Session): Promise<Result$1>;
declare function handleLocal(assignments: string[], session: Session): Result$1;
declare function handleShift(n: number, callStack: CallStack | null, session?: Session | null): Result$1;
declare function handleSet(args: string[], session: Session, _callStack?: CallStack | null): Result$1;
declare function handleTrap(_session: Session): Result$1;
declare function handleReturn(exitCode: number): Result$1;
declare function handleEcho(args: string[], nFlag?: boolean, eFlag?: boolean): Result$1;
declare function handlePrintf(args: string[]): Result$1;
/**
 * `read VAR1 [VAR2 ...]` — read one line from stdin and assign to env vars.
 * Mirrors Python's `mirage.workspace.executor.builtins.handle_read`.
 *
 * Mirrors POSIX behavior:
 *   - Single var: assign whole line.
 *   - Multiple vars: split on whitespace, last var gets the remainder.
 *   - No stdin / EOF: assign all vars to "" and exit 1.
 */
declare function handleRead(variables: string[], session: Session, stdin: ByteSource | null): Promise<Result$1>;
/**
 * `source FILE` / `. FILE` — read a script file and execute it.
 * Mirrors Python's `mirage.workspace.executor.builtins.handle_source`.
 */
declare function handleSource(dispatch: DispatchFn, executeFn: ExecuteStringFn, path: string | PathSpec, session: Session): Promise<Result$1>;
declare function handleSleep(args: string[]): Promise<Result$1>;

declare function getText(node: TSNodeLike): string;
declare function getCommandName(node: TSNodeLike): string;
declare function getParts(node: TSNodeLike): TSNodeLike[];
declare function getPipelineCommands(node: TSNodeLike): [TSNodeLike[], boolean[]];
declare function getWhileParts(node: TSNodeLike): [TSNodeLike, TSNodeLike[]];
declare function getForParts(node: TSNodeLike): [string, TSNodeLike[], TSNodeLike[]];
declare function getSubshellBody(node: TSNodeLike): TSNodeLike[];
declare function getRedirects(node: TSNodeLike): [TSNodeLike, Redirect[]];
declare function getRedirectTargetNode(node: TSNodeLike): TSNodeLike | null;
declare function getListParts(node: TSNodeLike): [TSNodeLike, string | null, TSNodeLike];
declare function getIfBranches(node: TSNodeLike): [[TSNodeLike, TSNodeLike[]][], TSNodeLike[] | null];
declare function getCaseWord(node: TSNodeLike): TSNodeLike;
declare function getCaseItems(node: TSNodeLike): [string[], TSNodeLike | null][];
declare function getDeclarationAssignments(node: TSNodeLike): string[];
declare function getDeclarationKeyword(node: TSNodeLike): string;
declare function getUnsetNames(node: TSNodeLike): string[];
declare function getTestArgv(node: TSNodeLike): string[];
declare function getCommandAssignments(node: TSNodeLike): string[];
declare function getNegatedCommand(node: TSNodeLike): TSNodeLike;
declare function getHeredocParts(redirectNode: TSNodeLike): [string, string];
declare function getHeredocMeta(redirectNode: TSNodeLike): [string, boolean, boolean];
declare function getHerestringContent(node: TSNodeLike): string;
declare function getProcessSubCommand(node: TSNodeLike): TSNodeLike;
declare function getFunctionName(node: TSNodeLike): string;
declare function getFunctionBody(node: TSNodeLike): TSNodeLike[] | null;

declare function normMountPrefix(prefix: string): string;

declare const COMPOUND_EXTENSIONS: ReadonlySet<string>;
declare function getExtension(path: string | null): string | null;
declare function resolveFirstPath(argv: string[], cwd: string, spec: CommandSpec): string;
declare function materializeStdout(stdout: ByteSource | null): Promise<Uint8Array>;
declare function stripPrefixFromPathKwargs(kwargs: Record<string, string | boolean>, spec: CommandSpec, prefix: string): Record<string, string | boolean>;

declare const ARITH_OPERATORS: ReadonlySet<string>;
declare const ARITH_DELIMITERS: ReadonlySet<string>;

declare function unescapePath(word: string): string;
declare function classifyWord(word: string, registry: MountRegistry, cwd: string): string | PathSpec;
declare function classifyBarePath(word: string, registry: MountRegistry, cwd: string): string | PathSpec;
declare function classifyParts(parts: string[], registry: MountRegistry, cwd: string, textArgs?: ReadonlySet<string> | null, pathArgs?: ReadonlySet<string> | null): (string | PathSpec)[];
declare function posixNormpath(path: string): string;
declare function shlexSplit(input: string): string[];

type ExecuteFn = (command: string, opts: {
    sessionId: string;
}) => Promise<IOResult>;
declare function safeEval(expr: string): number;
declare function expandNode(tsNode: TSNodeLike, session: Session, executeFn: ExecuteFn, callStack?: CallStack | null): Promise<string>;

declare function expandParts(parts: TSNodeLike[], session: Session, executeFn: ExecuteFn, callStack?: CallStack | null): Promise<string[]>;
declare function expandAndClassify(words: TSNodeLike[], session: Session, executeFn: ExecuteFn, registry: MountRegistry, cwd: string, callStack?: CallStack | null): Promise<(string | PathSpec)[]>;

interface ResourceWithGlob extends Resource {
    glob(paths: readonly PathSpec[], prefix?: string): Promise<PathSpec[]>;
}
declare function resolveGlobs(classified: readonly (string | PathSpec)[], registry: MountRegistry, textArgs?: ReadonlySet<string> | null): Promise<(string | PathSpec)[]>;

declare function expandTestExpr(node: TSNodeLike, session: Session, executeFn: ExecuteFn, cs: CallStack | null): Promise<string[]>;

type BridgeDispatchFn = (op: 'READ' | 'WRITE' | 'LIST', path: string, bytes?: Uint8Array) => Promise<unknown>;

interface PyodideRuntimeOptions {
    autoLoadFromImports?: boolean;
    bootstrapCode?: string;
    workspaceBridge?: BridgeDispatchFn;
    denyPackages?: readonly string[];
}
declare class PyodideRuntime {
    private pyodide;
    private initPromise;
    private bootstrapPromise;
    private queue;
    private readonly autoLoadFromImports;
    private readonly bootstrapCode;
    private readonly workspaceBridge;
    private readonly denyPackages;
    private bridge;
    private readonly mountedPrefixes;
    constructor(options?: PyodideRuntimeOptions);
    addMount(prefix: string): Promise<void>;
    removeMount(prefix: string): Promise<void>;
    private addMountOne;
    private removeMountOne;
    run(args: PythonRunArgs): Promise<PythonRunResult>;
    runRepl(args: PythonReplRunArgs): Promise<PythonReplRunResult>;
    close(): Promise<void>;
    private ensureLoaded;
    private wireBridgeIfNeeded;
    private loadImports;
    private runOne;
    private runOneRepl;
}

type Result = [ByteSource | null, IOResult, ExecutionNode];
interface ExecuteNodeDeps {
    dispatch: DispatchFn;
    registry: MountRegistry;
    jobTable: JobTable | null;
    executeFn: ExecuteFn;
    agentId: string;
    workspaceId: string;
    registerCloser: (fn: () => Promise<void>) => void;
    ensureOpen?: (resource: Resource) => Promise<void>;
    unmount?: (prefix: string) => Promise<void>;
    pythonRuntime?: PyodideRuntime;
    history?: CommandHistory;
}
declare function executeNode(deps: ExecuteNodeDeps, node: TSNodeLike, session: Session, stdin?: ByteSource | null, callStack?: CallStack | null): Promise<Result>;

type S3BrowserOperation = 'GET' | 'PUT' | 'HEAD' | 'DELETE' | 'LIST' | 'COPY';
interface S3BrowserSignOptions {
    contentType?: string;
    ttlSec?: number;
    listPrefix?: string;
    listDelimiter?: string;
    listContinuationToken?: string;
    copySource?: string;
}
type S3BrowserPresignedUrlProvider = (path: string, operation: S3BrowserOperation, options?: S3BrowserSignOptions) => Promise<string>;
interface S3Config {
    bucket: string;
    region?: string;
    endpoint?: string;
    accessKeyId?: string;
    secretAccessKey?: string;
    sessionToken?: string;
    forcePathStyle?: boolean;
    timeoutMs?: number;
    /**
     * Browser runtime only. When set, core/s3/* skips the AWS SDK entirely
     * and signs every request via this callback (your server-side signer),
     * then fetches the resulting presigned URL. Mirrors Python's boto3
     * session-behind-accessor abstraction — just with a different backend.
     */
    presignedUrlProvider?: S3BrowserPresignedUrlProvider;
    /** Optional default Content-Type for PUT via the presigner path. */
    defaultContentType?: string;
}
interface S3ConfigRedacted extends Omit<S3Config, 'accessKeyId' | 'secretAccessKey' | 'sessionToken' | 'presignedUrlProvider'> {
    accessKeyId?: string;
    secretAccessKey?: string;
    sessionToken?: string;
    presignedUrlProvider?: '<REDACTED>';
}
declare function redactConfig(config: S3Config): S3ConfigRedacted;

declare class S3Accessor extends Accessor {
    readonly config: S3Config;
    constructor(config: S3Config);
}
interface S3ResourceLike extends Resource {
    readonly accessor: S3Accessor;
}

declare const S3_PROMPT = "{prefix}\n  Remote S3 bucket. Maps S3 object keys to virtual paths.\n  IMPORTANT: This is a remote mount. Prefer targeted reads (grep, head) over full scans. Avoid cat on large files without piping to head/tail.\n  Supports: ls, cat, head, tail, grep, rg, wc, find, tree, jq, stat.\n  cat on .parquet/.orc/.feather returns a formatted table.";

declare const SCOPE_ERROR = 5000;

interface S3IndexEntryInit extends IndexEntryInit {
    etag?: string;
}
interface S3Object {
    Key?: string;
    Size?: number | null;
    LastModified?: Date | string | undefined;
    ETag?: string;
}
declare class S3IndexEntry extends IndexEntry {
    etag: string;
    constructor(init: S3IndexEntryInit);
    static fromObject(obj: S3Object): S3IndexEntry;
    static fromPrefix(prefix: string): S3IndexEntry;
}

declare function copy(accessor: S3Accessor, src: PathSpec, dst: PathSpec): Promise<void>;

declare function create(accessor: S3Accessor, path: PathSpec): Promise<void>;

declare function du(accessor: S3Accessor, path: PathSpec): Promise<number>;
/**
 * Return `[path, size]` pairs for every object under the prefix plus a trailing
 * entry with the total — mirrors Python's `du_all` used by `du -a`.
 */
declare function duAll(accessor: S3Accessor, path: PathSpec): Promise<[string, number][]>;

declare function exists(accessor: S3Accessor, path: PathSpec): Promise<boolean>;

declare function find(accessor: S3Accessor, path: PathSpec, options?: FindOptions): Promise<string[]>;

declare function resolveGlob$f(accessor: S3Accessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare function mkdir(accessor: S3Accessor, path: PathSpec): Promise<void>;

interface S3ReadOptions {
    offset?: number;
    size?: number;
}
declare function read$l(accessor: S3Accessor, path: PathSpec, _index?: IndexCacheStore, options?: S3ReadOptions): Promise<Uint8Array>;

declare function readdir$l(accessor: S3Accessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function rename(accessor: S3Accessor, src: PathSpec, dst: PathSpec): Promise<void>;

declare function rmR(accessor: S3Accessor, path: PathSpec): Promise<void>;

declare function rmdir(accessor: S3Accessor, path: PathSpec): Promise<void>;

declare function stat$l(accessor: S3Accessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function stream$8(accessor: S3Accessor, path: PathSpec): AsyncIterable<Uint8Array>;
declare function rangeRead(accessor: S3Accessor, path: PathSpec, offset: number, size: number): Promise<Uint8Array>;

declare function truncate(accessor: S3Accessor, path: PathSpec, length: number): Promise<void>;

declare function unlink(accessor: S3Accessor, path: PathSpec): Promise<void>;

declare function write(accessor: S3Accessor, path: PathSpec, data: Uint8Array): Promise<void>;

declare const S3_OPS: readonly RegisteredOp[];

declare function fileReadProvision(accessor: Accessor, paths: PathSpec[], _texts: string[], opts: CommandOpts): Promise<ProvisionResult>;
declare function headTailProvision(accessor: Accessor, paths: PathSpec[], _texts: string[], opts: CommandOpts): Promise<ProvisionResult>;
declare function metadataProvision(_accessor: Accessor, paths: PathSpec[], _texts: string[], _opts: CommandOpts): ProvisionResult;

interface SlackResponse {
    ok: boolean;
    error?: string;
    [key: string]: unknown;
}
declare class SlackApiError extends Error {
    readonly endpoint: string;
    readonly slackError: string;
    constructor(endpoint: string, slackError: string);
}
interface SlackTransport {
    call(endpoint: string, params?: Record<string, string>, body?: unknown): Promise<SlackResponse>;
}
declare abstract class HttpSlackTransport implements SlackTransport {
    protected readonly fetch: typeof fetch;
    protected abstract baseUrl(): string;
    protected abstract authHeaders(): Promise<Record<string, string>> | Record<string, string>;
    call(endpoint: string, params?: Record<string, string>, body?: unknown): Promise<SlackResponse>;
}
declare class NodeSlackTransport extends HttpSlackTransport {
    private readonly token;
    private readonly searchToken?;
    constructor(token: string, searchToken?: string | undefined);
    protected baseUrl(): string;
    protected authHeaders(): Record<string, string>;
    getSearchToken(): string | undefined;
}

interface BrowserSlackTransportOptions {
    proxyUrl: string;
    getHeaders?: () => Promise<Record<string, string>> | Record<string, string>;
}
declare class BrowserSlackTransport extends HttpSlackTransport {
    private readonly opts;
    constructor(opts: BrowserSlackTransportOptions);
    protected baseUrl(): string;
    protected authHeaders(): Promise<Record<string, string>>;
}

declare class SlackAccessor extends Accessor {
    readonly transport: SlackTransport;
    constructor(transport: SlackTransport);
}

declare const DISCORD_API = "https://discord.com/api/v10";
type DiscordMethod = 'GET' | 'POST' | 'PUT';
type DiscordResponse = unknown;
declare class DiscordApiError extends Error {
    readonly endpoint: string;
    readonly status: number;
    readonly discordError: string;
    readonly payload?: unknown | undefined;
    constructor(endpoint: string, status: number, discordError: string, payload?: unknown | undefined);
}
interface DiscordTransport {
    call(method: DiscordMethod, endpoint: string, params?: Record<string, string | number>, body?: Record<string, unknown>): Promise<DiscordResponse>;
}
declare abstract class HttpDiscordTransport implements DiscordTransport {
    protected readonly fetch: typeof fetch;
    protected abstract baseUrl(): string;
    protected abstract authHeaders(): Promise<Record<string, string>> | Record<string, string>;
    call(method: DiscordMethod, endpoint: string, params?: Record<string, string | number>, body?: Record<string, unknown>): Promise<DiscordResponse>;
}

interface BrowserDiscordTransportOptions {
    proxyUrl: string;
    getHeaders?: () => Promise<Record<string, string>> | Record<string, string>;
}
declare class BrowserDiscordTransport extends HttpDiscordTransport {
    private readonly opts;
    constructor(opts: BrowserDiscordTransportOptions);
    protected baseUrl(): string;
    protected authHeaders(): Promise<Record<string, string>>;
}

declare class DiscordAccessor extends Accessor {
    readonly transport: DiscordTransport;
    constructor(transport: DiscordTransport);
}
interface DiscordResourceLike extends Resource {
    readonly accessor: DiscordAccessor;
}

declare const SLACK_COMMANDS: readonly RegisteredCommand[];

declare const SLACK_VFS_OPS: readonly RegisteredOp[];

declare function read$k(accessor: SlackAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$k(accessor: SlackAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$k(accessor: SlackAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveSlackGlob(accessor: SlackAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const SLACK_PROMPT = "{prefix}\n  channels/\n    <channel-name>__<channel-id>/\n      <yyyy-mm-dd>.jsonl        # messages for that date\n  dms/\n    <user-name>__<dm-id>/\n      <yyyy-mm-dd>.jsonl\n  users/\n    <username>.json              # user profile\n  <channel-name> / <user-name> are sanitized \u2014 don't construct them.\n  Always ls directories first to discover exact names (they include IDs).\n  Messages are JSONL \u2014 use jq to extract fields like .text, .user, .ts.";
declare const SLACK_WRITE_PROMPT = "  Write commands:\n    slack-post-message <channel-path> \"message\"\n    slack-reply-to-thread <message-path> \"reply\"";

declare const DISCORD_COMMANDS: readonly RegisteredCommand[];

declare const DISCORD_VFS_OPS: readonly RegisteredOp[];

declare function read$j(accessor: DiscordAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$j(accessor: DiscordAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$j(accessor: DiscordAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveDiscordGlob(accessor: DiscordAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const DISCORD_PROMPT = "{prefix}\n  <guild-name>__<id>/\n    channels/\n      <channel-name>__<id>/\n        <yyyy-mm-dd>.jsonl       # messages for that date\n    members/\n      <username>__<id>.json      # member profile\n  <guild-name> and <channel-name> are sanitized \u2014 don't construct them.\n  Always ls directories first to discover exact names.\n  Messages are JSONL \u2014 use jq to extract .content, .author.username.";
declare const DISCORD_WRITE_PROMPT = "  Write commands:\n    discord-send-message --channel_id=<id> --text=\"message\"\n    discord-add-reaction --channel_id=<id> --message_id=<id> --reaction=\"emoji\"";

declare class TrelloApiError extends Error {
    readonly endpoint: string;
    readonly status: number;
    constructor(endpoint: string, status: number, message: string);
}
interface TrelloTransport {
    call(method: string, path: string, params?: Record<string, string>, body?: unknown): Promise<unknown>;
}
interface HttpTrelloTransportOptions {
    apiKey: string;
    apiToken: string;
    baseUrl?: string;
}
declare class HttpTrelloTransport implements TrelloTransport {
    protected readonly fetch: typeof fetch;
    private readonly apiKey;
    private readonly apiToken;
    private readonly baseUrl;
    constructor(opts: HttpTrelloTransportOptions);
    call(method: string, path: string, params?: Record<string, string>, body?: unknown): Promise<unknown>;
}

declare class TrelloAccessor extends Accessor {
    readonly transport: TrelloTransport;
    constructor(transport: TrelloTransport);
}
interface TrelloResourceLike extends Resource {
    readonly accessor: TrelloAccessor;
}

declare const TRELLO_COMMANDS: readonly RegisteredCommand[];

declare const TRELLO_VFS_OPS: readonly RegisteredOp[];

declare class LinearApiError extends Error {
    readonly errors: readonly unknown[];
    readonly status: number | null;
    constructor(message: string, errors?: readonly unknown[], status?: number | null);
}
interface LinearTransport {
    graphql(query: string, variables?: Record<string, unknown>): Promise<Record<string, unknown>>;
}
interface HttpLinearTransportOptions {
    apiKey: string;
    baseUrl?: string;
}
declare class HttpLinearTransport implements LinearTransport {
    protected readonly fetch: typeof fetch;
    private readonly apiKey;
    private readonly baseUrl;
    constructor(opts: HttpLinearTransportOptions);
    graphql(query: string, variables?: Record<string, unknown>): Promise<Record<string, unknown>>;
}

declare class LinearAccessor extends Accessor {
    readonly transport: LinearTransport;
    constructor(transport: LinearTransport);
}
interface LinearResourceLike extends Resource {
    readonly accessor: LinearAccessor;
}

declare const LINEAR_COMMANDS: readonly RegisteredCommand[];

declare const LINEAR_VFS_OPS: readonly RegisteredOp[];

interface ReadFilter {
    teamIds?: readonly string[];
}
declare function read$i(accessor: LinearAccessor, path: PathSpec, _index?: IndexCacheStore, filter?: ReadFilter): Promise<Uint8Array>;

interface LinearReaddirFilter {
    teamIds?: readonly string[];
}
declare function readdir$i(accessor: LinearAccessor, path: PathSpec, index?: IndexCacheStore, filter?: LinearReaddirFilter): Promise<string[]>;

declare function stat$i(accessor: LinearAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveLinearGlob(accessor: LinearAccessor, paths: readonly PathSpec[], index?: IndexCacheStore, filter?: LinearReaddirFilter): Promise<PathSpec[]>;

declare const LINEAR_PROMPT = "{prefix}\n  teams/\n    <team-key>__<team-name>__<team-id>/\n      team.json\n      members/\n        <display-name>__<user-id>.json\n      issues/\n        <issue-key>__<issue-id>/\n          issue.json\n          comments.jsonl\n      projects/\n        <name>__<project-id>.json\n      cycles/\n        <name>__<cycle-id>.json\n  Always ls directories first to discover exact names.\n\n  Folder/file name anatomy: every entity-named segment is\n    <sanitized-human-readable>__<id>\n  The human-readable part is sanitized \u2014 don't construct it; ls the\n  parent dir to discover exact entry names.\n  IDs are the LAST \"__\"-separated segment. To extract:\n    {prefix}/teams/STR__Strukto-ai__<team-id>/             -> <team-id>\n    {prefix}/teams/.../issues/STR-42__<issue-id>/          -> <issue-id>\n    {prefix}/teams/.../members/Alice__<user-id>.json       -> <user-id>\n\n  All cat output is mirage-normalized (snake_case keys), NOT the raw\n  Linear GraphQL response. Shapes:\n\n  team.json:\n    {\n      \"team_id\": \"...\", \"team_key\": \"STR\", \"team_name\": \"Strukto\",\n      \"name\": \"...\", \"description\": \"...\", \"timezone\": \"...\",\n      \"updated_at\": \"...\",\n      \"states\": [\n        { \"state_id\": \"...\", \"state_name\": \"Todo\", \"type\": \"unstarted\" },\n        ...                              # one element per workflow state\n      ]\n    }\n\n  issue.json:\n    {\n      \"issue_id\": \"...\", \"issue_key\": \"STR-42\",\n      \"title\": \"...\", \"description\": \"...\",\n      \"team_id\": \"...\", \"team_key\": \"...\", \"team_name\": \"...\",\n      \"project_id\": \"...\",   \"project_name\": \"...\",\n      \"cycle_id\": \"...\",     \"cycle_name\": \"...\",   \"cycle_number\": 7,\n      \"state_id\": \"...\",     \"state_name\": \"In Progress\",\n      \"assignee_id\": \"...\",  \"assignee_email\": \"...\", \"assignee_name\": \"...\",\n      \"creator_id\":  \"...\",  \"creator_email\":  \"...\", \"creator_name\":  \"...\",\n      \"priority\": 2,         # 0=none 1=urgent 2=high 3=medium 4=low\n      \"label_ids\":   [\"...\"],\n      \"label_names\": [\"bug\", \"frontend\"],\n      \"created_at\": \"...\", \"updated_at\": \"...\", \"url\": \"...\"\n    }\n\n  comments.jsonl:                      # one normalized comment per line\n    { \"comment_id\": \"...\", \"issue_id\": \"...\", \"issue_key\": \"STR-42\",\n      \"user_id\": \"...\", \"user_email\": \"...\", \"user_name\": \"...\",\n      \"body\": \"...\", \"created_at\": \"...\", \"updated_at\": \"...\",\n      \"url\": \"...\" }\n\n  project.json:\n    {\n      \"project_id\": \"...\", \"team_id\": \"...\",\n      \"name\": \"...\", \"description\": \"...\", \"state\": \"started\",\n      \"lead_id\": \"...\", \"updated_at\": \"...\", \"url\": \"...\",\n      \"issue_count\": 12,\n      \"issues\": [\n        { \"issue_id\": \"...\", \"issue_key\": \"STR-42\", \"title\": \"...\",\n          \"state_id\": \"...\", \"state_name\": \"In Progress\", \"url\": \"...\" }\n      ]\n    }\n\n  cycle.json:\n    {\n      \"cycle_id\": \"...\", \"team_id\": \"...\",\n      \"name\": \"...\", \"number\": 7,\n      \"starts_at\": \"...\", \"ends_at\": \"...\",\n      \"updated_at\": \"...\", \"url\": \"...\"\n    }\n\n  user.json:\n    {\n      \"user_id\": \"...\", \"name\": \"...\", \"display_name\": \"...\",\n      \"email\": \"...\", \"is_active\": true, \"is_admin\": false,\n      \"updated_at\": \"...\", \"url\": \"...\"\n    }\n\n  Useful jq paths:\n    .issue_key                              # issue.json\n    .state_name                             # issue.json\n    .assignee_email                         # issue.json\n    .label_names[]                          # issue.json\n    .issues[] | select(.state_name == \"In Progress\")   # project.json\n    .states[].state_name                    # team.json\n    [inputs] | length                       # comments.jsonl: count\n    [inputs | select(.user_email == \"x\")] | length     # comments.jsonl\n\n  Read commands:\n    linear-search --query \"bug login\"       # full-text across all issues";
declare const LINEAR_WRITE_PROMPT = "  Write commands (flags use UNDERSCORES, not hyphens; --issue_id and\n  --issue_key are interchangeable on every issue command):\n\n    linear-issue-create --team_id <team-id> \\\n      --title \"Title\" --description \"Body\"\n      # or: --description_file /path/to/desc.md\n\n    linear-issue-update --issue_key STR-42 \\\n      [--title \"New title\"] [--description \"...\" | --description_file ...]\n\n    linear-issue-assign --issue_key STR-42 --assignee_email user@example.com\n      # or: --assignee_id <user-id>\n\n    linear-issue-transition --issue_key STR-42 --state_name \"In Review\"\n      # or: --state_id <state-id>\n\n    linear-issue-set-priority --issue_key STR-42 --priority 2\n      # 0=none 1=urgent 2=high 3=medium 4=low\n\n    linear-issue-set-project --issue_key STR-42 --project_id <project-id>\n\n    linear-issue-add-label --issue_key STR-42 --label_id <label-id>\n\n    linear-issue-comment-add --issue_key STR-42 --body \"comment\"\n      # or: --body_file /path/to/comment.md\n\n    linear-issue-comment-update --comment_id <comment-id> --body \"...\"\n\n  IDs come from the path:\n    {prefix}/teams/STR__Strukto-ai__<team-id>/                  -> --team_id\n    {prefix}/teams/.../issues/STR-42__<issue-id>/               -> --issue_id\n    {prefix}/teams/.../projects/Roadmap__<project-id>.json      -> --project_id\n\n  Successful create returns JSON like:\n    {\"issue_id\": \"<uuid>\", \"issue_key\": \"STR-23\", \"title\": \"...\", ...}\n  Empty stdout means the call failed -- inspect stderr or retry.";

interface NotionTransport {
    callTool(name: string, args: Record<string, unknown>): Promise<Record<string, unknown>>;
}
declare class NotionMCPError extends Error {
    readonly code: string | null;
    readonly raw: unknown;
    constructor(message: string, opts?: {
        code?: string | null;
        raw?: unknown;
    });
}
interface MCPNotionTransportOptions {
    authProvider: OAuthClientProvider;
    serverUrl?: string;
}
declare class MCPNotionTransport implements NotionTransport {
    protected client: Client | null;
    private connectPromise;
    private readonly authProvider;
    private readonly serverUrl;
    constructor(opts: MCPNotionTransportOptions);
    private ensureConnected;
    callTool(name: string, args: Record<string, unknown>): Promise<Record<string, unknown>>;
}

declare class NotionAccessor extends Accessor {
    readonly transport: NotionTransport;
    constructor(transport: NotionTransport);
}
interface NotionResourceLike extends Resource {
    readonly accessor: NotionAccessor;
}

interface MemoryOAuthClientProviderOptions {
    clientMetadata: OAuthClientMetadata;
    redirect: (url: URL) => void | Promise<void>;
    redirectUrl?: string | URL;
}
declare class MemoryOAuthClientProvider implements OAuthClientProvider {
    private readonly opts;
    private _clientInformation;
    private _tokens;
    private _codeVerifier;
    constructor(opts: MemoryOAuthClientProviderOptions);
    get redirectUrl(): string | URL | undefined;
    get clientMetadata(): OAuthClientMetadata;
    clientInformation(): OAuthClientInformationMixed | undefined;
    saveClientInformation(info: OAuthClientInformationMixed): void;
    tokens(): OAuthTokens | undefined;
    saveTokens(tokens: OAuthTokens): void;
    redirectToAuthorization(authorizationUrl: URL): void | Promise<void>;
    saveCodeVerifier(codeVerifier: string): void;
    codeVerifier(): string;
    clearTokens(): void;
}

interface NotionReadAccessor {
    readonly transport: NotionTransport;
}
declare function read$h(accessor: NotionReadAccessor, path: PathSpec, _index?: IndexCacheStore): Promise<Uint8Array>;

interface NotionReaddirAccessor {
    readonly transport: NotionTransport;
}
declare function readdir$h(accessor: NotionReaddirAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

interface NotionStatAccessor {
    readonly transport: NotionTransport;
}
declare function stat$h(accessor: NotionStatAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

interface NotionGlobAccessor {
    readonly transport: NotionTransport;
}
declare function resolveNotionGlob(accessor: NotionGlobAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const NOTION_PROMPT = "{prefix}\n  <page-title>__<page-id>/\n    page.json\n    <child-page-title>__<child-id>/\n      page.json\n  Hierarchical page tree. cat shows page content as JSON.\n\n  <page-title> is sanitized \u2014 don't construct it; ls the parent dir.";
declare const NOTION_WRITE_PROMPT = "  Write commands:\n    notion-page-create --parent <parent-path> --title \"title\"";

declare const NOTION_COMMANDS: readonly RegisteredCommand[];

declare const NOTION_VFS_OPS: readonly RegisteredOp[];

declare class LangfuseApiError extends Error {
    readonly errors: readonly unknown[];
    readonly status: number | null;
    constructor(message: string, errors?: readonly unknown[], status?: number | null);
}
interface LangfuseTransport {
    request(path: string, query?: Record<string, string | number | undefined>): Promise<unknown>;
}
interface HttpLangfuseTransportOptions {
    publicKey: string;
    secretKey: string;
    host?: string;
}
declare class HttpLangfuseTransport implements LangfuseTransport {
    protected readonly fetch: typeof fetch;
    private readonly host;
    private readonly auth;
    constructor(opts: HttpLangfuseTransportOptions);
    request(path: string, query?: Record<string, string | number | undefined>): Promise<unknown>;
}

interface LangfuseAccessorConfig {
    defaultTraceLimit?: number;
    defaultSearchLimit?: number;
    defaultFromTimestamp?: string;
}
declare class LangfuseAccessor extends Accessor {
    readonly transport: LangfuseTransport;
    readonly config: LangfuseAccessorConfig;
    constructor(transport: LangfuseTransport, config?: LangfuseAccessorConfig);
}
interface LangfuseResourceLike extends Resource {
    readonly accessor: LangfuseAccessor;
}

declare const LANGFUSE_COMMANDS: readonly RegisteredCommand[];

declare const LANGFUSE_VFS_OPS: readonly RegisteredOp[];

declare function read$g(accessor: LangfuseAccessor, path: PathSpec, _index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$g(accessor: LangfuseAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$g(accessor: LangfuseAccessor, path: PathSpec, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveLangfuseGlob(accessor: LangfuseAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const LANGFUSE_PROMPT = "{prefix}\n  traces/\n    <trace-id>.json\n  sessions/\n    <session-id>/\n      <trace-id>.json\n  prompts/\n    <prompt-name>/\n      <version>.json\n  datasets/\n    <dataset-name>/\n      items.jsonl\n      runs/\n        <run-name>.jsonl";

interface LangfuseScope {
    level: string;
    resourceType: string | null;
    resourceId: string | null;
    subResource: string | null;
    resourcePath: string;
}
declare function detectScope$6(path: PathSpec | string): LangfuseScope;

declare function read$f(accessor: TrelloAccessor, path: PathSpec, _index?: IndexCacheStore): Promise<Uint8Array>;

interface TrelloReaddirFilter {
    workspaceId?: string;
    boardIds?: readonly string[];
}
declare function readdir$f(accessor: TrelloAccessor, path: PathSpec, index?: IndexCacheStore, filter?: TrelloReaddirFilter): Promise<string[]>;

declare function stat$f(accessor: TrelloAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveTrelloGlob(accessor: TrelloAccessor, paths: readonly PathSpec[], index?: IndexCacheStore, filter?: TrelloReaddirFilter): Promise<PathSpec[]>;

declare const TRELLO_PROMPT = "{prefix}\n  workspaces/\n    <workspace-name>__<workspace-id>/\n      workspace.json\n      boards/\n        <board-name>__<board-id>/\n          board.json\n          lists/\n            <list-name>__<list-id>/\n              list.json\n              cards/\n                <card-name>__<card-id>/\n                  card.json\n                  comments.jsonl\n  Always ls directories first to discover exact names.";
declare const TRELLO_WRITE_PROMPT = "  Write commands:\n    trello-card-create <list-path> \"name\" \"description\"\n    trello-card-comment-add <card-path> \"comment\"";

declare const GITHUB_API_BASE = "https://api.github.com";
declare const GITHUB_API_VERSION = "2022-11-28";
interface GitHubTransport {
    get(path: string, params?: Record<string, string>): Promise<unknown>;
}
declare class HttpGitHubTransport implements GitHubTransport {
    readonly token: string;
    readonly baseUrl: string;
    constructor(opts: {
        token: string;
        baseUrl?: string;
    });
    get(path: string, params?: Record<string, string>): Promise<unknown>;
}
declare class GitHubApiError extends Error {
    readonly status: number;
    constructor(message: string, status: number);
}
interface GitHubTreeItem {
    path: string;
    type: 'blob' | 'tree' | 'commit';
    sha: string;
    size?: number;
}
interface GitHubBlob {
    content: string;
    encoding: string;
    sha: string;
    size: number;
}
interface GitHubRepoInfo {
    default_branch: string;
}
declare function fetchRepoInfo(transport: GitHubTransport, owner: string, repo: string): Promise<GitHubRepoInfo>;
declare function fetchTree(transport: GitHubTransport, owner: string, repo: string, ref: string): Promise<{
    tree: GitHubTreeItem[];
    truncated: boolean;
}>;
declare function fetchDirTree(transport: GitHubTransport, owner: string, repo: string, treeSha: string): Promise<GitHubTreeItem[]>;
declare function fetchBlob(transport: GitHubTransport, owner: string, repo: string, sha: string): Promise<Uint8Array>;
interface GitHubCodeSearchResult {
    path: string;
    sha: string;
}

interface TreeEntry {
    path: string;
    type: string;
    sha: string;
    size: number | null;
}
declare function makeTreeEntry(item: GitHubTreeItem): TreeEntry;
declare function indexEntryFromTree(item: GitHubTreeItem): IndexEntry;

declare class GitHubAccessor extends Accessor {
    readonly transport: GitHubTransport;
    readonly owner: string;
    readonly repo: string;
    readonly ref: string;
    readonly defaultBranch: string;
    readonly truncated: boolean;
    readonly tree: Record<string, TreeEntry>;
    constructor(opts: {
        transport: GitHubTransport;
        owner: string;
        repo: string;
        ref: string;
        defaultBranch: string;
        truncated?: boolean;
        tree?: Record<string, TreeEntry>;
    });
    get isDefaultBranch(): boolean;
}
interface GitHubResourceLike extends Resource {
    readonly accessor: GitHubAccessor;
}

declare const GITHUB_COMMANDS: readonly RegisteredCommand[];

declare const GITHUB_VFS_OPS: readonly RegisteredOp[];

declare function read$e(accessor: GitHubAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$7(accessor: GitHubAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$e(accessor: GitHubAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$e(accessor: GitHubAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$e(accessor: GitHubAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare function search(accessor: GitHubAccessor, query: string, pathFilter?: string): Promise<GitHubCodeSearchResult[]>;
declare function narrowPaths(accessor: GitHubAccessor, pattern: string, paths: readonly PathSpec[]): Promise<PathSpec[]>;

declare const GITHUB_PROMPT = "{prefix}\n  Mirrors the GitHub repository file tree.\n  Standard commands: ls, cat, head, tail, grep, rg, wc, find, tree.";

interface CITransport {
    get(path: string, params?: Record<string, string>): Promise<unknown>;
    getBytes(path: string): Promise<Uint8Array>;
    getPaginated(path: string, listKey: string, params?: Record<string, string>, maxResults?: number): Promise<unknown[]>;
}
declare class HttpCITransport implements CITransport {
    readonly token: string;
    readonly baseUrl: string;
    constructor(opts: {
        token: string;
        baseUrl?: string;
    });
    private headers;
    get(path: string, params?: Record<string, string>): Promise<unknown>;
    getBytes(path: string): Promise<Uint8Array>;
    getPaginated(path: string, listKey: string, params?: Record<string, string>, maxResults?: number): Promise<unknown[]>;
}

declare class GitHubCIAccessor extends Accessor {
    readonly transport: CITransport;
    readonly owner: string;
    readonly repo: string;
    readonly days: number;
    readonly maxRuns: number;
    constructor(opts: {
        transport: CITransport;
        owner: string;
        repo: string;
        days?: number;
        maxRuns?: number;
    });
}
interface GitHubCIResourceLike extends Resource {
    readonly accessor: GitHubCIAccessor;
}

declare const GITHUB_CI_COMMANDS: readonly RegisteredCommand[];

declare const GITHUB_CI_VFS_OPS: readonly RegisteredOp[];

declare function read$d(accessor: GitHubCIAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$6(accessor: GitHubCIAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$d(accessor: GitHubCIAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$d(accessor: GitHubCIAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$d(accessor: GitHubCIAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

interface CIWorkflow {
    id: number;
    name?: string;
    path?: string;
    state?: string;
    [k: string]: unknown;
}
declare function listWorkflows(transport: CITransport, owner: string, repo: string): Promise<CIWorkflow[]>;
declare function getWorkflow(transport: CITransport, owner: string, repo: string, workflowId: string): Promise<CIWorkflow>;

interface CIRun {
    id: number;
    name?: string;
    status?: string;
    conclusion?: string;
    event?: string;
    head_branch?: string;
    updated_at?: string;
    [k: string]: unknown;
}
interface CIJob {
    id: number;
    name?: string;
    status?: string;
    conclusion?: string;
    completed_at?: string;
    steps?: unknown[];
    [k: string]: unknown;
}
declare function listRuns(transport: CITransport, owner: string, repo: string, days?: number, maxRuns?: number): Promise<CIRun[]>;
declare function getRun(transport: CITransport, owner: string, repo: string, runId: string): Promise<CIRun>;
declare function listJobsForRun(transport: CITransport, owner: string, repo: string, runId: string): Promise<CIJob[]>;
declare function getJob(transport: CITransport, owner: string, repo: string, jobId: string): Promise<CIJob>;
declare function downloadJobLog(transport: CITransport, owner: string, repo: string, jobId: string): Promise<Uint8Array>;

interface CIArtifact {
    id: number;
    name?: string;
    size_in_bytes?: number;
    [k: string]: unknown;
}
declare function listArtifacts(transport: CITransport, owner: string, repo: string, runId: string): Promise<CIArtifact[]>;
declare function downloadArtifact(transport: CITransport, owner: string, repo: string, artifactId: string): Promise<Uint8Array>;

declare function listAnnotations(transport: CITransport, owner: string, repo: string, checkRunId: string): Promise<unknown[]>;

declare const GITHUB_CI_PROMPT = "{prefix}\n  workflows/\n    <workflow-name>_<workflow-id>.json\n  runs/\n    <workflow-name>_<run-id>/\n      run.json\n      jobs/\n        <job-name>_<job-id>.json\n        <job-name>_<job-id>.log\n      annotations.jsonl\n      artifacts/\n        <artifact-name>_<artifact-id>.zip";

interface GoogleConfig {
    clientId: string;
    clientSecret?: string;
    refreshToken: string;
    refreshFn?: (refreshToken: string) => Promise<{
        accessToken: string;
        expiresIn: number;
    }>;
}

declare const TOKEN_URL = "https://oauth2.googleapis.com/token";
declare const DRIVE_API_BASE = "https://www.googleapis.com/drive/v3";
declare const DOCS_API_BASE = "https://docs.googleapis.com/v1";
declare const SLIDES_API_BASE = "https://slides.googleapis.com/v1";
declare const SHEETS_API_BASE = "https://sheets.googleapis.com/v4";
declare const GMAIL_API_BASE = "https://gmail.googleapis.com/gmail/v1";
declare const TOKEN_BUFFER_SECONDS = 300;
declare class GoogleApiError extends Error {
    readonly status: number;
    constructor(message: string, status: number);
}
declare function refreshAccessToken$2(config: GoogleConfig): Promise<[string, number]>;
declare class TokenManager {
    private readonly config;
    private accessToken;
    private expiresAt;
    private inflight;
    constructor(config: GoogleConfig);
    getToken(): Promise<string>;
    private refresh;
}
declare function googleHeaders(tm: TokenManager): Promise<Record<string, string>>;
declare function googleGet(tm: TokenManager, url: string, params?: Record<string, string | number>): Promise<unknown>;
declare function googlePost(tm: TokenManager, url: string, json: unknown): Promise<unknown>;
declare function googlePut(tm: TokenManager, url: string, json: unknown): Promise<unknown>;
declare function googleGetBytes(tm: TokenManager, url: string): Promise<Uint8Array>;
declare function googleGetStream(tm: TokenManager, url: string): AsyncIterable<Uint8Array>;

declare const MIME_TO_EXT: Readonly<Record<string, string>>;
declare const WORKSPACE_MIMES: ReadonlySet<string>;
interface DriveOwner {
    me?: boolean;
    displayName?: string;
    emailAddress?: string;
}
interface DriveFile {
    id: string;
    name: string;
    mimeType?: string;
    size?: string;
    quotaBytesUsed?: string;
    createdTime?: string;
    modifiedTime?: string;
    owners?: DriveOwner[];
    capabilities?: {
        canEdit?: boolean;
    };
    parents?: string[];
}
declare function listFiles(tm: TokenManager, opts?: {
    folderId?: string;
    mimeType?: string | null;
    trashed?: boolean;
    pageSize?: number;
    modifiedAfter?: string | null;
    modifiedBefore?: string | null;
}): Promise<DriveFile[]>;
declare function listAllFiles(tm: TokenManager, opts?: {
    mimeType?: string | null;
    trashed?: boolean;
    pageSize?: number;
    modifiedAfter?: string | null;
    modifiedBefore?: string | null;
}): Promise<DriveFile[]>;
declare function getFileMetadata$1(tm: TokenManager, fileId: string): Promise<DriveFile>;
declare function downloadFile$1(tm: TokenManager, fileId: string): Promise<Uint8Array>;
declare function downloadFileStream$1(tm: TokenManager, fileId: string): AsyncIterable<Uint8Array>;

declare class GoogleApiAccessor extends Accessor {
    readonly tokenManager: TokenManager;
    constructor(opts: {
        tokenManager: TokenManager;
    });
}

declare class GDocsAccessor extends GoogleApiAccessor {
}

declare const GDOCS_COMMANDS: readonly RegisteredCommand[];

declare const GDOCS_VFS_OPS: readonly RegisteredOp[];

declare function readDoc(tm: TokenManager, docId: string): Promise<Uint8Array>;
declare function read$c(accessor: GDocsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$5(accessor: GDocsAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$c(accessor: GDocsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$c(accessor: GDocsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$c(accessor: GDocsAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare function appendText(tm: TokenManager, docId: string, text: string): Promise<unknown>;

declare function batchUpdate$2(tm: TokenManager, docId: string, requestsJson: string): Promise<unknown>;

declare function createDoc(tm: TokenManager, title: string): Promise<unknown>;

declare const GDOCS_PROMPT = "{prefix}\n  owned/\n    <date>_<title>__<doc-id>.gdoc.json\n  shared/\n    <date>_<title>__<doc-id>.gdoc.json\n\n  Filename: <YYYY-MM-DD>_<title>__<doc-id>.gdoc.json\n    <YYYY-MM-DD>  modifiedTime, used for date-glob (e.g. 2026-05-*)\n    <title>       sanitized: spaces->_, non-[A-Za-z0-9_.-]->_, <=100 chars\n    <doc-id>      Google Docs document ID\n\n  Buckets:\n    owned/   docs you created\n    shared/  docs shared with you by others\n             - does NOT include docs you own and shared with others;\n               those are still in owned/.\n\n  gdoc.json structure (matches the Google Docs API documents.get response):\n    {\n      \"documentId\": \"...\",\n      \"title\": \"...\",\n      \"body\": {\n        \"content\": [\n          {                              # one element per block\n            \"paragraph\": {\n              \"elements\": [\n                { \"textRun\": { \"content\": \"the actual text\\n\",\n                               \"textStyle\": {...} } }\n              ],\n              \"paragraphStyle\": {...}\n            }\n          },\n          { \"table\": {...} },\n          { \"sectionBreak\": {...} }\n        ]\n      },\n      \"documentStyle\": {...},\n      \"namedStyles\": {...},\n      \"revisionId\": \"...\",\n      \"suggestionsViewMode\": \"...\"\n    }\n\n  Useful jq paths:\n    .title\n    .body.content[].paragraph.elements[].textRun.content   # all text\n    [.body.content[] | select(.table)] | length            # table count\n    .revisionId";
declare const GDOCS_WRITE_PROMPT = "  Write commands:\n    gws-docs-write --document <doc-id> --text \"text to append\"\n\n    gws-docs-documents-create --json '{\"title\": \"My Doc\"}'\n\n    gws-docs-documents-batchUpdate \\\n      --params '{\"documentId\": \"<id>\"}' \\\n      --json   '{\"requests\": [{\"insertText\": {\"location\": {\"index\": 1}, \"text\": \"Hello\"}}]}'\n\n  Newline gotcha: bash double-quoted \"...\\n...\" is NOT a newline; the\n  literal characters \\ + n end up in the doc. Either:\n    --text $'line1\\nline2'                          # ANSI-C quoting\n    --text \"$(printf 'line1\\nline2')\"               # printf interprets\n    gws-docs-documents-batchUpdate --json '{...}'    # JSON handles escapes\n\n  Delete:\n    rm {prefix}/owned/<file>.gdoc.json      # permanent delete from Drive\n    rm -f <path>                            # ignore if missing\n    Only operates on .gdoc.json files; owned/ and shared/ themselves\n    cannot be removed.";

interface DocEntry {
    id: string;
    name: string;
    modifiedTime: string;
    createdTime: string;
    owner: string | null;
    ownedByMe: boolean;
    canEdit: boolean;
    filename: string;
}
declare function sanitizeTitle$2(title: string): string;
declare function makeFilename$2(title: string, docId: string, modifiedTime?: string): string;

declare class GSheetsAccessor extends GoogleApiAccessor {
}

declare const GSHEETS_COMMANDS: readonly RegisteredCommand[];

declare const GSHEETS_VFS_OPS: readonly RegisteredOp[];

declare function readSpreadsheet(tm: TokenManager, spreadsheetId: string): Promise<Uint8Array>;
declare function readValues(tm: TokenManager, spreadsheetId: string, range: string): Promise<Uint8Array>;
declare function fetchSheetNames(tm: TokenManager, spreadsheetId: string): Promise<string[]>;
declare function read$b(accessor: GSheetsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$4(accessor: GSheetsAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$b(accessor: GSheetsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$b(accessor: GSheetsAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$b(accessor: GSheetsAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare class SheetsApiError extends Error {
    readonly status: number;
    constructor(message: string, status: number);
}
declare function writeValues(tm: TokenManager, spreadsheetId: string, range: string, valuesJson: string): Promise<unknown>;
declare function appendValues(tm: TokenManager, spreadsheetId: string, range: string, valuesJson: string): Promise<unknown>;

declare function batchUpdate$1(tm: TokenManager, spreadsheetId: string, requestsJson: string): Promise<unknown>;

declare function createSpreadsheet(tm: TokenManager, title: string): Promise<unknown>;

declare const GSHEETS_PROMPT = "{prefix}\n  owned/\n    <date>_<title>__<spreadsheet-id>.gsheet.json\n  shared/\n    <date>_<title>__<spreadsheet-id>.gsheet.json\n\n  Filename: <YYYY-MM-DD>_<title>__<spreadsheet-id>.gsheet.json\n    <YYYY-MM-DD>      modifiedTime, used for date-glob (e.g. 2026-05-*)\n    <title>           sanitized: spaces->_, non-[A-Za-z0-9_.-]->_, <=100 chars\n    <spreadsheet-id>  Google Sheets spreadsheet ID\n\n  Buckets:\n    owned/   sheets you created\n    shared/  sheets shared with you by others\n             - does NOT include sheets you own and shared with others;\n               those are still in owned/.\n\n  gsheet.json structure (matches the Google Sheets API spreadsheets.get response):\n    {\n      \"spreadsheetId\": \"...\",\n      \"spreadsheetUrl\": \"...\",\n      \"properties\": { \"title\": \"...\", \"locale\": \"...\", \"timeZone\": \"...\" },\n      \"sheets\": [\n        {                                      # one element per tab\n          \"properties\": {\n            \"sheetId\": 0, \"title\": \"...\", \"index\": 0,\n            \"gridProperties\": { \"rowCount\": 1000, \"columnCount\": 26 }\n          },\n          \"data\": [\n            {\n              \"rowData\": [\n                { \"values\": [\n                    { \"formattedValue\": \"...\",\n                      \"userEnteredValue\": {...},\n                      \"effectiveValue\": {...} }\n                ]}    # empty cells are omitted, not nullified\n              ]\n            }\n          ]\n        }\n      ],\n      \"namedRanges\": [...]\n    }\n\n  Useful jq paths:\n    .properties.title\n    .sheets[].properties.title                              # tab names\n    .sheets[0].data[0].rowData[].values[].formattedValue    # cell strings\n    .namedRanges[]\n\n  Read commands (alternative to cat for range-scoped reads \u2014 lighter):\n    gws-sheets-read --spreadsheet <id> --range Sheet1!A1:C10";
declare const GSHEETS_WRITE_PROMPT = "  Write commands:\n    gws-sheets-write \\\n      --params '{\"spreadsheetId\": \"<id>\", \"range\": \"Sheet1!A1:B2\", \"valueInputOption\": \"USER_ENTERED\"}' \\\n      --json   '{\"values\": [[\"Name\", \"Score\"], [\"Alice\", 42]]}'\n\n    gws-sheets-append --spreadsheet <id> --range Sheet1!A1 \\\n      --values \"Bob,37\"                          # comma-separated single row\n    gws-sheets-append --spreadsheet <id> --range Sheet1!A1 \\\n      --json-values '[[\"Bob\", 37], [\"Carol\", 51]]'   # multiple rows\n\n    gws-sheets-spreadsheets-create \\\n      --json '{\"properties\": {\"title\": \"Q2 Budget\"}}'\n\n    gws-sheets-spreadsheets-batchUpdate \\\n      --params '{\"spreadsheetId\": \"<id>\"}' \\\n      --json   '{\"requests\": [{\"addSheet\": {\"properties\": {\"title\": \"Q3\"}}}]}'\n\n  Delete:\n    rm {prefix}/owned/<file>.gsheet.json     # permanent delete from Drive\n    rm -f <path>                             # ignore if missing\n    Only operates on .gsheet.json files; owned/ and shared/ themselves\n    cannot be removed.";

interface SheetEntry {
    id: string;
    name: string;
    modifiedTime: string;
    createdTime: string;
    owner: string | null;
    ownedByMe: boolean;
    canEdit: boolean;
    filename: string;
}
declare function sanitizeTitle$1(title: string): string;
declare function makeFilename$1(title: string, docId: string, modifiedTime?: string): string;

declare class GSlidesAccessor extends GoogleApiAccessor {
}

declare const GSLIDES_COMMANDS: readonly RegisteredCommand[];

declare const GSLIDES_VFS_OPS: readonly RegisteredOp[];

declare function readPresentation(tm: TokenManager, presentationId: string): Promise<Uint8Array>;
declare function read$a(accessor: GSlidesAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$3(accessor: GSlidesAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$a(accessor: GSlidesAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$a(accessor: GSlidesAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$a(accessor: GSlidesAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare function batchUpdate(tm: TokenManager, presentationId: string, requestsJson: string): Promise<unknown>;

declare function createPresentation(tm: TokenManager, title: string): Promise<unknown>;

declare const GSLIDES_PROMPT = "{prefix}\n  owned/\n    <date>_<title>__<presentation-id>.gslide.json\n  shared/\n    <date>_<title>__<presentation-id>.gslide.json\n\n  Filename: <YYYY-MM-DD>_<title>__<presentation-id>.gslide.json\n    <YYYY-MM-DD>       modifiedTime, used for date-glob (e.g. 2026-05-*)\n    <title>            sanitized: spaces->_, non-[A-Za-z0-9_.-]->_, <=100 chars\n    <presentation-id>  Google Slides presentation ID\n\n  Buckets:\n    owned/   presentations you created\n    shared/  presentations shared with you by others\n             - does NOT include presentations you own and shared with others;\n               those are still in owned/.\n\n  gslide.json structure (matches the Google Slides API presentations.get response):\n    {\n      \"presentationId\": \"...\",\n      \"title\": \"...\",\n      \"pageSize\": { \"width\": {...}, \"height\": {...} },\n      \"slides\": [\n        {                                      # one element per slide\n          \"objectId\": \"...\",\n          \"pageElements\": [\n            {\n              \"objectId\": \"...\",\n              \"shape\": {\n                \"shapeType\": \"TEXT_BOX\",\n                \"text\": {\n                  \"textElements\": [\n                    { \"textRun\": { \"content\": \"the actual text\\n\",\n                                   \"style\": {...} } }\n                  ]\n                }\n              }\n            },\n            { \"image\": {...} },\n            { \"table\": {...} }\n          ]\n        }\n      ],\n      \"masters\": [...],\n      \"layouts\": [...]\n    }\n\n  Useful jq paths:\n    .title\n    .slides | length                                                    # slide count\n    .slides[].pageElements[].shape.text.textElements[].textRun.content  # all text\n    .slides[0].objectId";
declare const GSLIDES_WRITE_PROMPT = "  Write commands:\n    gws-slides-presentations-create --json '{\"title\": \"My Deck\"}'\n\n    gws-slides-presentations-batchUpdate \\\n      --params '{\"presentationId\": \"<id>\"}' \\\n      --json   '{\"requests\": [{\"createSlide\": {\"insertionIndex\": 1, \"slideLayoutReference\": {\"predefinedLayout\": \"BLANK\"}}}]}'\n\n  Delete:\n    rm {prefix}/owned/<file>.gslide.json     # permanent delete from Drive\n    rm -f <path>                             # ignore if missing\n    Only operates on .gslide.json files; owned/ and shared/ themselves\n    cannot be removed.";

interface SlideEntry {
    id: string;
    name: string;
    modifiedTime: string;
    createdTime: string;
    owner: string | null;
    ownedByMe: boolean;
    canEdit: boolean;
    filename: string;
}
declare function sanitizeTitle(title: string): string;
declare function makeFilename(title: string, docId: string, modifiedTime?: string): string;

declare class GDriveAccessor extends GoogleApiAccessor {
}

declare const GDRIVE_COMMANDS: readonly RegisteredCommand[];

declare const GDRIVE_VFS_OPS: readonly RegisteredOp[];

declare function readBytes(tm: TokenManager, fileId: string): Promise<Uint8Array>;
declare function read$9(accessor: GDriveAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$2(accessor: GDriveAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$9(accessor: GDriveAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$9(accessor: GDriveAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$9(accessor: GDriveAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const GDRIVE_PROMPT = "{prefix}\n  Mirrors Google Drive folder hierarchy. May contain:\n    <name>.gdoc.json    Google Docs    (cat returns gdoc.json - see /gdocs prompt)\n    <name>.gsheet.json  Google Sheets  (cat returns gsheet.json - see /gsheets prompt)\n    <name>.gslide.json  Google Slides  (cat returns gslide.json - see /gslides prompt)\n    <other-files>       PDFs, images, etc. - cat returns raw bytes\n\n  No owned/ vs shared/ split here: gdrive shows the user's full Drive view,\n  including files shared with the user that have been added to My Drive.\n\n  IMPORTANT: This is a remote mount. Prefer targeted reads over full scans.\n  Date-prefixed globs (2026-05-*) push to a Drive modifiedTime range query.\n\n  All gws-* commands from /gdocs, /gsheets, /gslides also work here\n  (the per-service prompts have flag examples).";

declare const DROPBOX_TOKEN_URL = "https://api.dropboxapi.com/oauth2/token";
declare const DROPBOX_API_BASE = "https://api.dropboxapi.com/2";
declare const DROPBOX_CONTENT_BASE = "https://content.dropboxapi.com/2";
interface DropboxConfig {
    clientId: string;
    clientSecret?: string;
    refreshToken: string;
    refreshFn?: (refreshToken: string) => Promise<{
        accessToken: string;
        expiresIn: number;
    }>;
}
declare class DropboxApiError extends Error {
    readonly status: number;
    constructor(message: string, status: number);
}
declare function refreshAccessToken$1(config: DropboxConfig): Promise<[string, number]>;
declare class DropboxTokenManager {
    private readonly config;
    private accessToken;
    private expiresAt;
    private inflight;
    constructor(config: DropboxConfig);
    getToken(): Promise<string>;
    private refresh;
}
declare function dropboxAuthHeaders(tm: DropboxTokenManager): Promise<Record<string, string>>;
declare function dropboxRpc(tm: DropboxTokenManager, endpoint: string, body: unknown): Promise<unknown>;
declare function dropboxDownload(tm: DropboxTokenManager, path: string): Promise<Uint8Array>;
declare function dropboxDownloadStream(tm: DropboxTokenManager, path: string): AsyncIterable<Uint8Array>;

type DropboxEntryTag = 'file' | 'folder' | 'deleted';
interface DropboxEntry {
    '.tag': DropboxEntryTag;
    id?: string;
    name: string;
    path_lower?: string;
    path_display?: string;
    size?: number;
    client_modified?: string;
    server_modified?: string;
    rev?: string;
    content_hash?: string;
}
declare function listFolder(tm: DropboxTokenManager, path: string, opts?: {
    recursive?: boolean;
    limit?: number;
}): Promise<DropboxEntry[]>;
declare function getMetadata(tm: DropboxTokenManager, path: string): Promise<DropboxEntry>;
declare function searchFiles(tm: DropboxTokenManager, query: string, opts?: {
    path?: string;
    maxResults?: number;
    fileStatus?: 'active' | 'deleted';
}): Promise<DropboxEntry[]>;

declare class DropboxAccessor extends Accessor {
    readonly tokenManager: DropboxTokenManager;
    constructor(opts: {
        tokenManager: DropboxTokenManager;
    });
}

declare const DROPBOX_COMMANDS: readonly RegisteredCommand[];

declare const DROPBOX_VFS_OPS: readonly RegisteredOp[];

declare function read$8(accessor: DropboxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream$1(accessor: DropboxAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$8(accessor: DropboxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$8(accessor: DropboxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$8(accessor: DropboxAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const DROPBOX_PROMPT = "{prefix}\n  Mirrors Dropbox folder hierarchy. Each entry is a real file or folder\n  (no special document types \u2014 Dropbox stores opaque files).\n    <name>/             folder\n    <name>.<ext>        file (cat returns raw bytes)\n\n  IMPORTANT: This is a remote mount. Prefer targeted reads over full scans.\n  Use ls on the parent dir first; don't construct paths from memory.\n\n  Available commands (use ONLY these; do not invent commands like `readFile`):\n    Filesystem: ls, cat, head, tail, nl, wc, stat, find, tree, grep, rg,\n                jq, awk, sed, sort, uniq, cut, diff, cmp, du, file,\n                base64, basename, dirname, realpath";

declare const BOX_TOKEN_URL = "https://api.box.com/oauth2/token";
declare const BOX_API_BASE = "https://api.box.com/2.0";
interface BoxConfig {
    clientId?: string;
    clientSecret?: string;
    refreshToken?: string;
    accessToken?: string;
    refreshFn?: (refreshToken: string) => Promise<{
        accessToken: string;
        refreshToken: string;
        expiresIn: number;
    }>;
    onRefreshTokenRotated?: (newRefreshToken: string) => void | Promise<void>;
}
declare class BoxApiError extends Error {
    readonly status: number;
    constructor(message: string, status: number);
}
declare function refreshAccessToken(config: BoxConfig, currentRefreshToken: string): Promise<{
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
}>;
declare class BoxTokenManager {
    private readonly config;
    private readonly devTokenMode;
    private currentRefreshToken;
    private accessToken;
    private expiresAt;
    private inflight;
    constructor(config: BoxConfig);
    /**
     * Returns the latest refresh token. Box rotates the refresh token on each
     * refresh, so the token passed to the constructor may be stale after the
     * first refresh. Persist this value if you want to survive restarts without
     * re-authenticating. Returns empty string in developer-token mode.
     */
    getRefreshToken(): string;
    getToken(): Promise<string>;
    private refresh;
}
declare function boxAuthHeaders(tm: BoxTokenManager): Promise<Record<string, string>>;
declare function boxGet(tm: BoxTokenManager, url: string, params?: Record<string, string | number>): Promise<unknown>;
declare function boxGetBytes(tm: BoxTokenManager, url: string, params?: Record<string, string | number>): Promise<Uint8Array>;
declare function boxGetStream(tm: BoxTokenManager, url: string, params?: Record<string, string | number>): AsyncIterable<Uint8Array>;

type BoxItemType = 'file' | 'folder' | 'web_link';
interface BoxItem {
    type: BoxItemType;
    id: string;
    name: string;
    size?: number;
    modified_at?: string;
    etag?: string;
    sha1?: string;
    parent?: {
        id: string;
        type: 'folder';
    } | null;
}
declare function listFolderItems(tm: BoxTokenManager, folderId: string, opts?: {
    limit?: number;
}): Promise<BoxItem[]>;
declare function getFileMetadata(tm: BoxTokenManager, fileId: string): Promise<BoxItem>;
declare function getFolderMetadata(tm: BoxTokenManager, folderId: string): Promise<BoxItem>;
declare function downloadFile(tm: BoxTokenManager, fileId: string): Promise<Uint8Array>;
declare function downloadFileStream(tm: BoxTokenManager, fileId: string): AsyncIterable<Uint8Array>;
declare function searchItems(tm: BoxTokenManager, query: string, opts?: {
    limit?: number;
    type?: 'file' | 'folder';
}): Promise<BoxItem[]>;
/**
 * Fetches the auto-extracted plain-text representation of a Box file.
 * Box transcodes .docx / .xlsx / .pptx (and many other formats) server-side
 * into plain text, exposed via the `representations` API. Returns "" if the
 * representation isn't ready or doesn't exist for this file type.
 */
declare function getExtractedText(tm: BoxTokenManager, fileId: string): Promise<string>;

declare class BoxAccessor extends Accessor {
    readonly tokenManager: BoxTokenManager;
    constructor(opts: {
        tokenManager: BoxTokenManager;
    });
}

declare const BOX_COMMANDS: readonly RegisteredCommand[];

declare const BOX_VFS_OPS: readonly RegisteredOp[];

declare function read$7(accessor: BoxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;
declare function stream(accessor: BoxAccessor, path: PathSpec, index?: IndexCacheStore): AsyncIterable<Uint8Array>;

declare function readdir$7(accessor: BoxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$7(accessor: BoxAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$7(accessor: BoxAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

declare const BOX_PROMPT = "{prefix}\n  Mirrors Box folder hierarchy. May contain:\n    <name>.boxnote.json     Box Note            (cat returns boxnote.json shape)\n    <name>.boxcanvas.json   Box Canvas          (cat returns boxcanvas.json shape)\n    <name>.gdoc.json        Box's Google Doc    (cat returns box-office.json shape)\n    <name>.gsheet.json      Box's Google Sheet  (cat returns box-office.json shape)\n    <name>.gslides.json     Box's Google Slides (cat returns box-office.json shape)\n    <other-files>           PDFs, images, parquet, etc. - cat returns raw bytes\n\n  IMPORTANT: This is a remote mount. Prefer targeted reads over full scans.\n  Box uses numeric folder IDs internally (root = 0); mirage caches the\n  path -> id mapping, so nested dirs cost one API call per level on first\n  access. Use ls on the parent dir before constructing a path.\n\n  JSON shapes returned by cat for the special file types:\n\n  boxnote.json {\n    \"id\":           \"...\",\n    \"body_text\":    \"paragraphs joined by \\n\",\n    \"paragraphs\":   [ { \"text\": \"...\", \"authors\": [\"...\"] } ],\n    \"authors\":      { \"<id>\": \"Author Name\" },\n    \"last_edit_at\": \"...\"\n  }\n\n  boxcanvas.json {\n    \"id\":              \"...\",\n    \"widget_count\":    ...,\n    \"widgets_by_type\": { \"shape\": ..., \"link\": ... },\n    \"body_text\":       \"shape labels joined by \\n\",\n    \"widgets\":         [ { \"id\": \"...\", \"type\": \"shape\", \"text\": \"...\" } ],\n    \"authors\":         [ \"...\" ]\n  }\n\n  box-office.json {\n    \"id\":          \"...\",\n    \"name\":        \"...\",\n    \"format\":      \"docx\" | \"xlsx\" | \"pptx\",\n    \"size\":        ...,\n    \"modified_at\": \"...\",\n    \"body_text\":   \"auto-extracted plain text\"\n  }\n\n  For plain text from any of these: cat <path> | jq -r .body_text";

interface BoxnoteParagraph {
    text: string;
    authors: string[];
}
interface BoxnoteProcessed {
    id: string;
    body_text: string;
    paragraphs: BoxnoteParagraph[];
    authors: Record<string, string>;
    last_edit_at: string;
}
/**
 * Restructures Box's raw .boxnote JSON into a clean, agent-friendly shape
 * with a top-level body_text field for one-shot reads. Mirrors the gdocs
 * read pattern.
 */
declare function processBoxnote(rawBytes: Uint8Array): Uint8Array;

interface BoxcanvasProcessedWidget {
    id: string;
    type: string;
    user_id: string;
    created_at: string;
    modified_at: string;
    modified_by: string;
    text: string;
}
interface BoxcanvasProcessed {
    id: string;
    widget_count: number;
    widgets_by_type: Record<string, number>;
    body_text: string;
    widgets: BoxcanvasProcessedWidget[];
    authors: string[];
}
/**
 * Restructures Box's raw .boxcanvas JSON into a clean, agent-friendly shape:
 * widget counts by type, concatenated body_text from all widgets that carry
 * doc content, and per-widget metadata. Mirrors the boxnote / gdocs pattern.
 */
declare function processBoxcanvas(rawBytes: Uint8Array): Uint8Array;

declare class GmailAccessor extends GoogleApiAccessor {
}

declare const GMAIL_COMMANDS: readonly RegisteredCommand[];

declare const GMAIL_VFS_OPS: readonly RegisteredOp[];

declare function read$6(accessor: GmailAccessor, path: PathSpec, index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$6(accessor: GmailAccessor, path: PathSpec, index?: IndexCacheStore): Promise<string[]>;

declare function stat$6(accessor: GmailAccessor, path: PathSpec, index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$6(accessor: GmailAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

interface GmailScope {
    useNative: boolean;
    labelName: string | null;
    dateStr: string | null;
    resourcePath: string;
}
declare function detectScope$5(path: PathSpec): GmailScope;

interface GmailLabel {
    id: string;
    name?: string;
    type?: string;
}
declare function listLabels(tokenManager: TokenManager): Promise<GmailLabel[]>;

interface GmailHeader {
    name?: string;
    value?: string;
}
interface GmailBody {
    data?: string;
    size?: number;
    attachmentId?: string;
}
interface GmailPayload {
    mimeType?: string;
    body?: GmailBody;
    headers?: GmailHeader[];
    filename?: string;
    parts?: GmailPayload[];
}
interface GmailMessageRaw {
    id?: string;
    threadId?: string;
    internalDate?: string;
    labelIds?: string[];
    snippet?: string;
    sizeEstimate?: number;
    payload?: GmailPayload;
}
interface GmailMessageStub {
    id: string;
    threadId?: string;
}
interface GmailAttachmentInfo {
    filename: string;
    attachmentId: string;
    size: number;
}
interface GmailAddress {
    name: string;
    email: string;
}
interface GmailMessageProcessed {
    id: string;
    thread_id: string;
    from: GmailAddress;
    to: GmailAddress[];
    cc: GmailAddress[];
    subject: string;
    date: string;
    body_text: string;
    snippet: string;
    labels: string[];
}
interface ListMessagesOptions {
    labelId?: string | null;
    query?: string | null;
    maxResults?: number;
}
declare function listMessages(tokenManager: TokenManager, opts?: ListMessagesOptions): Promise<GmailMessageStub[]>;
declare function getMessageRaw(tokenManager: TokenManager, messageId: string): Promise<GmailMessageRaw>;
declare function decodeBody(payload: GmailPayload | undefined): string;
declare function extractHeader(headers: GmailHeader[] | undefined, name: string): string;
declare function parseAddress(raw: string): GmailAddress;
declare function parseAddressList(raw: string): GmailAddress[];
declare function getAttachment(tokenManager: TokenManager, messageId: string, attachmentId: string): Promise<Uint8Array>;
declare function extractAttachments(payload: GmailPayload | undefined): GmailAttachmentInfo[];
declare function getMessageProcessed(tokenManager: TokenManager, messageId: string): Promise<GmailMessageProcessed>;

declare function sendMessage(tokenManager: TokenManager, to: string, subject: string, body: string): Promise<unknown>;
declare function replyMessage(tokenManager: TokenManager, messageId: string, body: string): Promise<unknown>;
declare function replyAllMessage(tokenManager: TokenManager, messageId: string, body: string): Promise<unknown>;
declare function forwardMessage(tokenManager: TokenManager, messageId: string, to: string): Promise<unknown>;

interface GmailSearchRow {
    id: string;
    subject: string;
    snippet: string;
    sender: string;
    date: string;
    label: string;
    bodyText: string;
}
declare function searchMessages(tokenManager: TokenManager, pattern: string, labelName?: string | null, dateStr?: string | null, maxResults?: number): Promise<GmailSearchRow[]>;
declare function formatGrepResults$2(rows: GmailSearchRow[], scope: GmailScope, prefix: string, pattern?: string): string[];

declare const GMAIL_PROMPT = "{prefix}\n  <label>/\n    <yyyy-mm-dd>/\n      <subject>__<message-id>.gmail.json    # email JSON file (the email itself)\n      <subject>__<message-id>/              # attachments dir, only if any\n        <attachment-filename>\n\n  Email body is in the .gmail.json file. The sibling dir holds attachments\n  only. Read with `cat`/`head`/`jq` on `<path>.gmail.json` (keep the suffix).\n\n  Commands: cat, ls, head, tail, nl, wc, stat, find, tree, grep, rg, jq,\n  basename, dirname, realpath, gws-gmail-read, gws-gmail-triage.\n  No others (no readFile, etc.).\n\n  Path: <label>/<yyyy-mm-dd>/<subject>__<message-id>.gmail.json\n    <label>       Gmail label (INBOX, SENT, DRAFT, IMPORTANT, STARRED,\n                  TRASH, SPAM, or any user label)\n    <yyyy-mm-dd>  date the message was received, used for date narrowing\n                  (ls {prefix}/INBOX/2026-05-03/ pushes after:/before: into the\n                  Gmail query - much cheaper than scanning the whole label)\n    <subject>     sanitized subject (don't construct it; ls the date dir)\n    <message-id>  Gmail message ID\n\n  email JSON structure (mirage-processed, NOT the raw Gmail API response):\n    {\n      \"id\": \"...\",\n      \"thread_id\": \"...\",\n      \"from\":    { \"name\": \"...\", \"email\": \"...\" },\n      \"to\":      [ { \"name\": \"...\", \"email\": \"...\" } ],\n      \"cc\":      [ ... ],\n      \"subject\": \"...\",\n      \"date\":    \"Mon, 3 May 2026 10:00:00 -0700\",\n      \"body_text\": \"decoded plain-text body\",\n      \"snippet\":   \"first ~200 chars from Gmail\",\n      \"labels\":  [ \"INBOX\", \"IMPORTANT\", ... ],\n      \"attachments\": [\n        { \"id\": \"...\", \"filename\": \"invoice.pdf\",\n          \"mime_type\": \"application/pdf\", \"size\": 12345 }\n      ]\n    }\n\n  Attachments: each message with attachments has a sibling directory at\n  <subject>__<message-id>/ (same name as the .gmail.json file but without\n  the extension). Cat a file inside to download bytes. grep -r over a\n  date dir skips these binary files automatically.\n\n  After a grep/rg hit, the result line points to .../<subject>__<id>.gmail.json\n  \u2014 the sibling attachments dir is NOT in grep output. Once you have the\n  message id (or the .gmail.json path), ls the same path with .gmail.json\n  stripped to list attachments directly:\n    ls {prefix}/INBOX/2026-05-03/<subject>__<message-id>/\n  ENOENT means the message has no attachments.\n\n  Useful jq paths:\n    .subject\n    .from.email\n    .body_text\n    .labels[]\n    .attachments[] | .filename\n\n  Read commands:\n    gws-gmail-read --id <message-id>             # same shape as cat\n    gws-gmail-triage --query \"is:unread\" --max 20  # summary list (id, from,\n                                                   # subject, date, snippet)";
declare const GMAIL_WRITE_PROMPT = "  Write commands:\n    gws-gmail-send --to \"to@email.com\" --subject \"Hi\" --body \"...\"\n\n    gws-gmail-reply     --message-id <id> --body \"...\"\n    gws-gmail-reply-all --message-id <id> --body \"...\"\n    gws-gmail-forward   --message-id <id> --to \"to@email.com\"\n\n  Body gotcha: bash double-quoted \"...\\n...\" is NOT a newline.\n  Use $'line1\\nline2' (ANSI-C quoting) or \"$(printf '...\\n...')\"\n  if you need real newlines in the body.\n\n  Delete:\n    rm {prefix}/<label>/<yyyy-mm-dd>/<subject>__<message-id>.gmail.json\n                                            # moves the message to Trash\n    rm -f <path>                            # ignore if missing\n    Only operates on .gmail.json files; label/, date/, and the\n    attachments folder cannot be removed (they are virtual).";

interface OptionalPeerConfig {
    feature: string;
    packageName: string;
    docsUrl?: string;
}
declare function loadOptionalPeer<T>(importer: () => Promise<T>, config: OptionalPeerConfig): Promise<T>;

type ValueTransform = (value: unknown) => unknown;
interface FieldNormalizer {
    /** Explicit `python_snake_case` → `tsCamelCase` overrides. */
    rename?: Record<string, string>;
    /** Optional per-source-key value transforms (run before rename). */
    transform?: Record<string, ValueTransform>;
    /** Source keys to drop silently (e.g. unsupported fields). */
    drop?: readonly string[];
}
declare function snakeToCamel(snake: string): string;
/**
 * Translate a Python-style snake_case config blob to a TS camelCase one.
 *
 * Lookup order for each input key:
 *   1. `drop` — silently skipped.
 *   2. `rename` — used as the output key.
 *   3. Default — `snakeToCamel(key)`. Already-camelCase keys round-trip unchanged.
 *
 * Values pass through untouched unless `transform[key]` is set.
 */
declare function normalizeFields(input: Record<string, unknown>, spec?: FieldNormalizer): Record<string, unknown>;

interface PgQueryResult<R = Record<string, unknown>> {
    rows: R[];
    rowCount: number;
}
interface PgDriver {
    query<R = Record<string, unknown>>(sql: string, params?: readonly unknown[]): Promise<PgQueryResult<R>>;
    close(): Promise<void>;
}

interface PostgresConfig {
    dsn: string;
    schemas?: readonly string[];
    defaultRowLimit?: number;
    maxReadRows?: number;
    maxReadBytes?: number;
    defaultSearchLimit?: number;
}
interface PostgresConfigResolved {
    dsn: string;
    schemas: readonly string[] | null;
    defaultRowLimit: number;
    maxReadRows: number;
    maxReadBytes: number;
    defaultSearchLimit: number;
}
declare function normalizePostgresConfig(input: Record<string, unknown>): PostgresConfig;
declare function resolvePostgresConfig(config: PostgresConfig): PostgresConfigResolved;

declare class PostgresAccessor extends Accessor {
    readonly driver: PgDriver;
    readonly config: PostgresConfigResolved;
    constructor(driver: PgDriver, config: PostgresConfigResolved);
    get store(): PgDriver;
}

declare const POSTGRES_PROMPT = "{prefix}\n  database.json                  cross-schema graph + sizes\n  <schema>/                      Postgres schema (namespace)\n    tables/<table>/\n      schema.json                column types, PK/FK, indexes\n      rows.jsonl                 data (size-guarded)\n    views/<view>/\n      schema.json\n      rows.jsonl\n  Read database.json first to plan joins. Reading rows.jsonl is refused\n  for tables above the configured row/byte threshold; use head, tail, wc,\n  or grep, all of which push predicates down to SQL.";

declare const POSTGRES_OPS: readonly RegisteredOp[];

declare const POSTGRES_COMMANDS: readonly RegisteredCommand[];

interface ReadOptions$1 {
    limit?: number | null;
    offset?: number | null;
}
declare function read$5(accessor: PostgresAccessor, path: PathSpec | string, _index?: IndexCacheStore, options?: ReadOptions$1): Promise<Uint8Array>;

declare function readdir$5(accessor: PostgresAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat$5(accessor: PostgresAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$5(accessor: PostgresAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

type EntityKind = 'tables' | 'views';
type PostgresScope = {
    level: 'root';
    resourcePath: string;
} | {
    level: 'database_json';
    file: 'database.json';
    resourcePath: string;
} | {
    level: 'schema';
    schema: string;
    resourcePath: string;
} | {
    level: 'kind';
    schema: string;
    kind: EntityKind;
    resourcePath: string;
} | {
    level: 'entity';
    schema: string;
    kind: EntityKind;
    entity: string;
    resourcePath: string;
} | {
    level: 'entity_schema';
    schema: string;
    kind: EntityKind;
    entity: string;
    file: 'schema.json';
    resourcePath: string;
} | {
    level: 'entity_rows';
    schema: string;
    kind: EntityKind;
    entity: string;
    file: 'rows.jsonl';
    resourcePath: string;
} | {
    level: 'invalid';
    resourcePath: string;
};
declare function detectScope$4(path: PathSpec | string): PostgresScope;

interface EntityMatches {
    schema: string;
    kind: string;
    entity: string;
    rows: Record<string, unknown>[];
}
declare function searchEntity(accessor: PostgresAccessor, schema: string, _kind: string, entity: string, pattern: string, limit: number): Promise<Record<string, unknown>[]>;
declare function searchKind(accessor: PostgresAccessor, schema: string, kind: string, pattern: string, limit: number): Promise<EntityMatches[]>;
declare function searchSchema(accessor: PostgresAccessor, schema: string, pattern: string, limit: number): Promise<EntityMatches[]>;
declare function searchDatabase$1(accessor: PostgresAccessor, pattern: string, limit: number): Promise<EntityMatches[]>;
declare function formatGrepResults$1(results: readonly EntityMatches[]): string[];

interface MongoFindOptions {
    limit?: number;
    sort?: Record<string, 1 | -1>;
    projection?: Record<string, unknown>;
    skip?: number;
}
interface MongoDriver {
    listDatabases(): Promise<string[]>;
    listCollections(database: string): Promise<string[]>;
    findDocuments<T = Record<string, unknown>>(database: string, collection: string, filter?: Record<string, unknown>, options?: MongoFindOptions): Promise<T[]>;
    countDocuments(database: string, collection: string, filter?: Record<string, unknown>): Promise<number>;
    listIndexes(database: string, collection: string): Promise<Record<string, unknown>[]>;
    close(): Promise<void>;
}

interface MongoDBConfig {
    uri: string;
    databases?: readonly string[];
    defaultDocLimit?: number;
    defaultSearchLimit?: number;
    maxDocLimit?: number;
}
interface MongoDBConfigResolved {
    uri: string;
    databases: readonly string[] | null;
    defaultDocLimit: number;
    defaultSearchLimit: number;
    maxDocLimit: number;
}
declare function normalizeMongoDBConfig(input: Record<string, unknown>): MongoDBConfig;
declare function resolveMongoDBConfig(config: MongoDBConfig): MongoDBConfigResolved;

declare class MongoDBAccessor extends Accessor {
    readonly driver: MongoDriver;
    readonly config: MongoDBConfigResolved;
    constructor(driver: MongoDriver, config: MongoDBConfigResolved);
}

declare const MONGODB_PROMPT = "{prefix}\n  <database>/\n    <collection>.jsonl\n  Each .jsonl file is a collection. Documents are JSON objects, one per line.\n  Use jq to query fields. grep to search across documents.";

declare const MONGODB_OPS: readonly RegisteredOp[];

declare const MONGODB_COMMANDS: readonly RegisteredCommand[];

interface ReadOptions {
    limit?: number | null;
    offset?: number | null;
}
declare function read$4(accessor: MongoDBAccessor, path: PathSpec | string, _index?: IndexCacheStore, options?: ReadOptions): Promise<Uint8Array>;

declare function readdir$4(accessor: MongoDBAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat$4(accessor: MongoDBAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$4(accessor: MongoDBAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

interface MongoDBScope {
    level: 'root' | 'database' | 'file' | 'invalid';
    database: string | null;
    collection: string | null;
    resourcePath: string;
}
interface DetectScopeOptions {
    singleDb?: boolean;
    singleDbName?: string | null;
}
declare function detectScope$3(path: PathSpec | string, options?: DetectScopeOptions): MongoDBScope;

declare function listDatabases(accessor: MongoDBAccessor): Promise<string[]>;
declare function listCollections(accessor: MongoDBAccessor, database: string): Promise<string[]>;
declare function findDocuments<T = Record<string, unknown>>(accessor: MongoDBAccessor, database: string, collection: string, filter?: Record<string, unknown>, options?: MongoFindOptions): Promise<T[]>;
declare function countDocuments(accessor: MongoDBAccessor, database: string, collection: string, filter?: Record<string, unknown>): Promise<number>;
declare function listIndexes(accessor: MongoDBAccessor, database: string, collection: string): Promise<Record<string, unknown>[]>;

interface CollectionMatches {
    database: string;
    collection: string;
    docs: Record<string, unknown>[];
}
declare function searchCollection(accessor: MongoDBAccessor, database: string, collection: string, pattern: string, limit: number): Promise<Record<string, unknown>[]>;
declare function searchDatabase(accessor: MongoDBAccessor, database: string, pattern: string, limit: number): Promise<CollectionMatches[]>;
declare function formatGrepResults(results: readonly CollectionMatches[]): string[];

interface SSCholarAuthorProfile {
    authorId: string;
    name: string;
    url?: string | null;
    affiliations?: string[] | null;
    homepage?: string | null;
    paperCount?: number | null;
    citationCount?: number | null;
    hIndex?: number | null;
    externalIds?: Record<string, string[]> | null;
}
interface SSCholarAuthorPapersOptions {
    limit?: number;
    offset?: number;
    fields?: readonly string[];
}
interface SSCholarAuthorPapersResult {
    offset: number;
    next?: number | null;
    data: {
        paperId: string;
        title?: string | null;
        year?: number | null;
        fieldsOfStudy?: string[] | null;
    }[];
}
interface SSCholarAuthorSearchResult {
    total: number;
    offset: number;
    next?: number | null;
    data: SSCholarAuthorProfile[];
}

interface SSCholarAuthor {
    authorId: string | null;
    name: string;
}
interface SSCholarTLDR {
    model: string;
    text: string;
}
interface SSCholarPaper {
    paperId: string;
    externalIds?: Record<string, string> | null;
    title?: string | null;
    abstract?: string | null;
    year?: number | null;
    venue?: string | null;
    publicationDate?: string | null;
    authors?: SSCholarAuthor[] | null;
    tldr?: SSCholarTLDR | null;
    fieldsOfStudy?: string[] | null;
    citationCount?: number | null;
    referenceCount?: number | null;
    influentialCitationCount?: number | null;
    openAccessPdf?: {
        url?: string | null;
    } | null;
}
interface SSCholarSearchOptions {
    query?: string;
    fieldsOfStudy?: string;
    year?: string | number;
    limit?: number;
    offset?: number;
    sort?: string;
    fields?: readonly string[];
}
interface SSCholarSearchResult {
    total: number;
    offset: number;
    next?: number | null;
    data: SSCholarPaper[];
}
interface SSCholarSnippet {
    text: string;
    snippetKind?: string | null;
    section?: string | null;
    snippetOffset?: {
        start: number;
        end: number;
    } | null;
    paper: {
        corpusId?: number | null;
        openAccessInfo?: {
            license?: string | null;
        } | null;
    };
}
interface SSCholarSnippetMatch {
    snippet: SSCholarSnippet;
    paper: SSCholarPaper;
    score?: number | null;
}
interface SSCholarSnippetSearchResult {
    retrievalVersion?: string | null;
    next?: number | null;
    data: SSCholarSnippetMatch[];
}

interface SSCholarDriver {
    getPaper(paperId: string, fields?: readonly string[]): Promise<SSCholarPaper>;
    searchPapers(options: SSCholarSearchOptions): Promise<SSCholarSearchResult>;
    searchSnippets(query: string, limit?: number): Promise<SSCholarSnippetSearchResult>;
    getAuthor(authorId: string, fields?: readonly string[]): Promise<SSCholarAuthorProfile>;
    getAuthorPapers(authorId: string, options?: SSCholarAuthorPapersOptions): Promise<SSCholarAuthorPapersResult>;
    searchAuthors(query: string, limit?: number, fields?: readonly string[]): Promise<SSCholarAuthorSearchResult>;
    close(): Promise<void>;
}

interface SSCholarConfig {
    apiKey?: string | null;
    baseUrl?: string;
    defaultListLimit?: number;
    defaultSearchLimit?: number;
    defaultSnippetLimit?: number;
}
interface SSCholarConfigResolved {
    apiKey: string | null;
    baseUrl: string;
    defaultListLimit: number;
    defaultSearchLimit: number;
    defaultSnippetLimit: number;
}
declare function normalizeSSCholarConfig(input: Record<string, unknown>): SSCholarConfig;
declare function resolveSSCholarConfig(config?: SSCholarConfig): SSCholarConfigResolved;

declare class SSCholarAccessor extends Accessor {
    readonly driver: SSCholarDriver;
    readonly config: SSCholarConfigResolved;
    constructor(driver: SSCholarDriver, config: SSCholarConfigResolved);
}

declare const SSCHOLAR_PAPER_PROMPT = "{prefix}\n  <field-slug>/                e.g. computer-science, medicine, biology (23 fields)\n    <year>/                    2000..now\n      <paperId>/\n        meta.json              title, year, venue, citations, openAccessPdf\n        abstract.txt           full abstract\n        tldr.txt               one-sentence AI summary\n        authors.json           [{authorId, name}, ...]\n  Listings show top 100 most recent papers in <field>/<year>/.\n  Use 'search \"<query>\"' to query papers by topic. Use 'grep \"<query>\"' to find passages.";

declare const SSCHOLAR_PAPER_OPS: readonly RegisteredOp[];

declare const SSCHOLAR_PAPER_COMMANDS: readonly RegisteredCommand[];

declare function read$3(accessor: SSCholarAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$3(accessor: SSCholarAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat$3(_accessor: SSCholarAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$3(accessor: SSCholarAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

type SSCholarPaperLevel = 'root' | 'field' | 'year' | 'paper' | 'file' | 'invalid';
interface SSCholarPaperScope {
    level: SSCholarPaperLevel;
    fieldSlug: string | null;
    field: string | null;
    year: string | null;
    paperId: string | null;
    filename: string | null;
    resourcePath: string;
}
declare function detectScope$2(path: PathSpec | string): SSCholarPaperScope;

declare const SSCHOLAR_FIELDS: readonly string[];
declare function fieldToSlug(field: string): string;
declare function slugToField(slug: string): string | null;
declare const SSCHOLAR_FIELD_SLUGS: readonly string[];
declare const SSCHOLAR_YEARS: readonly string[];
declare const PAPER_FILES: readonly string[];

declare function getPaper(accessor: SSCholarAccessor, paperId: string, fields?: readonly string[]): Promise<SSCholarPaper>;
declare function searchPapersByField(accessor: SSCholarAccessor, field: string, year: string | null, limit: number, offset?: number): Promise<SSCholarSearchResult>;
declare function searchPapers(accessor: SSCholarAccessor, query: string, field: string | null, year: string | null, limit: number): Promise<SSCholarSearchResult>;
declare function searchSnippets(accessor: SSCholarAccessor, query: string, limit: number): Promise<SSCholarSnippetSearchResult>;

interface HttpSSCholarDriverOptions {
    baseUrl?: string;
    apiKey?: string | null;
    fetchImpl?: typeof fetch;
    headers?: Record<string, string>;
}
declare class HttpSSCholarDriver implements SSCholarDriver {
    readonly baseUrl: string;
    private readonly apiKey;
    private readonly fetchImpl;
    private readonly headers;
    constructor(options?: HttpSSCholarDriverOptions);
    private buildHeaders;
    private getJson;
    getPaper(paperId: string, fields?: readonly string[]): Promise<SSCholarPaper>;
    searchPapers(options: SSCholarSearchOptions): Promise<SSCholarSearchResult>;
    searchSnippets(query: string, limit?: number): Promise<SSCholarSnippetSearchResult>;
    getAuthor(authorId: string, fields?: readonly string[]): Promise<SSCholarAuthorProfile>;
    getAuthorPapers(authorId: string, options?: SSCholarAuthorPapersOptions): Promise<SSCholarAuthorPapersResult>;
    searchAuthors(query: string, limit?: number, fields?: readonly string[]): Promise<SSCholarAuthorSearchResult>;
    close(): Promise<void>;
}

declare const SSCHOLAR_AUTHOR_PROMPT = "{prefix}\n  <authorId>/\n    profile.json   author profile (name, affiliations, paperCount, hIndex, citationCount)\n    papers.json    list of papers by the author [{paperId, title, year}, ...]\n  Use 'find-author \"<name>\"' to search authors by name.";

declare const SSCHOLAR_AUTHOR_OPS: readonly RegisteredOp[];

declare const SSCHOLAR_AUTHOR_COMMANDS: readonly RegisteredCommand[];

declare function read$2(accessor: SSCholarAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$2(_accessor: SSCholarAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat$2(_accessor: SSCholarAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$2(accessor: SSCholarAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

type SSCholarAuthorLevel = 'root' | 'author' | 'file' | 'papers' | 'invalid';
interface SSCholarAuthorScope {
    level: SSCholarAuthorLevel;
    authorId: string | null;
    filename: string | null;
    resourcePath: string;
}
declare const AUTHOR_FILES: readonly string[];
declare function detectAuthorScope(path: PathSpec | string): SSCholarAuthorScope;

declare function getAuthor(accessor: SSCholarAccessor, authorId: string, fields?: readonly string[]): Promise<SSCholarAuthorProfile>;
declare function getAuthorPapers(accessor: SSCholarAccessor, authorId: string, limit?: number): Promise<SSCholarAuthorPapersResult>;
declare function searchAuthors(accessor: SSCholarAccessor, query: string, limit?: number): Promise<SSCholarAuthorSearchResult>;

interface VercelUser {
    id: string;
    username?: string | null;
    email?: string | null;
    name?: string | null;
    createdAt?: number | null;
    avatar?: string | null;
}
interface VercelTeam {
    id: string;
    slug?: string | null;
    name?: string | null;
    createdAt?: number | null;
    avatar?: string | null;
}
interface VercelTeamMember {
    uid: string;
    username?: string | null;
    email?: string | null;
    role?: string | null;
    createdAt?: number | null;
}
interface VercelProject {
    id: string;
    name: string;
    accountId?: string | null;
    framework?: string | null;
    createdAt?: number | null;
    updatedAt?: number | null;
    link?: {
        type?: string;
        repo?: string;
        org?: string;
    } | null;
    latestDeployments?: {
        uid: string;
        url?: string;
        state?: string;
    }[] | null;
    targets?: Record<string, unknown> | null;
}
interface VercelDomain {
    name: string;
    apexName?: string | null;
    projectId?: string | null;
    redirect?: string | null;
    redirectStatusCode?: number | null;
    gitBranch?: string | null;
    verified?: boolean | null;
    createdAt?: number | null;
}
interface VercelEnvVar {
    id: string;
    key: string;
    type?: string | null;
    target?: string[] | null;
    value?: string | null;
    configurationId?: string | null;
    createdAt?: number | null;
    updatedAt?: number | null;
}
interface VercelDeployment {
    uid: string;
    name?: string | null;
    url?: string | null;
    state?: string | null;
    type?: string | null;
    creator?: {
        uid: string;
        email?: string;
        username?: string;
    } | null;
    createdAt?: number | null;
    buildingAt?: number | null;
    ready?: number | null;
    meta?: Record<string, unknown> | null;
    target?: string | null;
}
interface VercelDeploymentEvent {
    type?: string | null;
    created?: number | null;
    payload?: Record<string, unknown> | null;
}
interface VercelListOptions {
    limit?: number;
    cursor?: string;
}
interface VercelDriver {
    getUser(): Promise<VercelUser>;
    listTeams(options?: VercelListOptions): Promise<{
        teams: VercelTeam[];
    }>;
    getTeam(teamId: string): Promise<VercelTeam>;
    listTeamMembers(teamId: string): Promise<{
        members: VercelTeamMember[];
    }>;
    listProjects(options?: VercelListOptions): Promise<{
        projects: VercelProject[];
    }>;
    getProject(projectIdOrName: string): Promise<VercelProject>;
    listProjectDomains(projectId: string): Promise<{
        domains: VercelDomain[];
    }>;
    listProjectEnv(projectId: string): Promise<{
        envs: VercelEnvVar[];
    }>;
    listProjectDeployments(projectId: string, options?: VercelListOptions): Promise<{
        deployments: VercelDeployment[];
    }>;
    getDeployment(deploymentId: string): Promise<VercelDeployment>;
    listDeploymentEvents(deploymentId: string): Promise<VercelDeploymentEvent[]>;
    close(): Promise<void>;
}

interface VercelConfig {
    token?: string | null;
    teamId?: string | null;
    baseUrl?: string;
    defaultListLimit?: number;
}
interface VercelConfigResolved {
    token: string | null;
    teamId: string | null;
    baseUrl: string;
    defaultListLimit: number;
}
declare function normalizeVercelConfig(input: Record<string, unknown>): VercelConfig;
declare function resolveVercelConfig(config?: VercelConfig): VercelConfigResolved;

declare class VercelAccessor extends Accessor {
    readonly driver: VercelDriver;
    readonly config: VercelConfigResolved;
    constructor(driver: VercelDriver, config: VercelConfigResolved);
}

declare const VERCEL_PROMPT = "{prefix}\n  user.json                  current Vercel user\n  teams/<teamId>/\n      info.json\n      members.json\n  projects/<projectId>/\n      info.json              project metadata\n      domains.json           list of attached domains\n      env.json               environment variable keys (values redacted)\n      deployments/<deploymentId>/\n          info.json          deployment metadata, build state\n          events.json        build/runtime events";

declare const VERCEL_OPS: readonly RegisteredOp[];

declare const VERCEL_COMMANDS: readonly RegisteredCommand[];

declare function read$1(accessor: VercelAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir$1(accessor: VercelAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat$1(_accessor: VercelAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob$1(accessor: VercelAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

type VercelLevel = 'root' | 'user_file' | 'teams_dir' | 'team_dir' | 'team_file' | 'projects_dir' | 'project_dir' | 'project_file' | 'deployments_dir' | 'deployment_dir' | 'deployment_file' | 'invalid';
interface VercelScope {
    level: VercelLevel;
    teamId: string | null;
    projectId: string | null;
    deploymentId: string | null;
    filename: string | null;
    resourcePath: string;
}
declare const ROOT_ENTRIES$1: readonly string[];
declare const TEAM_FILES: readonly string[];
declare const PROJECT_FILES$1: readonly string[];
declare const PROJECT_SUBDIRS: readonly string[];
declare const DEPLOYMENT_FILES: readonly string[];
declare function detectScope$1(path: PathSpec | string): VercelScope;

declare function getUser$1(accessor: VercelAccessor): Promise<VercelUser>;
declare function listTeams(accessor: VercelAccessor): Promise<VercelTeam[]>;
declare function getTeam(accessor: VercelAccessor, teamId: string): Promise<VercelTeam>;
declare function listTeamMembers(accessor: VercelAccessor, teamId: string): Promise<VercelTeamMember[]>;
declare function listProjects$1(accessor: VercelAccessor): Promise<VercelProject[]>;
declare function getProject$1(accessor: VercelAccessor, projectIdOrName: string): Promise<VercelProject>;
declare function listProjectDomains(accessor: VercelAccessor, projectId: string): Promise<VercelDomain[]>;
declare function listProjectEnv(accessor: VercelAccessor, projectId: string): Promise<VercelEnvVar[]>;
declare function listProjectDeployments(accessor: VercelAccessor, projectId: string): Promise<VercelDeployment[]>;
declare function getDeployment(accessor: VercelAccessor, deploymentId: string): Promise<VercelDeployment>;
declare function listDeploymentEvents(accessor: VercelAccessor, deploymentId: string): Promise<VercelDeploymentEvent[]>;

interface HttpVercelDriverOptions {
    baseUrl?: string;
    token?: string | null;
    teamId?: string | null;
    fetchImpl?: typeof fetch;
    headers?: Record<string, string>;
}
declare class HttpVercelDriver implements VercelDriver {
    readonly baseUrl: string;
    private readonly token;
    private readonly teamId;
    private readonly fetchImpl;
    private readonly headers;
    constructor(options?: HttpVercelDriverOptions);
    private buildHeaders;
    private getJson;
    getUser(): Promise<VercelUser>;
    listTeams(options?: VercelListOptions): Promise<{
        teams: VercelTeam[];
    }>;
    getTeam(teamId: string): Promise<VercelTeam>;
    listTeamMembers(teamId: string): Promise<{
        members: VercelTeamMember[];
    }>;
    listProjects(options?: VercelListOptions): Promise<{
        projects: VercelProject[];
    }>;
    getProject(projectIdOrName: string): Promise<VercelProject>;
    listProjectDomains(projectId: string): Promise<{
        domains: VercelDomain[];
    }>;
    listProjectEnv(projectId: string): Promise<{
        envs: VercelEnvVar[];
    }>;
    listProjectDeployments(projectId: string, options?: VercelListOptions): Promise<{
        deployments: VercelDeployment[];
    }>;
    getDeployment(deploymentId: string): Promise<VercelDeployment>;
    listDeploymentEvents(deploymentId: string): Promise<VercelDeploymentEvent[]>;
    close(): Promise<void>;
}

interface PostHogUser {
    uuid?: string | null;
    distinct_id?: string | null;
    email?: string | null;
    first_name?: string | null;
    last_name?: string | null;
    organization?: {
        id: string;
        name?: string;
    } | null;
}
interface PostHogProject {
    id: number;
    uuid?: string | null;
    name: string;
    api_token?: string | null;
    organization?: string | null;
    created_at?: string | null;
    is_demo?: boolean | null;
}
interface PostHogPaged<T> {
    count?: number | null;
    next?: string | null;
    previous?: string | null;
    results: T[];
}
interface PostHogDriver {
    getUser(): Promise<PostHogUser>;
    listProjects(): Promise<PostHogProject[]>;
    getProject(projectId: number | string): Promise<PostHogProject>;
    listFeatureFlags(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listCohorts(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listDashboards(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listInsights(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listPersons(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    close(): Promise<void>;
}

interface PostHogConfig {
    apiKey?: string | null;
    host?: string;
    defaultListLimit?: number;
}
interface PostHogConfigResolved {
    apiKey: string | null;
    baseUrl: string;
    defaultListLimit: number;
}
declare function normalizePostHogConfig(input: Record<string, unknown>): PostHogConfig;
declare function resolvePostHogConfig(config?: PostHogConfig): PostHogConfigResolved;

declare class PostHogAccessor extends Accessor {
    readonly driver: PostHogDriver;
    readonly config: PostHogConfigResolved;
    constructor(driver: PostHogDriver, config: PostHogConfigResolved);
}

declare const POSTHOG_PROMPT = "{prefix}\n  user.json                          current PostHog user (api/users/@me)\n  projects/<projectId>/\n      info.json                      project metadata\n      feature_flags.json             feature flag definitions\n      cohorts.json                   cohorts\n      dashboards.json                saved dashboards\n      insights.json                  saved insights\n      persons.json                   persons (top N most recent)\n  Each *.json is a paginated snapshot \u2014 top defaultListLimit rows.";

declare const POSTHOG_OPS: readonly RegisteredOp[];

declare const POSTHOG_COMMANDS: readonly RegisteredCommand[];

declare function read(accessor: PostHogAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<Uint8Array>;

declare function readdir(accessor: PostHogAccessor, path: PathSpec | string, index?: IndexCacheStore): Promise<string[]>;

declare function stat(_accessor: PostHogAccessor, path: PathSpec | string, _index?: IndexCacheStore): Promise<FileStat>;

declare function resolveGlob(accessor: PostHogAccessor, paths: readonly PathSpec[], index?: IndexCacheStore): Promise<PathSpec[]>;

type PostHogLevel = 'root' | 'user_file' | 'projects_dir' | 'project_dir' | 'project_file' | 'invalid';
interface PostHogScope {
    level: PostHogLevel;
    projectId: string | null;
    filename: string | null;
    resourcePath: string;
}
declare const ROOT_ENTRIES: readonly string[];
declare const PROJECT_FILES: readonly string[];
declare function detectScope(path: PathSpec | string): PostHogScope;

declare function getUser(accessor: PostHogAccessor): Promise<PostHogUser>;
declare function listProjects(accessor: PostHogAccessor): Promise<PostHogProject[]>;
declare function getProject(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogProject>;
declare function listFeatureFlags(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogPaged<unknown>>;
declare function listCohorts(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogPaged<unknown>>;
declare function listDashboards(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogPaged<unknown>>;
declare function listInsights(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogPaged<unknown>>;
declare function listPersons(accessor: PostHogAccessor, projectId: number | string): Promise<PostHogPaged<unknown>>;

interface HttpPostHogDriverOptions {
    baseUrl?: string;
    apiKey?: string | null;
    fetchImpl?: typeof fetch;
    headers?: Record<string, string>;
}
declare class HttpPostHogDriver implements PostHogDriver {
    readonly baseUrl: string;
    private readonly apiKey;
    private readonly fetchImpl;
    private readonly headers;
    constructor(options?: HttpPostHogDriverOptions);
    private buildHeaders;
    private getJson;
    getUser(): Promise<PostHogUser>;
    listProjects(): Promise<PostHogProject[]>;
    getProject(projectId: number | string): Promise<PostHogProject>;
    listFeatureFlags(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listCohorts(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listDashboards(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listInsights(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    listPersons(projectId: number | string, limit?: number): Promise<PostHogPaged<unknown>>;
    close(): Promise<void>;
}

declare function setHttpProxyBase(base: string | null): void;

declare const VERSION = "0.0.0";

export { AMBIGUOUS_NAMES, ARITH_DELIMITERS, ARITH_OPERATORS, Accessor, type AggregateResult, AsyncLineIterator, type AsyncReadBytesFn, type AsyncReaddirFn, type AsyncStatFn, BOX_API_BASE, BOX_COMMANDS, BOX_PROMPT, BOX_TOKEN_URL, BOX_VFS_OPS, BarrierPolicy, BoxAccessor, BoxApiError, type BoxConfig, type BoxItem, type BoxItemType, BoxTokenManager, type BoxcanvasProcessed, type BoxcanvasProcessedWidget, type BoxnoteParagraph, type BoxnoteProcessed, BreakSignal, BrowserDiscordTransport, type BrowserDiscordTransportOptions, BrowserSlackTransport, type BrowserSlackTransportOptions, type ByteSource, type CIArtifact, type CIJob, type CIRun, type CITransport, type CIWorkflow, COMPOUND_EXTENSIONS, CachableAsyncIterator, CacheEntry, type CacheEntryInit, CallFrame, type CallFrameInit, CallStack, type CommandFn, type CommandFnResult, type CommandOptions, type CommandOpts, CommandSpec, type CommandSpecInit, ConsistencyPolicy, ContinueSignal, type CrossCommandOptions, DEFAULT_AGENT_ID, DEFAULT_SESSION_ID, DISCORD_API, DISCORD_COMMANDS, DISCORD_PROMPT, DISCORD_VFS_OPS, DISCORD_WRITE_PROMPT, DOCS_API_BASE, DRIVE_API_BASE, DROPBOX_API_BASE, DROPBOX_COMMANDS, DROPBOX_CONTENT_BASE, DROPBOX_PROMPT, DROPBOX_TOKEN_URL, DROPBOX_VFS_OPS, DevFiles, DevResource, DevStore, DiscordAccessor, DiscordApiError, type DiscordMethod, type DiscordResourceLike, type DiscordResponse, type DiscordTransport, type DispatchFn, type DocEntry, type DriveFile, type DriveOwner, DropboxAccessor, DropboxApiError, type DropboxConfig, type DropboxEntry, type DropboxEntryTag, DropboxTokenManager, type ExecuteFn, type ExecuteNodeDeps, type ExecuteNodeFn, type ExecuteOptions, ExecuteResult, type ExecuteStringFn, ExecutionHistory, type ExecutionHistoryOptions, ExecutionNode, type ExecutionNodeInit, ExecutionRecord, type ExecutionRecordInit, FILE_MIME_MAP, type FieldNormalizer, type FileCache, FileStat, type FileStatInit, FileType, type FiletypeFn, type FindOptions, GDOCS_COMMANDS, GDOCS_PROMPT, GDOCS_VFS_OPS, GDOCS_WRITE_PROMPT, GDRIVE_COMMANDS, GDRIVE_PROMPT, GDRIVE_VFS_OPS, GDocsAccessor, GDriveAccessor, GENERAL_BC, GENERAL_COMMANDS, GENERAL_CURL, GENERAL_DATE, GENERAL_EXPR, GENERAL_SEQ, GENERAL_WGET, GITHUB_API_BASE, GITHUB_API_VERSION, GITHUB_CI_COMMANDS, GITHUB_CI_PROMPT, GITHUB_CI_VFS_OPS, GITHUB_COMMANDS, GITHUB_PROMPT, GITHUB_VFS_OPS, GMAIL_API_BASE, GMAIL_COMMANDS, GMAIL_PROMPT, GMAIL_VFS_OPS, GMAIL_WRITE_PROMPT, GSHEETS_COMMANDS, GSHEETS_PROMPT, GSHEETS_VFS_OPS, GSHEETS_WRITE_PROMPT, GSLIDES_COMMANDS, GSLIDES_PROMPT, GSLIDES_VFS_OPS, GSLIDES_WRITE_PROMPT, GSheetsAccessor, GSlidesAccessor, GitHubAccessor, GitHubApiError, type GitHubBlob, GitHubCIAccessor, type GitHubCIResourceLike, type GitHubRepoInfo, type GitHubResourceLike, type GitHubTransport, type TreeEntry as GitHubTreeEntry, type GitHubTreeItem, GmailAccessor, type GmailAddress, type GmailAttachmentInfo, type GmailHeader, type GmailLabel, type GmailMessageProcessed, type GmailMessageRaw, type GmailMessageStub, type GmailPayload, type GmailScope, type GmailSearchRow, GoogleApiAccessor, GoogleApiError, type GoogleConfig, type GrepLinesOptions, type GrepStreamOptions, HttpCITransport, HttpDiscordTransport, HttpGitHubTransport, HttpLangfuseTransport, type HttpLangfuseTransportOptions, HttpLinearTransport, type HttpLinearTransportOptions, HttpPostHogDriver, type HttpPostHogDriverOptions, HttpSSCholarDriver, type HttpSSCholarDriverOptions, HttpSlackTransport, HttpTrelloTransport, type HttpTrelloTransportOptions, HttpVercelDriver, type HttpVercelDriverOptions, IOResult, type IOResultInit, IndexCacheStore, type IndexConfig, IndexEntry, type IndexEntryInit, Job, JobStatus, JobTable, type JobTaskResult, LANGFUSE_COMMANDS, LANGFUSE_PROMPT, LANGFUSE_VFS_OPS, LINEAR_COMMANDS, LINEAR_PROMPT, LINEAR_VFS_OPS, LINEAR_WRITE_PROMPT, LangfuseAccessor, LangfuseApiError, type LangfuseResourceLike, type LangfuseScope, type LangfuseTransport, LinearAccessor, LinearApiError, type LinearReaddirFilter, type LinearResourceLike, type LinearTransport, type ListResult, LogEntry, type LogEntryInit, type LookupResult, LookupStatus, MCPNotionTransport, type MCPNotionTransportOptions, MIME_TO_EXT, MONGODB_COMMANDS, MONGODB_OPS, MONGODB_PROMPT, MemoryOAuthClientProvider, type MemoryOAuthClientProviderOptions, type CollectionMatches as MongoCollectionMatches, MongoDBAccessor, type MongoDBConfig, type MongoDBConfigResolved, type MongoDBScope, type MongoDriver, type MongoFindOptions, Mount, type MountInit, MountMode, MountRegistry, NOOPAccessor, NOTION_COMMANDS, NOTION_PROMPT, NOTION_VFS_OPS, NOTION_WRITE_PROMPT, NodeSlackTransport, NodeType, NotionAccessor, NotionMCPError, type NotionResourceLike, type NotionTransport, Observer, type OpFn, type OpKwargs, type OpOptions, OpRecord, type OpRecordInit, Operand, type OperandInit, OperandKind, OpsRegistry, Option, type OptionInit, type OptionalPeerConfig, POSTGRES_COMMANDS, POSTGRES_OPS, POSTGRES_PROMPT, POSTHOG_COMMANDS, POSTHOG_OPS, PROJECT_FILES as POSTHOG_PROJECT_FILES, POSTHOG_PROMPT, ROOT_ENTRIES as POSTHOG_ROOT_ENTRIES, ParsedArgs, type ParsedArgsInit, PathSpec, type PathSpecInit, type PgDriver, type PgQueryResult, PostHogAccessor, type PostHogConfig, type PostHogConfigResolved, type PostHogDriver, type PostHogLevel, type PostHogPaged, type PostHogProject, type PostHogScope, type PostHogUser, PostgresAccessor, type PostgresConfig, type PostgresConfigResolved, Precision, type ProvisionFn, ProvisionResult, type ProvisionResultInit, RAMAccessor, RAMFileCacheStore, RAMIndexCacheStore, RAMResource, RAMStore, RAM_AWK, RAM_BASE64, RAM_BASENAME, RAM_CAT, RAM_CAT_FEATHER, RAM_CAT_HDF5, RAM_CAT_PARQUET, RAM_CMP, RAM_COLUMN, RAM_COMM, RAM_COMMANDS, RAM_CP, RAM_CSPLIT, RAM_CUT, RAM_CUT_FEATHER, RAM_CUT_HDF5, RAM_CUT_PARQUET, RAM_DIFF, RAM_DIRNAME, RAM_DU, RAM_EXPAND, RAM_FILE, RAM_FILE_FEATHER, RAM_FILE_HDF5, RAM_FILE_PARQUET, RAM_FIND, RAM_FMT, RAM_FOLD, RAM_GREP, RAM_GREP_FEATHER, RAM_GREP_HDF5, RAM_GREP_PARQUET, RAM_GUNZIP, RAM_GZIP, RAM_HEAD, RAM_HEAD_FEATHER, RAM_HEAD_HDF5, RAM_HEAD_PARQUET, RAM_ICONV, RAM_JOIN, RAM_JQ, RAM_LN, RAM_LOOK, RAM_LS, RAM_LS_FEATHER, RAM_LS_HDF5, RAM_LS_PARQUET, RAM_MD5, RAM_MKDIR, RAM_MKTEMP, RAM_MV, RAM_NL, RAM_OPS, RAM_PASTE, RAM_PATCH, RAM_READLINK, RAM_REALPATH, RAM_REV, RAM_RG, RAM_RM, RAM_SED, RAM_SHA256SUM, RAM_SHUF, RAM_SORT, RAM_SPLIT, RAM_STAT, RAM_STAT_FEATHER, RAM_STAT_HDF5, RAM_STAT_PARQUET, RAM_STRINGS, RAM_TAC, RAM_TAIL, RAM_TAIL_FEATHER, RAM_TAIL_HDF5, RAM_TAIL_PARQUET, RAM_TAR, RAM_TEE, RAM_TOUCH, RAM_TR, RAM_TREE, RAM_TSORT, RAM_UNEXPAND, RAM_UNIQ, RAM_UNZIP, RAM_WC, RAM_WC_FEATHER, RAM_WC_HDF5, RAM_WC_PARQUET, RAM_XXD, RAM_ZCAT, RAM_ZGREP, RAM_ZIP, Redirect, type RedirectInit, RedirectKind, RegisteredCommand, type RegisteredCommandInit, type RegisteredOp, type Resource, ResourceName, ResourceType, type ResourceWithGlob, ReturnSignal, type RgFolderFiletypeOptions, type RgFullOptions, S3Accessor, type S3BrowserOperation, type S3BrowserPresignedUrlProvider, type S3BrowserSignOptions, type S3Config, type S3ConfigRedacted, S3IndexEntry, type S3IndexEntryInit, type S3Object, type S3ResourceLike, S3_COMMANDS, S3_OPS, S3_PROMPT, SCOPE_ERROR as S3_SCOPE_ERROR, SHEETS_API_BASE, SLACK_COMMANDS, SLACK_PROMPT, SLACK_VFS_OPS, SLACK_WRITE_PROMPT, SLIDES_API_BASE, BUILTIN_SPECS as SPECS, SSCHOLAR_AUTHOR_COMMANDS, AUTHOR_FILES as SSCHOLAR_AUTHOR_FILES, SSCHOLAR_AUTHOR_OPS, SSCHOLAR_AUTHOR_PROMPT, SSCHOLAR_FIELDS, SSCHOLAR_FIELD_SLUGS, SSCHOLAR_PAPER_COMMANDS, PAPER_FILES as SSCHOLAR_PAPER_FILES, SSCHOLAR_PAPER_OPS, SSCHOLAR_PAPER_PROMPT, SSCHOLAR_YEARS, SSCholarAccessor, type SSCholarAuthor, type SSCholarAuthorLevel, type SSCholarAuthorPapersOptions, type SSCholarAuthorPapersResult, type SSCholarAuthorProfile, type SSCholarAuthorScope, type SSCholarAuthorSearchResult, type SSCholarConfig, type SSCholarConfigResolved, type SSCholarDriver, type SSCholarPaper, type SSCholarPaperLevel, type SSCholarPaperScope, type SSCholarSearchOptions, type SSCholarSearchResult, type SSCholarSnippet, type SSCholarSnippetMatch, type SSCholarSnippetSearchResult, type SSCholarTLDR, type SedCommand, Session, type SessionInit, SessionManager, type SheetEntry, SheetsApiError, ShellBuiltin, type ShellParser, type ShellParserConfig, SlackAccessor, SlackApiError, type SlackResponse, type SlackTransport, type SlideEntry, type SortKeyOptions, TOKEN_BUFFER_SECONDS, TOKEN_URL, TRELLO_COMMANDS, TRELLO_PROMPT, TRELLO_VFS_OPS, TRELLO_WRITE_PROMPT, type TSNodeLike, TYPE_EXTENSIONS, type TarEntry, TokenManager, TrelloAccessor, TrelloApiError, type TrelloReaddirFilter, type TrelloResourceLike, type TrelloTransport, VERCEL_COMMANDS, DEPLOYMENT_FILES as VERCEL_DEPLOYMENT_FILES, VERCEL_OPS, PROJECT_FILES$1 as VERCEL_PROJECT_FILES, PROJECT_SUBDIRS as VERCEL_PROJECT_SUBDIRS, VERCEL_PROMPT, ROOT_ENTRIES$1 as VERCEL_ROOT_ENTRIES, TEAM_FILES as VERCEL_TEAM_FILES, VERSION, type ValueTransform, VercelAccessor, type VercelConfig, type VercelConfigResolved, type VercelDeployment, type VercelDeploymentEvent, type VercelDomain, type VercelDriver, type VercelEnvVar, type VercelLevel, type VercelListOptions, type VercelProject, type VercelScope, type VercelTeam, type VercelTeamMember, type VercelUser, WORKSPACE_MIMES, Workspace, type WorkspaceOptions, WorkspaceRunner, applyBarrier, asyncChain, boxAuthHeaders, downloadFile as boxDownloadFile, downloadFileStream as boxDownloadFileStream, boxGet, boxGetBytes, getExtractedText as boxGetExtractedText, getFileMetadata as boxGetFileMetadata, getFolderMetadata as boxGetFolderMetadata, boxGetStream, listFolderItems as boxListFolderItems, read$7 as boxRead, readdir$7 as boxReaddir, refreshAccessToken as boxRefreshAccessToken, resolveGlob$7 as boxResolveGlob, searchItems as boxSearchItems, stat$7 as boxStat, stream as boxStream, catProvision, classifyBarePath, classifyParts, classifyWord, closeQuietly, command, compareKeys, compilePattern, concatAggregate, copy, countNewlines, create, createShellParser, crossCommand, decodeBase64, defaultFingerprint, deflateRaw, detectFileType, detectScope$3 as detectMongoScope, detectScope as detectPostHogScope, detectScope$4 as detectPostgresScope, detectAuthorScope as detectSSCholarAuthorScope, detectScope$2 as detectSSCholarPaperScope, detectScope$1 as detectVercelScope, read$j as discordRead, readdir$j as discordReaddir, stat$j as discordStat, drain, dropboxAuthHeaders, dropboxDownload, dropboxDownloadStream, getMetadata as dropboxGetMetadata, listFolder as dropboxListFolder, read$8 as dropboxRead, readdir$8 as dropboxReaddir, refreshAccessToken$1 as dropboxRefreshAccessToken, resolveGlob$8 as dropboxResolveGlob, dropboxRpc, searchFiles as dropboxSearchFiles, stat$8 as dropboxStat, stream$1 as dropboxStream, du, duAll, edScript, encodeBase64, escapeRegex, evalJsonlStream, executeNode, executeProgram, exists, exitOnEmpty, expandAndClassify, expandBraces, expandNode, expandParts, expandTestExpr, extractWriteData, cat$2 as featherCat, cut$2 as featherCut, describe$2 as featherDescribe, grep$2 as featherGrep, head$2 as featherHead, ls$2 as featherLs, lsFallback$2 as featherLsFallback, stat$o as featherStat, tail$2 as featherTail, wc$2 as featherWc, fetchBlob as fetchGitHubBlob, fetchDirTree as fetchGitHubDirTree, fetchRepoInfo as fetchGitHubRepoInfo, fetchTree as fetchGitHubTree, find, formatFileResult, formatLsLong, appendText as gdocsAppendText, batchUpdate$2 as gdocsBatchUpdate, createDoc as gdocsCreateDoc, makeFilename$2 as gdocsMakeFilename, read$c as gdocsRead, readDoc as gdocsReadDoc, readdir$c as gdocsReaddir, resolveGlob$c as gdocsResolveGlob, sanitizeTitle$2 as gdocsSanitizeTitle, stat$c as gdocsStat, stream$5 as gdocsStream, read$9 as gdriveRead, readBytes as gdriveReadBytes, readdir$9 as gdriveReaddir, resolveGlob$9 as gdriveResolveGlob, stat$9 as gdriveStat, stream$2 as gdriveStream, getCaseItems, getCaseWord, getCommandAssignments, getCommandName, getDeclarationAssignments, getDeclarationKeyword, getExtension, getForParts, getFunctionBody, getFunctionName, getHeredocMeta, getHeredocParts, getHerestringContent, getIfBranches, getListParts, getNegatedCommand, getParts, getPipelineCommands, getProcessSubCommand, getRedirectTargetNode, getRedirects, getSubshellBody, getTestArgv, getText, getUnsetNames, getWhileParts, downloadArtifact as githubCiDownloadArtifact, downloadJobLog as githubCiDownloadJobLog, getJob as githubCiGetJob, getRun as githubCiGetRun, getWorkflow as githubCiGetWorkflow, listAnnotations as githubCiListAnnotations, listArtifacts as githubCiListArtifacts, listJobsForRun as githubCiListJobsForRun, listRuns as githubCiListRuns, listWorkflows as githubCiListWorkflows, read$d as githubCiRead, readdir$d as githubCiReaddir, resolveGlob$d as githubCiResolveGlob, stat$d as githubCiStat, stream$6 as githubCiStream, indexEntryFromTree as githubIndexEntryFromTree, makeTreeEntry as githubMakeTreeEntry, narrowPaths as githubNarrowPaths, read$e as githubRead, readdir$e as githubReaddir, resolveGlob$e as githubResolveGlob, search as githubSearchCode, stat$e as githubStat, stream$7 as githubStream, decodeBody as gmailDecodeBody, detectScope$5 as gmailDetectScope, extractAttachments as gmailExtractAttachments, extractHeader as gmailExtractHeader, formatGrepResults$2 as gmailFormatGrepResults, forwardMessage as gmailForwardMessage, getAttachment as gmailGetAttachment, getMessageProcessed as gmailGetMessageProcessed, getMessageRaw as gmailGetMessageRaw, listLabels as gmailListLabels, listMessages as gmailListMessages, parseAddress as gmailParseAddress, parseAddressList as gmailParseAddressList, read$6 as gmailRead, readdir$6 as gmailReaddir, replyAllMessage as gmailReplyAllMessage, replyMessage as gmailReplyMessage, resolveGlob$6 as gmailResolveGlob, searchMessages as gmailSearchMessages, sendMessage as gmailSendMessage, stat$6 as gmailStat, downloadFile$1 as googleDriveDownloadFile, downloadFileStream$1 as googleDriveDownloadFileStream, getFileMetadata$1 as googleDriveGetFileMetadata, listAllFiles as googleDriveListAllFiles, listFiles as googleDriveListFiles, googleGet, googleGetBytes, googleGetStream, googleHeaders, googlePost, googlePut, grepContextLines, grepFilesOnly, grepLines, grepRecursive, grepStream, appendValues as gsheetsAppendValues, batchUpdate$1 as gsheetsBatchUpdate, createSpreadsheet as gsheetsCreateSpreadsheet, fetchSheetNames as gsheetsFetchSheetNames, makeFilename$1 as gsheetsMakeFilename, read$b as gsheetsRead, readSpreadsheet as gsheetsReadSpreadsheet, readValues as gsheetsReadValues, readdir$b as gsheetsReaddir, resolveGlob$b as gsheetsResolveGlob, sanitizeTitle$1 as gsheetsSanitizeTitle, stat$b as gsheetsStat, stream$4 as gsheetsStream, writeValues as gsheetsWriteValues, batchUpdate as gslidesBatchUpdate, createPresentation as gslidesCreatePresentation, makeFilename as gslidesMakeFilename, read$a as gslidesRead, readPresentation as gslidesReadPresentation, readdir$a as gslidesReaddir, resolveGlob$a as gslidesResolveGlob, sanitizeTitle as gslidesSanitizeTitle, stat$a as gslidesStat, stream$3 as gslidesStream, guessType, gunzip, gzip, handleBackground, handleCase, handleCd, handleCommand, handleConnection, handleCrossMount, handleEcho, handleEval, handleExport, handleFor, handleIf, handleJobs, handleKill, handleLocal, handlePipe, handlePrintenv, handlePrintf, handlePs, handleRead, handleRedirect, handleReturn, handleSelect, handleSet, handleShift, handleSleep, handleSource, handleSubshell, handleTest, handleTrap, handleUnset, handleUntil, handleWait, handleWhile, cat$1 as hdf5Cat, cut$1 as hdf5Cut, describe$1 as hdf5Describe, grep$1 as hdf5Grep, head$1 as hdf5Head, ls$1 as hdf5Ls, lsFallback$1 as hdf5LsFallback, stat$n as hdf5Stat, tail$1 as hdf5Tail, wc$1 as hdf5Wc, headProvision, headerAggregate, humanSize, inflateRaw, interpretEscapes, isCrossMount, isJsonlPath, isStreamableJsonlExpr, jqEval, jqProvision, detectScope$6 as langfuseDetectScope, read$g as langfuseRead, readdir$g as langfuseReaddir, stat$g as langfuseStat, read$i as linearRead, readdir$i as linearReaddir, stat$i as linearStat, loadOptionalPeer, lookupVar, materialize, materializeStdout, md5, md5Hex, mergeStdoutStderr, mkdir, countDocuments as mongoCountDocuments, findDocuments as mongoFindDocuments, formatGrepResults as mongoFormatGrepResults, listCollections as mongoListCollections, listDatabases as mongoListDatabases, listIndexes as mongoListIndexes, read$4 as mongoRead, readdir$4 as mongoReaddir, searchCollection as mongoSearchCollection, searchDatabase as mongoSearchDatabase, stat$4 as mongoStat, normMountPrefix, normalizeFields, normalizeMongoDBConfig, normalizePostHogConfig, normalizePostgresConfig, normalizeSSCholarConfig, normalizeVercelConfig, read$h as notionRead, readdir$h as notionReaddir, stat$h as notionStat, op, cat as parquetCat, cut as parquetCut, describe as parquetDescribe, grep as parquetGrep, head as parquetHead, ls as parquetLs, lsFallback as parquetLsFallback, stat$m as parquetStat, tail as parquetTail, wc as parquetWc, parseCommand, parseJsonAuto, parseJsonPath, parseLimit, parseN, parseOneCommand, parseProgram, parseToKwargs, peekExitCode, posixNormpath, formatGrepResults$1 as postgresFormatGrepResults, read$5 as postgresRead, readdir$5 as postgresReaddir, searchDatabase$1 as postgresSearchDatabase, searchEntity as postgresSearchEntity, searchKind as postgresSearchKind, searchSchema as postgresSearchSchema, stat$5 as postgresStat, getProject as posthogGetProject, getUser as posthogGetUser, listCohorts as posthogListCohorts, listDashboards as posthogListDashboards, listFeatureFlags as posthogListFeatureFlags, listInsights as posthogListInsights, listPersons as posthogListPersons, listProjects as posthogListProjects, read as posthogRead, readdir as posthogReaddir, stat as posthogStat, prefixAggregate, processBoxcanvas, processBoxnote, quietMatch, rangeRead, read$l as read, readStdinAsync, readTar, readdir$l as readdir, record, recordStream, redactConfig as redactS3Config, refreshAccessToken$2 as refreshAccessToken, registerOp, rename, resolveDiscordGlob, resolveFirstPath, resolveGlobs, resolveLangfuseGlob, resolveLinearGlob, resolveMongoDBConfig, resolveGlob$4 as resolveMongoGlob, resolveNotionGlob, resolvePath, resolvePostHogConfig, resolveGlob as resolvePostHogGlob, resolvePostgresConfig, resolveGlob$5 as resolvePostgresGlob, resolveGlob$f as resolveS3Glob, resolveGlob$2 as resolveSSCholarAuthorGlob, resolveSSCholarConfig, resolveGlob$3 as resolveSSCholarPaperGlob, resolveSlackGlob, resolveSource, resolveTrelloGlob, resolveVercelConfig, resolveGlob$1 as resolveVercelGlob, rgFolderFiletype, rgFull, rgMatchesFilter, rmR, rmdir, runWithRecording, fileReadProvision as s3FileReadProvision, headTailProvision as s3HeadTailProvision, metadataProvision as s3MetadataProvision, safeEval, executeProgram as sedExecuteProgram, parseOneCommand as sedParseOneCommand, parseProgram as sedParseProgram, setHttpProxyBase, setVirtualPrefix, sha256, sha256Hex, shlexSplit, read$k as slackRead, readdir$k as slackReaddir, stat$k as slackStat, snakeToCamel, sortKey, specOf, read$2 as sscholarAuthorRead, readdir$2 as sscholarAuthorReaddir, stat$2 as sscholarAuthorStat, fieldToSlug as sscholarFieldToSlug, getAuthor as sscholarGetAuthor, getAuthorPapers as sscholarGetAuthorPapers, getPaper as sscholarGetPaper, read$3 as sscholarPaperRead, readdir$3 as sscholarPaperReaddir, stat$3 as sscholarPaperStat, searchAuthors as sscholarSearchAuthors, searchPapers as sscholarSearchPapers, searchPapersByField as sscholarSearchPapersByField, searchSnippets as sscholarSearchSnippets, slugToField as sscholarSlugToField, stat$l as stat, statProvision, stream$8 as stream, stripPrefixFromPathKwargs, tailBytes, throwUnsupported, translateReplacement, read$f as trelloRead, readdir$f as trelloReaddir, stat$f as trelloStat, truncate, unescapePath, unifiedDiff, unlink, getDeployment as vercelGetDeployment, getProject$1 as vercelGetProject, getTeam as vercelGetTeam, getUser$1 as vercelGetUser, listDeploymentEvents as vercelListDeploymentEvents, listProjectDeployments as vercelListProjectDeployments, listProjectDomains as vercelListProjectDomains, listProjectEnv as vercelListProjectEnv, listProjects$1 as vercelListProjects, listTeamMembers as vercelListTeamMembers, listTeams as vercelListTeams, read$1 as vercelRead, readdir$1 as vercelReaddir, stat$1 as vercelStat, wcAggregate, wrapBytes, wrapCachableStreams, write, writeTar, yieldBytes };
